[
  {
    "id": "llamafactory-pr-10650",
    "nodeId": "PR_kwDOJodxvs7yLeYc",
    "repository": "hiyouga/LlamaFactory",
    "number": 10650,
    "type": "pr",
    "title": "fix(data): GLM4-MOE tool_extractor never overridden, round-trip fails",
    "bodyExcerpt": "## What does this PR do?\n\n`GLM4MOEToolUtils` overrides `tool_formatter`/`function_formatter` to produce its own XML-ish format (`<tool_call>{name}\\n<arg_key>...</arg_key>\\n<arg_value>...</arg_value>...`, matching `GLM4_MOE_TOOL_PROMPT`), but never overrides `tool_extractor` — it inherits `QwenToolUtils.tool_extractor`, which does `json.loads()` on the `<tool_call>...</tool_call>` body. That body is never valid JSON for this format, so extraction always fails and falls through to returning the raw, unparsed string instead of `list[FunctionCall]`.\n\nSeparately, `function_formatter` never emitted the closing `</tool_call>` tag that `GLM4_MOE_TOOL_PROMPT` itself documents (`\"\\n...\\n</tool_call>\\n\"`), which the new extractor needs to delimit consecutive tool calls when the model emits more than one.\n\n```python\ntu = get_tool_utils(\"glm4_moe\")\nformatted = tu.function_formatter([FunctionCall(\"get_weather\", '{\"city\": \"Paris\"}')])\ntu.tool_extractor(formatted)\n# before: returns the same raw string unchanged (not parsed)\n# after:  [FunctionCall(name='get_weather', arguments='{\"city\": \"Paris\"}')]\n```\n\nConfirmed this is live, registered code: `\"glm4_moe\"` is in the `TOOLS` registry and wired into multiple real templates in `src/llamafactory/data/template.py` — not experimental/dead code. Any chat inference using a glm4_moe-family template with tool calling currently cannot parse the model's own generated tool calls back into structured `FunctionCall`s.\n\n## Fix\n\n- Add a `tool_extractor` override to `GLM4MOEToolUtils`, mirroring the regex-based extraction pattern already used by sibling classes (`MiniMaxM2ToolUtils`/`Qwen35ToolUtils`) for their own XML-ish formats: match `<tool_call>NAME ... </tool_call>` blocks, then pull `<arg_key>/<arg_value>` pairs out of each block (with a `json.loads` attempt per value, falling back to the raw string — same convention as the sibling extractors).\n- Add the missing closing `</tool_call>` tag in `function_formatter`.\n\n## Tests\n\nAdded to `tests/da",
    "author": "chuenchen309",
    "state": "open",
    "labels": [],
    "url": "https://github.com/hiyouga/LlamaFactory/pull/10650",
    "createdAt": "2026-07-15T19:14:17.000Z",
    "updatedAt": "2026-08-29T08:34:19.000Z",
    "mergedAt": null,
    "firstSeenAt": "2026-08-30T05:39:24.275Z",
    "contentHash": "sha256:6fd17a4f1297c742952d7761d7e8babe71a37771881f210963c45787fb759019",
    "category": "FIX",
    "categorySource": "rules",
    "summary": null
  },
  {
    "id": "llamafactory-pr-10648",
    "nodeId": "PR_kwDOJodxvs7x6lpi",
    "repository": "hiyouga/LlamaFactory",
    "number": 10648,
    "type": "pr",
    "title": "feat: add MTP support for SFT",
    "bodyExcerpt": "# What does this PR do?\r\n\r\nAdds optional Multi-Token Prediction (MTP) training to the SFT trainer. When\r\n`num_mtp_layers > 0`, an external `MtpModel` is attached to the base model and\r\ntrained with a teacher-forcing auxiliary loss. The implementation is\r\nnon-intrusive: no transformers modeling files are modified.\r\n\r\nThis works for both standard 2D RoPE models (e.g., DeepSeek-V3, GLM-4-MoE) and\r\nMRoPE models (e.g., Qwen3.5 / Qwen3.6 dense).\r\n\r\n## Requirements\r\n\r\n- `transformers` must include `transformers.modeling_layers.MtpModel`, introduced\r\n  in [huggingface/transformers#46229](https://github.com/huggingface/transformers/pull/46229).\r\n  Until that PR is released, install from source:\r\n\r\n  ```bash\r\n  pip install git+https://github.com/huggingface/transformers.git\r\n  ```\r\n## Before submitting\r\n\r\n- [x] Did you read the [contributor guideline](https://github.com/hiyouga/LLaMA-Factory/blob/main/.github/CONTRIBUTING.md)?\r\n- [ ] Did you write any new necessary tests?\r\n",
    "author": "curnane-lab",
    "state": "open",
    "labels": [],
    "url": "https://github.com/hiyouga/LlamaFactory/pull/10648",
    "createdAt": "2026-07-15T07:42:31.000Z",
    "updatedAt": "2026-08-23T06:09:12.000Z",
    "mergedAt": null,
    "firstSeenAt": "2026-08-24T10:27:08.865Z",
    "contentHash": "sha256:a1df6c8e45792cb88216c29f018e8cd793efeedf066a97b2a6f4890c17f1ac59",
    "category": "NEW_SUPPORT",
    "categorySource": "ai",
    "summary": {
      "headlineZh": "SFT 新增 MTP 支持",
      "summaryZh": "新增可选的 Multi-Token Prediction（MTP）训练：当 `num_mtp_layers > 0` 时，为基础模型挂载外部 `MtpModel`，并通过教师强制辅助损失进行训练；不修改 Transformers 建模文件，同时支持标准二维 RoPE 与 MRoPE 模型；依赖包含 `transformers.modeling_layers.MtpModel` 的 Transformers 版本；该组件在对应版本发布前需从源码安装",
      "models": [
        "DeepSeek-V3",
        "GLM-4-MoE",
        "Qwen3.5",
        "Qwen3.6"
      ],
      "hardware": [],
      "scenarios": [
        "TRAINING"
      ],
      "category": "NEW_SUPPORT",
      "severity": "INFO",
      "impactScopeZh": "影响 SFT 训练器及支持二维 RoPE 或 MRoPE 的模型。",
      "attention": "ROUTINE",
      "capabilityCandidate": true,
      "evidence": [
        "【标题】feat: add MTP support for SFT",
        "【正文】Adds optional Multi-Token Prediction (MTP) training to the SFT trainer."
      ],
      "truncated": false,
      "model": "gpt-5.6-luna",
      "promptVersion": "activity-v4-complete-brief",
      "generatedAt": "2026-08-24T11:17:54.490Z"
    }
  }
]
