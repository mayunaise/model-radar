{
  "schemaVersion": 1,
  "scope": "daily-incremental",
  "date": "2026-09-05",
  "generatedAt": "2026-09-05T04:49:01.874Z",
  "seed": "2026-09-05",
  "samplePerStratum": 2,
  "summary": {
    "population": 57,
    "sampled": 57,
    "strata": 7,
    "structuralFailures": 0,
    "sourceMismatches": 0,
    "sourceChecksSkipped": 0,
    "classificationDrifts": 0,
    "relevanceFailures": 0,
    "reviewWarnings": 31
  },
  "items": [
    {
      "id": "verl-pr-7417",
      "repository": "volcengine/verl",
      "number": 7417,
      "type": "pr",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[perf, model] feat: pack Ulysses QKV into one all-to-all",
      "bodyExcerpt": "<!-- Title: [perf, model] feat: pack Ulysses QKV into one all-to-all -->\r\n\r\n### What does this PR do?\r\n\r\nReplace three independent Ulysses Q/K/V all-to-all calls with one packed\r\ncollective when all three tensors have the same shape, dtype, and device. Q/K/V\r\nare concatenated along dimension zero, redistributed once, and returned as\r\nsplit views.\r\n\r\nThe helper is wired into Apertus, GLM4V, Llama, Qwen2, Qwen2-VL, and the generic\r\nTransformers monkey patch. Incompatible layouts retain the existing three-call\r\npath. This reduces collective launches from three to one; it does not reduce\r\nthe communicated bytes and temporarily increases HBM usage for the packed\r\nbuffer.\r\n\r\nThis is PR 1 of 3. Later PRs add a pre-packed API and variable-payload GQA\r\nsupport.\r\n\r\n### Checklist Before Starting\r\n\r\n- [x] Searched open PRs for similar work:\r\n  https://github.com/verl-project/verl/pulls?q=is%3Apr+is%3Aopen+ulysses+qkv\r\n  Existing results concern other Ulysses/model support and do not replace three\r\n  equal-layout QKV collectives with one packed collective.\r\n- [x] Title follows `[{modules}] {type}: {description}`:\r\n  `[perf, model] feat: pack Ulysses QKV into one all-to-all`.\r\n\r\n### Test\r\n\r\nRun on 2 x NVIDIA A100-SXM4-40GB:\r\n\r\n```bash\r\nNCCL_P2P_DISABLE=1 NCCL_SHM_DISABLE=1 \\\r\ntorchrun --standalone --nproc-per-node=2 -m pytest -q \\\r\n  tests/special_distributed/test_packed_qkv_ulysses.py\r\n```\r\n\r\nResult: `8 passed` on each rank. The tests compare packed and legacy forward and\r\nbackward results exactly for FP32, BF16, FP8 e4m3, and FP8 e5m2 across both\r\nsupported sequence/head layouts, and assert one versus three collective calls.\r\n\r\nThe final three-PR stack was also tested on 2 x NVIDIA H200 NVL with default\r\nNCCL settings: `14 passed` on each rank in 9.11 seconds.\r\n\r\n```bash\r\npre-commit run --all-files --show-diff-on-failure --color=always\r\n```\r\n\r\nResult on the rebased three-PR series head: every hook passed, including Ruff, Ruff\r\nformat, mypy, config generation, license, device AP",
      "labels": [],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM",
        "GLM4"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "verl-pr-7669",
      "repository": "volcengine/verl",
      "number": 7669,
      "type": "pr",
      "category": "DOCS",
      "categoryValid": true,
      "ruleCategory": "DOCS",
      "classificationConsistent": true,
      "title": "[doc, ci] feat: add Ascend tutorial doc translation pipeline",
      "bodyExcerpt": "### What does this PR do?\r\n\r\nAdd an automated Chinese-to-English documentation translation pipeline for\r\n`docs/ascend_tutorial/`, keeping the English docs in sync with the Chinese\r\nsources.\r\n\r\nThe pipeline:\r\n\r\n- Scans `docs/ascend_tutorial/zh/` (Markdown + reStructuredText, excluding\r\n  `index.rst`) and translates them into English documents under\r\n  `docs/ascend_tutorial/en/`, mirroring the directory structure and keeping\r\n  the original file format (`.md` stays `.md`, `.rst` stays `.rst`).\r\n- Runs on a scheduled GitHub Actions workflow (every 3 days) and on manual\r\n  trigger, then opens an auto-translation PR for review.\r\n- Caches per-block translations as `.po` translation memory under\r\n  `docs/ascend_tutorial/locale/`, so incremental runs only translate changed\r\n  blocks and reuse existing translations.\r\n\r\n### Key Features\r\n\r\n- **Fail-closed translation**: blocks that fail to translate are never\r\n  persisted as fake translations; the affected document is reported as failed\r\n  so no incomplete document is merged.\r\n- **Reliability**: per-block automatic retry with backoff, per-request\r\n  timeout, and a failed-document summary + failed block preview in the logs.\r\n- **Performance**: concurrent block translation (configurable) to keep runtime\r\n  low even on slower models.\r\n- **Model-agnostic**: any OpenAI-compatible LLM can be plugged in by setting\r\n  the endpoint, model name, and API key (defaults: Volcano Engine gateway +\r\n  `glm-5.2`).\r\n- **Quality**: enforces a translation standard (Google Developer\r\n  Documentation Style Guide) via the translation skill document\r\n  (`.agent/skills/translate-ascend/skill.md`), including active voice, simple\r\n  present tense, terminology consistency, and structure preservation.\r\n- **Sphinx-compliant output**: generated RST documents are cleaned up\r\n  (indentation restored, title underlines padded, stray fences removed, line\r\n  endings normalized) so the docs build cleanly with Sphinx.\r\n- **Complete navigation**: all translated Eng",
      "labels": [],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-issue-40926",
      "repository": "vllm-project/vllm",
      "number": 40926,
      "type": "issue",
      "category": "BUG",
      "categoryValid": true,
      "ruleCategory": "BUG",
      "classificationConsistent": true,
      "title": "[Bug]: V1 engine + MTP + GLM-5.1 (DSA + MoE + MLA) — workers hang under sustained traffic, sample_tokens RPC timeout, EngineDeadError",
      "bodyExcerpt": "### 🐛 Describe the bug\n\nUnder V1 engine + MTP speculative decoding + TP=8 + GLM-5.1-FP8 (`GlmMoeDsaForCausalLM`), workers hang under sustained production traffic. The scheduler is unable to advance — `step_counter=0` in the dump, requests stuck in flight — and after 30s the `sample_tokens` RPC times out, killing EngineCore. The container's outer process survives so vLLM internally restarts the engine (~12-17 min downtime), but this is a recurring failure pattern.\n\nThis is the same bug `jsboige` diagnosed for `new-TonyWang` in [#35104](https://github.com/vllm-project/vllm/issues/35104#issuecomment-4308377748) — a different bug than what PR #40303 fixes (no `SystemError`, no `memory_fence` on the stack). new-TonyWang's report is buried in #35104 comments and has no standalone issue, so filing here.\n\n### Stack trace\n\n```\nFile \".../vllm/v1/engine/core.py\", line 1205, in _process_engine_step\n    outputs, model_executed = self.step_fn()\nFile \".../vllm/v1/engine/core.py\", line 523, in step_with_batch_queue\n    model_output = future.result()\n...\nFile \".../vllm/v1/executor/multiproc_executor.py\", line 388, in get_response\n    raise TimeoutError(f\"RPC call to {method} timed out.\") from e\nTimeoutError: RPC call to sample_tokens timed out.\n```\n\nThe underlying cause:\n\n```\nFile \".../vllm/distributed/device_communicators/shm_broadcast.py\", line 756, in dequeue\n    with self.acquire_read(timeout, indefinite) as buf:\nFile \".../shm_broadcast.py\", line 675, in acquire_read\n    self._spin_condition.wait(timeout_ms=read_timeout.timeout_ms())\nFile \".../shm_broadcast.py\", line 632, in timeout_ms\n    raise TimeoutError\n```\n\nWorkers are stuck upstream of the message queue — no output coming back. Scheduler dump confirms it:\n\n```\nSchedulerStats(num_running_reqs=2, num_waiting_reqs=0, step_counter=0,\n  kv_cache_usage=0.405, prefix_cache_stats=PrefixCacheStats(reset=False, requests=0, ...),\n  spec_decoding_stats=None, ...)\n```\n\n### Reproduction (deterministic given enough wall clock)\n\n| Setup",
      "labels": [],
      "filterDisposition": "eligible",
      "relevanceScore": 7,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-issue-41094",
      "repository": "vllm-project/vllm",
      "number": 41094,
      "type": "issue",
      "category": "BUG",
      "categoryValid": true,
      "ruleCategory": "BUG",
      "classificationConsistent": true,
      "title": "[Bug]: [GLM-5.1] [MTP] DeepGEMM context_lens.is_contiguous assertion in paged MQA metadata",
      "bodyExcerpt": "### Describe the bug\n\n`zai-org/GLM-5.1-FP8` crashes under vLLM V1 + MTP speculative decoding with a DeepGEMM paged MQA metadata assertion:\n\n```text\nRuntimeError: Assertion error (/workspace/.deps/deepgemm-src/csrc/apis/attention.hpp:201): context_lens.is_contiguous()\n```\n\nThis looks very similar to #40987, but on GLM-5.1-FP8 instead of DeepSeek-V4. It may also be related to #40926, since this is the same GLM-5.1 / DSA / MoE / MLA / MTP serving class.\n\nIn our deployment, short requests usually work, but a long-context chat request caused the engine to die. The pod restarted cleanly after the fatal EngineCore error. This does not look like OOM: Kubernetes reported the previous container state as `Completed` with exit code `0`, and the first root-cause error was the DeepGEMM assertion above.\n\n### Environment\n\n* vLLM image: `vllm/vllm-openai@sha256:46da022ce07aae43e4ffae844aeab467a223437e071abadf566555699fbf16c3` (v0.20.0 image digest)\n* Model: `zai-org/GLM-5.1-FP8`\n* Served model name: `zai-org/glm-5.1`\n* Hardware: 8x NVIDIA B200\n* Runtime: Kubernetes/k3s pod, TP=8\n* KV cache dtype: `fp8`\n* Prefix caching: enabled\n* MTP: enabled, `num_speculative_tokens=3`\n\n### Serve command\n\n```bash\nvllm serve \\\n  --model zai-org/GLM-5.1-FP8 \\\n  --served-model-name zai-org/glm-5.1 \\\n  --trust-remote-code \\\n  --enable-prompt-tokens-details \\\n  --enable-auto-tool-choice \\\n  --tool-call-parser glm47 \\\n  --reasoning-parser glm45 \\\n  --speculative-config.method mtp \\\n  --speculative-config.num_speculative_tokens 3 \\\n  --max-model-len 202752 \\\n  --gpu-memory-utilization 0.88 \\\n  --kv-cache-dtype fp8 \\\n  --enable-prefix-caching \\\n  --max-num-seqs 256 \\\n  --max-num-batched-tokens 8192 \\\n  --chat-template-content-format string \\\n  --tensor-parallel-size 8\n```\n\nThe container also runs this before `vllm serve`:\n\n```bash\nmkdir -p /tmp/flashinfer-cubin /tmp/flashinfer-cubin-shadow/flashinfer_cubin\nprintf '%b' '__version__ = \"0.6.8.post1\"\\nCUBIN_DIR = \"/tmp/flashinfer-cubin\"\\ndef get_cubin_dir():\\n",
      "labels": [
        "stale"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM",
        "GLM4"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-issue-41525",
      "repository": "vllm-project/vllm",
      "number": 41525,
      "type": "issue",
      "category": "BUG",
      "categoryValid": true,
      "ruleCategory": "BUG",
      "classificationConsistent": true,
      "title": "[Bug]: the A800 architecture cannot start the glm5.1 model.",
      "bodyExcerpt": "### Your current environment\n\nvLLM version: docker vllm/vllm-openai:v0.19.1 or vllm/vllm-openai:v0.20.0-cu130-ubuntu2404\nGPU: 8x NVIDIA A800-SXM4-80GB (sm80, Ampere)\nOS: Ubuntu 22.04, Linux 5.15.0\n\n### 🐛 Describe the bug\n\nI currently plan to deploy the glm5.1 model using 8 A800 servers, but I found the deployment failed. Could you please help me figure out how to solve this problem?\n\n> In addition, I can successfully build the minimax m2.7 and kimi k2.6 models. However, glm5.1 and deepseek v4 both failed.\n\nstart vllm container command.\n\neg.\n\n```bash\nHEAD_IP=100.64.10.10\ndocker rm -f vllm-glm5.1\n\ndocker run -it --gpus all \\\n  --net=host \\\n  --name vllm-glm5.1 \\\n  --privileged --ipc=host \\\n  -v /mnt/public-storage/model/ZhipuAI:/models \\\n  -v /dev/infiniband:/dev/infiniband \\\n  -e NCCL_DEBUG=INFO \\\n  -e NCCL_IB_HCA=mlx5_10,mlx5_11,mlx5_12,mlx5_13 \\\n  -e NCCL_SOCKET_IFNAME=bond0 \\\n  -e GLOO_SOCKET_IFNAME=bond0 \\\n  -e NCCL_IB_DISABLE=0 \\\n  -e NCCL_NET_GDR_LEVEL=5 \\\n  harbor-pro.xxx.net/library/vllm/vllm-openai:v0.19.1 \\\n  --served-model-name GLM-5.1 \\\n  --model /models/GLM-5.1 \\\n  --trust-remote-code \\\n  --chat-template-content-format=string \\\n  --tensor-parallel-size 8 \\\n  --pipeline-parallel-size 8 \\\n  --nnodes 8 \\\n  --node-rank 0 \\\n  --master-addr \"$HEAD_IP\" \\\n  --tool-call-parser glm47 \\\n  --enable-auto-tool-choice \\\n  --reasoning-parser glm45 \n```\n\nlog error \n\n```bash\n(Worker_PP0_TP0 pid=600) ERROR 05-02 13:02:22 [multiproc_executor.py:857] ValueError: No valid attention backend found for cuda with AttentionSelectorConfig(head_size=576, dtype=torch.bfloat16, kv_cache_dtype=auto, block_size=None, use_mla=True, has_sink=False, use_sparse=True, use_mm_prefix=False, use_per_head_quant_scales=False, attn_type=AttentionType.DECODER). \nReasons: {FLASH_ATTN_MLA: [sparse not supported, compute capability not supported, FlashAttention MLA not supported on this device], FLASHMLA: [sparse not supported, compute capability not supported, FlashMLA Sparse is only supported on Ho",
      "labels": [
        "bug",
        "stale"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM",
        "GLM4"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-issue-49349",
      "repository": "vllm-project/vllm",
      "number": 49349,
      "type": "issue",
      "category": "NEW_SUPPORT",
      "categoryValid": true,
      "ruleCategory": "NEW_SUPPORT",
      "classificationConsistent": true,
      "title": "Zero JIT compilation during runtime",
      "bodyExcerpt": "### 🚀 The feature, motivation and pitch\n\n## Summary\n\nThis issue tracks adoption and follow-up work for the shared JIT warmup infrastructure proposed in #47456.\n\n## Foundation (blockers)\n\n- [x] #47451\n- [x] #49315\n- [x] #50174\n\n## Model-level de-JITification\n\n### DeepSeek V4\n- [ ] #49627\n    - [x] #50175\n    - [ ] https://github.com/vllm-project/vllm/pull/53564\n    - [ ] https://github.com/vllm-project/vllm/pull/53565\n    - [ ] #50176\n    - [ ] https://github.com/vllm-project/vllm/pull/53566\n    - [ ] #50177\n    - [ ] https://github.com/vllm-project/vllm/pull/53567\n    - [ ] #50178\n    - [ ] Enable `--jit-monitor-mode error` on CI for DSv4: https://github.com/vllm-project/vllm/pull/50109\n\n### GLM-5.2/5.3\n- [ ] https://github.com/vllm-project/vllm/pull/55348\n    - [ ] Enable `--jit-monitor-mode error` on CI for GLM-5.2/5.3: https://github.com/vllm-project/vllm/pull/50109\n\n### Kimi-K3\n- [ ] https://github.com/vllm-project/vllm/pull/54784\n    - [ ] Enable `--jit-monitor-mode error` on CI for Kimi-K3: https://github.com/vllm-project/vllm/pull/50109\n\nComplete model de-JITification is achieved when the model can run seamlessly with `--jit-monitor-mode error,` introduced in [#46167](https://github.com/vllm-project/vllm/pull/46167). Enabling this mode in CI will detect any untracked kernels that still compile at runtime and therefore require additional warmup coverage.\n\n## Supporting work. Related backend/kernel coverage\n\n### CuTeDSL\n\n- [ ] #48156\n- [ ] https://github.com/vllm-project/vllm/pull/48619\n\n### TileLang\n\n- [ ] #47807\n\n### Triton\n\n- [ ] #48804\n- [ ] #48805\n- [ ] #48363\n- [ ] #47637\n- [ ] #45601\n\n## Other related PRs \n\n### Unified runtime monitoring\n- [x] #46167\n- [x] #46621\n- [x] #46718\n\n### Compilation artifact portability and reuse\n\n#### DeepGEMM\n\n- [ ] #48190\n- [ ] deepseek-ai/DeepGEMM#388\n- [ ] https://github.com/vllm-project/vllm/pull/49930\n\n## Related open issues\n- [ ] https://github.com/vllm-project/vllm/issues/50394\n- [ ] https://github.com/vllm-project/vl",
      "labels": [
        "feature request",
        "kimi"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-issue-54451",
      "repository": "vllm-project/vllm",
      "number": 54451,
      "type": "issue",
      "category": "BUG",
      "categoryValid": true,
      "ruleCategory": "BUG",
      "classificationConsistent": true,
      "title": "[ROCm] DFlash unusable for GLM-5.3-Flash: glm5next lacks SupportsEagle3, and ROCM_AITER_MLA_SPARSE has no non-causal path",
      "bodyExcerpt": "### DFlash on GLM-5.3-Flash / ROCm: the target model interface is missing, and the sparse-MLA backend cannot do non-causal\n\nTrying to run `incoai/GLM-5.3-Flash-DFlash2` as a drafter for `zai-org/GLM-5.3-Flash` on 8x MI350X (gfx950, ROCm 7.2.3, TP8), I hit two separate walls. The first has a small fix; the second looks structural.\n\n**1. `models/glm5next/` does not implement `SupportsEagle3`.**\n\n```\nRuntimeError: Model does not support EAGLE3 interface\n```\n\nDFlash needs the target model to expose auxiliary hidden states from the layers named in the drafter's `target_layer_ids`. `glm5next` implements neither `EagleModelMixin` nor `SupportsEagle3`. Wiring it the way `qwen3_moe.py` does works — with one GLM-specific caveat worth recording:\n\n```python\nclass Glm5NextModel(nn.Module, EagleModelMixin):\n    ...\n        # residual=None: on GLM-5.3 `residual` is the mHC bundle of several\n        # residual streams, not a single same-shaped residual, so the mixin's\n        # default `hidden_states + residual` does not apply.\n        aux_hidden_states = self._maybe_add_hidden_state(\n            [], self.start_layer, hidden_states, None\n        )\n        for aux_idx, layer in enumerate(self._active_layers, start=self.start_layer):\n            hidden_states, residual, post, comb = layer(...)\n            self._maybe_add_hidden_state(\n                aux_hidden_states, aux_idx + 1, hidden_states, None\n            )\n```\n\nPassing `residual` as the mixin expects fails with `The size of tensor a (8192) must match the size of tensor b (4)` because of the manifold-constrained hyper-connections. With `None` the mixin stores the hidden state directly, which is what the drafter consumes.\n\n**Verified harmless when unused**: with this in place and no drafter configured, throughput and acceptance are unchanged (2 773 tok/s, acceptance 3.06 at 96 concurrency, within the ±5 % run-to-run spread) — `_maybe_add_hidden_state` is a no-op while `aux_hidden_state_layers` is empty.\n\n**2. After that, the d",
      "labels": [
        "rocm",
        "kimi",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-issue-55327",
      "repository": "vllm-project/vllm",
      "number": 55327,
      "type": "issue",
      "category": "BUG",
      "categoryValid": true,
      "ruleCategory": "BUG",
      "classificationConsistent": true,
      "title": "[ROCm] gfx950 top-k policy is gated on topK==1024, leaving index_topk=2048 models (GLM-5.x) on the generic 10-way split",
      "bodyExcerpt": "## Summary\n\nThe measured gfx950 policy for the sparse-attention indexer's top-k, added in #52882, is gated on `topK == 1024`. Models with `index_topk: 2048` — GLM-5.2, GLM-5.3 — never reach it and fall back to the generic `multipleBlocksPerRowConfig = 10` with 512 threads per split block.\n\nOn MI355X that is the wrong choice for **every shape we measured**. A policy calibrated for `topK == 2048` gives a median 25 % and up to 64 % reduction in kernel time, with no shape getting slower and identical results.\n\n## Where\n\n`csrc/libtorch_stable/sampler.cu`, `top_k_per_row_decode`:\n\n```cpp\nconst bool useGfx950DeviceLengthAwareTopK1024 =\n    topK == 1024 && numRows > 64 && numRows <= 256 && gfx950;   // ~line 754\nconst bool useGfx950TopK1024 =\n    topK == 1024 && numRows <= 64 && gfx950;                    // ~line 793\nint multipleBlocksPerRowConfig = 10;                            // everything else\n```\n\n## Why the gap is larger, not smaller, at topK=2048\n\nThe merge kernel walks `B * topK` candidates in a **single** block per row:\n\n```cpp\nvllm::topKPerRowDecode<1024, true, false, true>\n    <<<numRows, 1024, ...>>>( ..., multipleBlocksPerRowConfig * topK, ... );\n```\n\nAt topK=1024 with B=10 that is 10 240 candidates; at topK=2048 it is **20 480**. The cost #52882 set out to remove is twice as large for the models that do not get the fix.\n\nrocprofv3, 4 rows / 2 000 active columns, MI355X, GLM-5.3 (`index_topk: 2048`):\n\n| kernel | grid | median |\n|---|---|---|\n| `topKPerRowDecode<1024,true,false,true>` (merge) | 4 × **1** | **95.4 µs** |\n| `topKPerRowDecode<512,true,true,false>` (split) | 4 × 10 | 5.0 µs |\n\nThe split is irrelevant; the merge is the entire cost. In a full decode profile this kernel held 12 % of GPU time.\n\n## Short contexts are the worst case, not long ones\n\n`numColumns` is `logits.size(1)`, and the indexer's logits are allocated at `max_model_len` (`fp8_fp4_paged_mqa_logits(..., max_model_len=max_model_len)`). With a 1M model length the two-stage path is therefo",
      "labels": [
        "rocm",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-issue-55344",
      "repository": "vllm-project/vllm",
      "number": 55344,
      "type": "issue",
      "category": "BUG",
      "categoryValid": true,
      "ruleCategory": "BUG",
      "classificationConsistent": true,
      "title": "[Bug][ROCm] Startup crash (bare AssertionError) when the AITER custom-AR cutoff is below the fused allreduce+RMSNorm compile range",
      "bodyExcerpt": "### Your current environment\n\nROCm 7.2.3, MI355X (gfx950), 8x TP4, vLLM main, AITER custom all-reduce enabled\n(`VLLM_ROCM_USE_AITER=1`, `disable_custom_all_reduce=False`), fused\nallreduce+RMSNorm pass on (`pass_config.fuse_allreduce_rms: True`).\nModel: GLM-5.3 (`hidden_size=6144`, bf16), `--max-num-batched-tokens 8192`.\n\n### 🐛 Describe the bug\n\nSetting `AITER_CUSTOM_AR_MAX_SIZE` to a value below the compile range of the\nfused allreduce+RMSNorm pass makes vLLM **crash during startup** with a bare\n`AssertionError` and no message, instead of falling back to RCCL.\n\nReproduce (TP>=2 on ROCm, AITER custom AR + `fuse_allreduce_rms` enabled):\n\n```\nAITER_CUSTOM_AR_MAX_SIZE=1048576 vllm serve <model> -tp 4 --max-num-batched-tokens 8192\n```\n\n```\nERROR [multiproc_executor.py:1055]   File \"/build/vllm/_aiter_ops.py\", line 956,\n                                     in _rocm_aiter_fused_allreduce_rmsnorm_impl\nERROR [multiproc_executor.py:1055]     assert result is not None\nERROR [multiproc_executor.py:1055] AssertionError\nRuntimeError: Engine core initialization failed. See root cause above.\n```\n\nStartup log of the same run: `'compile_ranges_endpoints': [5461, 8192]`.\n\n### Root cause\n\nThree sizes have to agree, and nothing keeps them consistent:\n\n| quantity | source | value in the repro |\n|---|---|---|\n| compile range of the fusion | `max_size // (hidden_size * itemsize)`, `vllm/config/vllm.py:2164` | 5461 tokens (= 64 MiB) |\n| registered IPC pool | `AiterCustomAllreduce.MAX_SIZE` | 128 MiB |\n| **AITER runtime cutoff** | `min(self._car_max_size, self.max_size / 2)`, `aiter/dist/device_communicators/custom_all_reduce.py:1167` | **1 MiB (85 tokens)** |\n\n`vllm/config/vllm.py` derives the compile range from\n`AiterCustomAllreduce.effective_max_size()`, which only looks at `MAX_SIZE` and\nis unaware of `AITER_CUSTOM_AR_MAX_SIZE`. So the pass is compiled for message\nsizes that `should_custom_ar()` rejects at runtime. `custom_fused_ar_rms()` then\nreturns `None`, and `_rocm_aiter_fused_allr",
      "labels": [
        "rocm"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-issue-55351",
      "repository": "vllm-project/vllm",
      "number": 55351,
      "type": "issue",
      "category": "BUG",
      "categoryValid": true,
      "ruleCategory": "BUG",
      "classificationConsistent": true,
      "title": "[Bug][ROCm] gfx950 FP32 router GEMM is unreachable for DeepSeek-family models: shape (6144, 256) is listed but the weight is bf16",
      "bodyExcerpt": "### Your current environment\n\nROCm 7.2.3, MI355X (gfx950), TP4, vLLM main + #54845.\nModel: GLM-5.3 (`glm_moe_dsa`, `hidden_size=6144`, `n_routed_experts=256`, bf16).\n\n### 🐛 Describe the bug\n\nThe gfx950 low-M FP32 router GEMM added in #54845 never runs for DeepSeek-family\nmodels, including the `(6144, 256)` shape it explicitly supports.\n\n`GateLinear` gates the path on `self.weight.dtype == torch.float32`, but\n`deepseek_v2.py` constructs it without `params_dtype`, so the weight stays in the\nmodel dtype (bf16):\n\n```python\nself.router_dtype = _get_moe_router_dtype(config)\nself.gate = GateLinear(\n    config.hidden_size,\n    config.n_routed_experts,\n    out_dtype=self.router_dtype,     # fp32 output ...\n    prefix=f\"{prefix}.gate\",         # ... but the weight stays bf16\n)\n```\n\nThis is inconsistent with what the same function asks for:\n`_get_moe_router_dtype` returns `torch.float32` unconditionally for\n`model_type == \"glm_moe_dsa\"`, and GLM-5.3's own config carries\n`\"moe_router_dtype\": \"float32\"`. The routing is declared fp32; the weight is not.\n\nA log line at construction time makes it visible:\n\n```\nNACHWEIS Router-GEMM: gfx950=True, shape (6144, 256) supported=True,\n                      weight=torch.bfloat16, bias=False -> fp32 path OFF\n```\n\nSo `(6144, 256)` is in `ROCM_FP32_ROUTER_GEMM_SUPPORTED_SHAPES` and the hardware\nmatches, but the path is never taken. The same applies to every DeepSeek-family\nmodel going through `deepseek_v2.py`.\n\n### Enabling it\n\nvLLM already has the mechanism: `GateLinear(force_fp32_compute=True)` stores the\nweight in fp32 when the platform has no specialized bf16 kernel. `nemotron_h.py`\nuses it; the DeepSeek/GLM path does not. Setting it where fp32 routing is already\nrequested flips the path on:\n\n```python\nforce_fp32_compute=self.router_dtype == torch.float32,\n```\n\n```\nweight=torch.float32, bias=False -> fp32 path ON\n```\n\n### Measurement\n\nI want to be straight about the payoff: **on my setup, turning it on did not\nhelp.** Same build, same fl",
      "labels": [
        "rocm",
        "deepseek"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-issue-55373",
      "repository": "vllm-project/vllm",
      "number": 55373,
      "type": "issue",
      "category": "BUG",
      "categoryValid": true,
      "ruleCategory": "BUG",
      "classificationConsistent": true,
      "title": "[Bug][ROCm] Sparse indexer clears the full max_model_len logits buffer every decode step: 17% throughput on a 1M context",
      "bodyExcerpt": "### Your current environment\n\nROCm 7.2.3, MI355X (gfx950), TP4, vLLM main.\nGLM-5.3 (`glm_moe_dsa`, hidden 6144, 256 experts), `--max-model-len 1048576`,\nMTP k=3, 64 concurrent requests.\n\n### 🐛 Describe the bug\n\nOn ROCm, the DeepSeek-V3.2 style sparse indexer clears its entire logits buffer\non every decode step, even though the consumer only ever reads up to the real\nsequence lengths. The buffer is sized `(B * next_n, max_model_len)`, so with a\n1M context window this is a large amount of pure overhead.\n\n`vllm/v1/attention/ops/rocm_aiter_mla_sparse.py`, both ROCm paths:\n\n```python\n# HIP indexer kernel path\nout_logits.fill_(float(\"-inf\"))\n...\n# deepgemm path\nout_logits.nan_to_num_(float(\"-inf\"))\n```\n\nProfiled with the torch profiler, 20 decode iterations:\n\n| load | kernel | share of GPU time |\n|---|---|---|\n| 1 request | `__amd_rocclr_fillBufferAligned` | **4.5 %** |\n| 64 concurrent | `nan_to_num` (vectorized_elementwise) | **6.4 %** |\n\nSizing: at 64 requests with MTP k=3 the buffer is `256 x 1048576 x 4 B = 1.07 GB`,\nwhile an 8000-token context uses 8.2 MB of it — **0.76 %**. The arithmetic matches\nthe measurement: 1.07 GB written at ~4 TB/s is 268 µs, and the profiler shows\n239.6 µs per call.\n\n### Why it looks unnecessary\n\nThe same caller (`sparse_attn_indexer_kpool.py`) passes **`clean_logits=False`**\non the CUDA path and does not clear at all. That works because the consumer,\n`torch.ops._C.top_k_per_row_decode(logits, next_n, seq_lens, ...)`, is given the\nreal per-request sequence lengths and never reads past them. The region beyond\n`seq_lens` is dead on every platform.\n\nROCm has no equivalent to `clean_logits` and clears unconditionally.\n\n### Measurement with the clearing removed\n\nSame build, same flags, one env switch. Correctness first — a faster run with\nworse answers would not be a win:\n\nTwo full runs, with the order of the arms reversed in the second so an ordering\nartifact would show up:\n\n| | clearing on | clearing off |\n|---|---|---|\n| GSM8K (300 questions",
      "labels": [
        "rocm"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-issue-55416",
      "repository": "vllm-project/vllm",
      "number": 55416,
      "type": "issue",
      "category": "BUG",
      "categoryValid": true,
      "ruleCategory": "BUG",
      "classificationConsistent": true,
      "title": "[Bug]: --numa-bind is a silent no-op on ROCm images (numactl not installed)",
      "bodyExcerpt": "### Your current environment\n\n<details>\n<summary>Environment</summary>\n\n```\nImage: vllm/vllm-openai-rocm:nightly (also reproduced on a ROCm 7.2.3 derivative)\nGPU: 8x MI355X (gfx950), TP4\nModel: GLM-5.3 (GlmMoeDsaForCausalLM), MXFP4 experts + FP8 attention\n```\n\n</details>\n\n### 🐛 Describe the bug\n\n`--numa-bind` (added in #43270) is silently a no-op on the ROCm images, because\n**`numactl` is not installed in them**, while `vllm/utils/numa_utils.py` shells out\nto it.\n\n`_probe_numactl_args()` catches `OSError` and returns `False`, so\n`_resolve_numactl_args()` walks all three candidates (full args, CPU-only, `\"\"`)\nand returns `\"\"`. The `logger.warning` in that function only fires when a\n*different* candidate succeeds — when **every** candidate fails there is no\nwarning at all, not even at DEBUG level. The user passes `--numa-bind`, sees\nnothing in the log, and gets no binding.\n\n#### Minimal reproduction\n\n```console\n$ docker run --rm --entrypoint bash vllm/vllm-openai-rocm:nightly -c '\npython3 - <<PY\nimport logging, shutil\nlogging.basicConfig(level=logging.DEBUG)\nfrom vllm.utils.numa_utils import _resolve_numactl_args, _probe_numactl_args\nprint(\"numactl on PATH:\", shutil.which(\"numactl\"))\nprint(\"_probe_numactl_args(...):\", _probe_numactl_args(\"--cpunodebind=0 --membind=0\"))\nprint(\"_resolve_numactl_args(...) ->\", repr(_resolve_numactl_args(\"--cpunodebind=0 --membind=0\")))\nPY'\nnumactl on PATH: None\n_probe_numactl_args(...): False\n_resolve_numactl_args(...) -> ''\n```\n\nNo log output between those lines.\n\n#### Why this matters\n\nPer-worker CPU binding is not cosmetic on multi-socket hosts. Docker's\n`--cpuset-cpus` binds the *container*; all five processes (EngineCore + 4 TP\nworkers) then share the same core set and the scheduler moves them freely — we\ncaught one worker running on an SMT sibling.\n\nMeasured on our setup (TP4, 64 concurrent requests, real-text dataset,\n`torch.profiler` with rocprofiler-sdk 1.3.2 so the traces are trustworthy):\n\nEvery *compute* kernel is identical ",
      "labels": [
        "rocm"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-39168",
      "repository": "vllm-project/vllm",
      "number": 39168,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "DOCS",
      "classificationConsistent": true,
      "title": "[ROCm] Fixing GLM-5.1, DeepSeek-V3.2, DeepSeek-V4 on gfx942",
      "bodyExcerpt": "This PR:\r\n* Fixes a bug in cache_kernels.cu that resulted in incorrect results for block_stride not multiple of 16\r\n* Adds block size 16 to supported sparse MLA block sizes on ROCm\r\n* Fixes a few bugs in the Python code\r\n* Adds optimized Triton kernels with support of arbitrary block size for two of the steps\r\n* In case of rocm_fp8_paged_mqa_logits, routes to the Triton kernel by default, since AITER  is slower for block size 1 and it does not handle block size 16 correctly\r\n\r\nI've verified that, after this PR, both GLM-5-FP8 and DeepSeek-V3.2 work correctly on ROCm (MI325  / gfx942) with both --block-size 1 and --block-size 16.",
      "labels": [
        "documentation",
        "rocm",
        "ready",
        "v1",
        "deepseek",
        "DSv4",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-51238",
      "repository": "vllm-project/vllm",
      "number": 51238,
      "type": "pr",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[Perf][Reasoning] O(delta) reasoning-end check for engine parsers",
      "bodyExcerpt": "## Problem\n\n`ParserEngineReasoningAdapter` forwards `is_reasoning_end` to its parser engine but not `is_reasoning_end_streaming`, so every parser-engine-backed reasoning parser falls through to `ReasoningParser`'s default:\n\n```python\ndef is_reasoning_end_streaming(self, input_ids, delta_ids) -> bool:\n    return self.is_reasoning_end(input_ids)\n```\n\n`should_advance` (`vllm/v1/structured_output/__init__.py:430`) calls this **once per structured-output request per decode step** while the request is still inside its reasoning block, so the scheduler re-scans the accumulated sequence every step. The adapter also did `list(input_ids)` on each call, materializing the whole prompt+output token sequence.\n\nResolving the MRO for the registered reasoning parsers, **17 of 30 inherit that default; 13 of them via this one adapter** — `qwen3`, `mistral`, `kimi_k2`, `minimax_m2`, `glm45`, `glm47`, `gemma4`, `deepseek_v4`, `inkling`, `ling3`, `mimo`, `nemotron_v3`, `seed_oss`.\n\n## Measurement\n\nSimulating exactly what `should_advance` does — `Qwen3ParserReasoningAdapter`, one call per decode step over a growing reasoning block, `all_token_ids` as a real `ConstantList`:\n\n| reasoning tokens | before, µs/step | after, µs/step |\n|---|---|---|\n| 500 | 21.7 | 0.58 |\n| 1000 | 46.7 | 0.56 |\n| 2000 | 98.2 | 0.56 |\n| 4000 | 202.2 | 0.56 |\n\nEmpirical exponent `log2(t(2n)/t(n))` goes from **2.04–2.10 to 1.00** — the per-step cost was growing linearly with the reasoning block and is now flat. With a 20k-token prompt in front (agentic multi-turn), the before figure starts at 118 µs/step and reaches 300 µs/step at 4k reasoning tokens, versus a flat 0.55 µs after; that floor was the `list(input_ids)` copy.\n\nAt 4k reasoning tokens that is ~200 µs of scheduler time per request per step, and the scheduler is on the critical path.\n\n## Change\n\n- `ParserEngine.is_reasoning_end_streaming` — same backward marker scan as `is_reasoning_end`, restricted to the tokens produced this step. Empty delta falls back t",
      "labels": [
        "tool-calling",
        "kimi"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 5,
      "matchedTerms": [
        "GLM",
        "GLM4"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-51314",
      "repository": "vllm-project/vllm",
      "number": 51314,
      "type": "pr",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[ROCm][Perf] Skip cleaning sparse prefill MQA logits",
      "bodyExcerpt": "## Summary\n\nThe sparse prefill path creates the MQA-logits buffer with `torch.full(..., -inf)`.\nThis launches an FP32 fill before every logits kernel.\n\nThis PR passes `clean_logits=False` to AITER, so the buffer uses `torch.empty`\nand the fill is removed.\n\n## Safety\n\nThe only consumer is `top_k_per_row_prefill`. It reads each row only inside\n`[cu_seqlen_ks, cu_seqlen_ke)`. Its vectorized path also guards the final partial\nload, so padding values are never read. The top-k result is unchanged.\n\n## Results\n\nMI355X, TP=4, rank 0, comparable before/after captures:\n\n| metric | before | after |\n|---|---:|---:|\n| FP32 fill launches | 18,162 | **0** |\n| FP32 fill GPU time | 1,250.2 ms | **0 ms** |\n| MQA-logits launches | 18,108 | 17,930 |\n\nThe removed fill was **0.89% of total GPU kernel time**. In the sample below it\ntakes 74.6 µs and is fully serialized before the logits kernel.\n\n![Before: FP32 fill before MQA logits](https://raw.githubusercontent.com/amd-sriram/vllm/ce9490285bbd8099e2d37c5aec8b3d24b7a20785/figures/fig_pr4_before.png)\n\n![After: fill removed](https://raw.githubusercontent.com/amd-sriram/vllm/ce9490285bbd8099e2d37c5aec8b3d24b7a20785/figures/fig_pr4_after.png)\n\n## Model-level e2e results\n\n### Test conditions\n\n| | |\n|---|---|\n| Hardware | MI355X (gfx950), TP=4 |\n| Model | `amd/GLM-5.2-MXFP4`, fp8 KV cache |\n| Stack | vLLM `69715823`, AITER `4a1cc77`, flydsl `0.3.0` |\n| Server | `--async-scheduling --max-num-seqs 256 --max-num-batched-tokens 16384 --block-size 64 --no-enable-prefix-caching`, `index_topk_freq=4` |\n| Env | `VLLM_ROCM_USE_AITER=1`, `VLLM_ROCM_USE_AITER_SPARSE_INDEXER=1`, `AITER_USE_FLYDSL_MOE_SORTING=1`, `VLLM_ROCM_QUICK_REDUCE_QUANTIZATION=INT4` |\n| Perf | `vllm bench serve`, random dataset, `--ignore-eos --request-rate inf`, ISL 1024 and 8192, OSL 1024, concurrency 1-256 (16 points) |\n| Accuracy | `lm_eval` gsm8k 5-shot chat template with thinking enabled, `max_gen_toks=8192`; RULER `niah_single_2` at 16k |\n| Sampling | checkpoint default (tempe",
      "labels": [
        "rocm"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-51315",
      "repository": "vllm-project/vllm",
      "number": 51315,
      "type": "pr",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[ROCm][Perf] Fuse the DSA indexer prologue with AITER",
      "bodyExcerpt": "## Summary\n\nThe DSA indexer prologue uses four kernels for K normalization, Q/K RoPE,\nFP8 quantization, and the K-cache write.\n\nThis PR uses AITER's fused `indexer_qk_rope_quant_and_cache` kernel instead.\nThe fused path is enabled only for its supported BF16/FP8 shape; other\nconfigurations keep the existing path.\n\n## Results\n\nMI355X, TP=4, rank 0:\n\n| indexer stage | before | after | change |\n|---|---:|---:|---:|\n| launches | 4 | 1 | **-75%** |\n| decode time | 17.4 µs | 5.9 µs | **-66%** |\n| prefill time | 163.9 µs | 270.6 µs | **+65%** |\n\nThe fused kernel is faster for decode but slower for prefill. Across the full\ntrace, prologue GPU time changes from 1,093.6 ms to 1,041.4 ms (**-4.8%**).\n\nOne decode indexer stage at the same zoom:\n\n![Before: four prologue kernels](https://raw.githubusercontent.com/amd-sriram/vllm/ce9490285bbd8099e2d37c5aec8b3d24b7a20785/figures/fig_pr3_before.png)\n\n![After: one fused prologue kernel](https://raw.githubusercontent.com/amd-sriram/vllm/ce9490285bbd8099e2d37c5aec8b3d24b7a20785/figures/fig_pr3_after.png)\n\nEnd-to-end geomean:\n\n| metric | before | after | change |\n|---|---:|---:|---:|\n| throughput | 811.75 | 817.58 | **+0.72%** |\n| TPOT | 24.12 ms | 23.96 ms | **-0.66%** |\n| ITL | 23.85 ms | 23.69 ms | **-0.67%** |\n\n## Model-level e2e results\n\n### Test conditions\n\n| | |\n|---|---|\n| Hardware | MI355X (gfx950), TP=4 |\n| Model | `amd/GLM-5.2-MXFP4`, fp8 KV cache |\n| Stack | vLLM `69715823`, AITER `4a1cc77`, flydsl `0.3.0` |\n| Server | `--async-scheduling --max-num-seqs 256 --max-num-batched-tokens 16384 --block-size 64 --no-enable-prefix-caching`, `index_topk_freq=4` |\n| Env | `VLLM_ROCM_USE_AITER=1`, `VLLM_ROCM_USE_AITER_SPARSE_INDEXER=1`, `AITER_USE_FLYDSL_MOE_SORTING=1`, `VLLM_ROCM_QUICK_REDUCE_QUANTIZATION=INT4` |\n| Perf | `vllm bench serve`, random dataset, `--ignore-eos --request-rate inf`, ISL 1024 and 8192, OSL 1024, concurrency 1-256 (16 points) |\n| Accuracy | `lm_eval` gsm8k 5-shot chat template with thinking enabled, `max_gen_tok",
      "labels": [
        "rocm",
        "deepseek"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-51392",
      "repository": "vllm-project/vllm",
      "number": 51392,
      "type": "pr",
      "category": "DOCS",
      "categoryValid": true,
      "ruleCategory": "DOCS",
      "classificationConsistent": true,
      "title": "[Quantization] Support online quantization with partially pre-quantized checkpoints",
      "bodyExcerpt": "## Disclosure\r\n\r\nAI assistance was used. The changes were reviewed and tested manually.\r\n\r\n## Purpose\r\n\r\nAllow online quantization on the unquantized `linear` or `moe` (or fine-grained `targets`) portions of a partially pre-quantized checkpoint **from ANY `quant_method` (Quark, ModelOpt, compressed-tensors, etc.)**. \r\n\r\nThe original `quant_method` remains responsible for its pre-quantized layers, the composed config applies the requested online method only when the checkpoint returns an unquantized method.\r\n\r\nExample use cases:\r\n- Supersedes https://github.com/vllm-project/vllm/pull/49313 that attempts to do the same for `amd/GLM-5.2-MXFP4` that quantizes only the MOE layers.\r\n- Supersedes https://github.com/vllm-project/vllm/pull/48983 that attempts to do the same with modelopt checkpoint\r\n- Take e.g. [`nvidia/Qwen3.5-122B-A10B-NVFP4`](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4) (modelopt checkpoint) or [`amd/Qwen3.5-397B-A17B-MXFP4`](https://huggingface.co/amd/Qwen3.5-397B-A17B-MXFP4) (quark checkpoint) that apply non-shared MOE expert only quantization, and quantize additional layers as needed (for evaluation or optimization purposes\r\n- Relax the need to release several models with different preset pre-quantized layers / different ignore lists).\r\n- Eventually, support `shared_experts` online quantization +fusion (not in this PR, see draft https://github.com/fxmarty-amd/vllm/pull/6)\r\n\r\nThis PR will work nicely with https://github.com/vllm-project/vllm/pull/51285 once it is merged.\r\n\r\nImplementation details:\r\n\r\n- Online quantization is stored as `QuantizationConfig.online_quant_config`. `QuantizationConfig.get_effective_quant_method` **resolves the global quant method, including both the base checkpoint quant method and the online quant method**. The original `QuantizationConfig` subclass instantiation is preserved (e.g. `Fp8Config`), so **model developers do not need to be aware of this online quantization composition** (see examples below).\r\n- Selected",
      "labels": [
        "documentation",
        "speculative-decoding",
        "ready",
        "quantization",
        "kimi",
        "k3"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-52863",
      "repository": "vllm-project/vllm",
      "number": 52863,
      "type": "pr",
      "category": "OTHER",
      "categoryValid": true,
      "ruleCategory": "OTHER",
      "classificationConsistent": true,
      "title": "[Kernel][PCP] Publish replicated KV updates through PyTorch symmetric memory",
      "bodyExcerpt": "## Summary\r\n\r\nReplace the PCP KV-update AllGather with direct publication of final quantized Main-KV and Indexer-K rows into each replicated cache through PyTorch SymmetricMemory. The symmetric-memory domain is owned by the KV-cache lifecycle, scoped to the runtime PCP group, and uses one capture-safe release/acquire barrier after publication.\r\n\r\nThe gather path remains the fallback. Direct KV is selected automatically when the CUDA PCP topology and attention layers support it. Set `VLLM_USE_PCP_DIRECT_KV=0` to disable it; `1` requests it and warns if the configuration is unsupported.\r\n\r\n```text\r\nAS-IS: gather then insert\r\n\r\n  local final K/V rows\r\n          |\r\n          v\r\n  PCP AllGather of update tensors\r\n          |\r\n          v\r\n  each rank inserts gathered rows\r\n          |\r\n          v\r\n  replicated local KV cache\r\n```\r\n\r\n```text\r\nTHIS PR: publish final rows directly\r\n\r\n  local final K/V rows\r\n          |\r\n          +---- store to PCP peer cache 0\r\n          +---- store to PCP peer cache 1\r\n          +---- store to PCP peer cache N\r\n          |\r\n          v\r\n  system-scope release/acquire barrier\r\n          |\r\n          v\r\n  replicated local KV cache\r\n\r\n  no KV-update AllGather\r\n  no receive-side cache insertion\r\n```\r\n\r\nThe cache layout and capacity are unchanged. Existing attention kernels still consume ordinary local cache tensors.\r\n\r\n## Design\r\n\r\n- `KVCache` owns the backing storage, peer views, rendezvous handles, and teardown.\r\n- The PCP domain is built from the runtime PCP device group; TP2/PCP2 therefore publishes independently within each same-TP PCP subgroup.\r\n- Fused DeepSeek/GLM cache-store kernels write the final FP8 Main-KV and Indexer-K bytes to every PCP peer pointer.\r\n- A device-epoch barrier provides system-scope publication and supports CUDA Graph replay and separate DBO lanes.\r\n- Sparse-MLA PIECEWISE CUDA Graphs are supported when direct KV is selected.\r\n- Full-graph modes remain gated. In particular, rank-divergent PCP prefill replay is no",
      "labels": [
        "needs-rebase",
        "deepseek",
        "nvidia",
        "mrv2"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-53094",
      "repository": "vllm-project/vllm",
      "number": 53094,
      "type": "pr",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[ROCm][Perf] Fuse DSA indexer QK preprocessing with AITER",
      "bodyExcerpt": "## Purpose\n\nEvery DeepSeek-Sparse-Attention layer (DeepSeek-V3.2, GLM-5.x, and their MTP drafts) runs a lightweight indexer whose pre-processing costs five kernel launches per layer per step:\n\n1. `LayerNorm` on `k`\n2. RoPE on the leading rope dims of `q` and `k`\n3. per-token-group FP8 quantization of `q`\n4. a pointwise fold of the `q` scale into the indexer `weights`\n5. FP8 quantization of `k` plus the paged indexer K-cache write\n\nAt decode sizes this is launch-bound: the cost barely moves with token count. AITER already ships a kernel that does all five in one launch (`indexer_qk_rope_quant_and_cache`). This PR wires it in behind a new, default-off env flag `VLLM_ROCM_USE_AITER_INDEXER_QK_FUSION`.\n\nThe kernel writes the indexer K cache itself, so the sparse-attention indexer op skips its own insert (`skip_k_cache_insert`). Two data-movement optimizations ride along: the rope `cos_sin_cache` halves are registered once as strided views on the indexer's rotary embedding instead of being re-split every layer every step, and one zero-initialized `q_fp8`/`weights` output pair is shared by the indexer layers of a model (allocated next to `topk_indices_buffer` and sized the same way), with only the first indexer of a forward pass zero-filling it. The change is Python-only: the kernel is already in the AITER version the tree pins.\n\nThe fused path engages only when all of the following hold, and otherwise falls back silently to today's code:\n\n- `VLLM_ROCM_USE_AITER=1` and `VLLM_ROCM_USE_AITER_INDEXER_QK_FUSION=1`\n- gfx942 / gfx950 (`on_mi3xx()`): AITER ships this kernel in its CK build, and the RDNA paths that `SparseAttnIndexer.forward_hip` now also serves have Triton-only AITER\n- the in-place-RoPE indexer path, i.e. the `rotary_embedding` custom op is enabled\n- DSA indexer shapes `index_head_dim == 128` and `qk_rope_head_dim == 64`\n- no context parallelism (`decode_context_parallel_size == 1` and `prefill_context_parallel_size == 1`). The fused kernel is driven by `slot_ma",
      "labels": [
        "performance",
        "rocm",
        "needs-rebase",
        "deepseek"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-53263",
      "repository": "vllm-project/vllm",
      "number": 53263,
      "type": "pr",
      "category": "DOCS",
      "categoryValid": true,
      "ruleCategory": "DOCS",
      "classificationConsistent": true,
      "title": "[6/N][KV Connector][NIXL] Stage host KV reads through device memory",
      "bodyExcerpt": "## Summary\n\n- Stage same-node NIXL reads into registered device buffers when every destination region is host memory.\n- Pipeline the completed device-to-host copies without reporting a request complete until both transfer legs finish.\n- Bound shutdown and retain staging slots through asynchronous copy completion, including failure and abort paths.\n\nThis avoids UCX's slow TCP-loopback path for direct device-to-host NIXL reads while leaving device-resident and mixed layouts on their existing path.\n\n## Duplicate check\n\nOpen-PR searches found no other PR implementing same-node NIXL host-read staging. This is a generic NIXL prerequisite used by the HiSparse stack, rather than a HiSparse-specific path.\n\n## Tests\n\n```bash\nsource .venv/bin/activate\npython -m pytest tests/v1/kv_connector/unit/test_nixl_connector.py -k host_stager -q\n```\n\nResult: 4 passed. Changed-file pre-commit hooks also passed.\n\n## Evaluation\n\nOn 8xB300 with GLM-5.2-FP8 in a 4P+4D NIXL setup, the isolated 32K-token import improved from about 17.7 s to 3.6 s. A GSM8K run remained stable (0.807 to 0.817) with no staging errors.\n\n## AI assistance\n\nAI assistance was used for implementation, tests, review, and documentation. The human submitter is responsible for reviewing every changed line and defending the change end-to-end.\n",
      "labels": [
        "documentation",
        "speculative-decoding",
        "ready",
        "torch.compile",
        "ci/build",
        "deepseek",
        "cpu",
        "kv-connector",
        "nvidia",
        "mrv2",
        "dflash",
        "scheduler",
        "kv-cache-manager",
        "glm",
        "minimax"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-53580",
      "repository": "vllm-project/vllm",
      "number": 53580,
      "type": "pr",
      "category": "OTHER",
      "categoryValid": true,
      "ruleCategory": "OTHER",
      "classificationConsistent": true,
      "title": "[XPU] Route grouped_topk to the fused _moe_C kernel on XPU",
      "bodyExcerpt": "\r\n## Purpose\r\n\r\nvllm-xpu-kernels ships a fused grouped_topk (noaux_tc) kernel registered into the same `_moe_C::grouped_topk` op that CUDA uses, but both gates guarding it test `is_cuda()`, so XPU always fell back to the eager decomposition.\r\n\r\nThis PR routes grouped_topk to the fused _moe_C kernel on XPU and registers a fake (meta) implementation for _moe_C::grouped_topk, which previously had only device kernels and no meta kernel, so torch.compile could not trace through it resulting in graph breaks (not XPU-specific — adding the meta implementation helps on CUDA too).\r\n\r\nAffects: every DeepSeek-V3-style (noaux_tc) MoE model — DeepSeek-V3/R1, Kimi-K2.6, Trinity-Nano, Ling/Bailing, GLM-4.5, Longcat-Flash\r\n\r\n## Test Result\r\n\r\n<html>\r\n<body>\r\n<!--StartFragment-->\r\n\r\nTested model: arcee-ai/Trinity-Nano-Base\r\nInputs: 4096-in / 1024-out, concurrency 48\r\nB70 XPU\r\n\r\n| arm | out tok/s | vs eager+native | vs default | latency (s) | routing kernel (ms) | routing launches | total launches | compiled regions |\r\n|:--|--:|--:|--:|--:|--:|--:|--:|--:|\r\n| eager + native | 1066.1 | — | -6.2% | 46.11 | 269.0 | 33,912 | 174,119 | 2 |\r\n| eager + fused | 1149.8 | +7.9% | +1.2% | 42.75 | 224.1 | 4,752 | 142,205 | 6 |\r\n| torch.compile + native *(current default)* | 1136.1 | +6.6% | — | 43.26 | 268.2 | 33,912 | 140,943 | 4 |\r\n| torch.compile + fused | 1203.6 | +12.9% | +5.9% | 40.84 | 225.3 | 4,752 | 109,029 | 8 |\r\n| **torch.compile + fused + meta impl** | **1213.0** | **+13.8%** | **+6.8%** | 40.52 | 225.1 | 4,752 | 109,029 | 4 |\r\n\r\n</body>\r\n</html>\r\n",
      "labels": [
        "intel-gpu",
        "verified"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM",
        "GLM-4",
        "GLM-4.5"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-53592",
      "repository": "vllm-project/vllm",
      "number": 53592,
      "type": "pr",
      "category": "DOCS",
      "categoryValid": true,
      "ruleCategory": "DOCS",
      "classificationConsistent": true,
      "title": "[7/N] HiSparse: add DeepSeek V4 support",
      "bodyExcerpt": "## Summary\n\n- Integrate DeepSeek V4 C4 sparse MLA with HiSparse resident and host-backed cache paths.\n- Add the compressed slot/block geometry needed by DeepSeek V4 while keeping non-C4 cache groups resident on GPU.\n- Preserve the split FP8 value/scale page layout during host backup and hot-buffer staging.\n- Resolve C4 FlashMLA decode selections through HiSparse and stage compressed prefill cache rows.\n\nThis support was extracted from #51323 so the validated GLM/DeepSeek-V3.2 HiSparse implementation can be reviewed and merged without DeepSeek V4's compressed cache geometry. It does not duplicate another open PR; searches for DeepSeek V4 HiSparse work found only the parent HiSparse PR from which this change was split.\n\n## Tests\n\n- `cmake --build --preset release --target _C_stable_libtorch` (passed)\n- `python -m pytest tests/v1/attention/test_sparse_mla_backends.py -q -k hisparse` (22 passed)\n- `python -m pytest tests/v1/core/test_kv_cache_utils.py tests/v1/worker/test_utils.py -q` (100 passed)\n- Pre-commit hooks passed for all files changed while splitting the generic and DeepSeek V4 paths.\n\n## Model evaluation\n\nThe latest DeepSeek V4 B300 throughput sweep exposed a substantial high-concurrency regression and an allocator failure under saturation. The sweep also used a metrics revision with invalid hot-path counter instrumentation, so those numbers are not suitable as final performance evidence. This PR remains draft until DeepSeek V4 performance and saturation robustness are resolved and re-evaluated.\n\nAI assistance was used for implementation, review, testing, and preparation of this PR.\n",
      "labels": [
        "documentation",
        "speculative-decoding",
        "torch.compile",
        "ci/build",
        "deepseek",
        "cpu",
        "kv-connector",
        "nvidia",
        "mrv2",
        "DSv4",
        "dflash",
        "scheduler",
        "kv-cache-manager",
        "glm",
        "minimax"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-53739",
      "repository": "vllm-project/vllm",
      "number": 53739,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][Parser] Terminate streamed tool arguments the flush cannot extend",
      "bodyExcerpt": "## Purpose\n\nA streaming tool call can end with `finish_reason` set while `arguments` is cut off mid-string. Any client that calls `json.loads` on the accumulated arguments fails on it. Reproduced live on Qwen3.8-27B in 7 of 25 requests sent straight at the worker, no proxy in the path.\n\n`_safe_arg_prefix` (`parser_engine.py:283`) deliberately streams unterminated string values for prefix-stable keys. Its own docstring carries the contract: \"String values for keys in `string_keys` are prefix-stable, so stream their unterminated content instead of buffering long arguments until the closing tag arrives.\" Something has to close them, and that something is the flush.\n\n`_flush_arg_converter` (`parser_engine.py:963`) re-derives the arguments with `partial=False`. Every in-tree `arg_converter` builds its result from completed parameters and folds the still-open one in under `if partial:`. So when generation ends while a parameter is open, the non-partial re-derivation **drops that parameter**, and the result is neither longer than nor a prefix of what was already streamed. Both guards fail, the function returns `None`, and nothing ever terminates the prefix the client is holding.\n\nThis is not conditional on the model misbehaving. Anything that ends generation with a parameter open reaches it: a `max_tokens` stop, a stop string, or a malformed continuation.\n\n### Blast radius, measured\n\nEach converter called directly with one complete parameter and one open parameter:\n\n| converter | configs | behavior at `partial=False` |\n|---|---|---|\n| `_qwen3_arg_converter` | qwen3 | drops the open parameter, 64 chars -> 21 |\n| `_minimax_m2_arg_converter` | minimax_m2 | drops the open parameter, 64 -> 21 |\n| `_glm47_arg_converter` | glm47_moe | drops the open parameter, 64 -> 21 |\n| `_dsml_arg_converter` | deepseek_v4, deepseek_v32 | drops the open parameter, 64 -> 21 |\n| `_inkling_arg_converter` | inkling | returns the same unterminated span, so the flush has nothing to append |\n| `_gemma",
      "labels": [
        "bug",
        "tool-calling",
        "qwen"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM",
        "GLM4"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-53789",
      "repository": "vllm-project/vllm",
      "number": 53789,
      "type": "pr",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[ROCm][Perf] Bound the decode paged-MQA-logits sanitize to the top-k read region",
      "bodyExcerpt": "## Summary\n\nAfter [vllm#49714](https://github.com/vllm-project/vllm/pull/49714), every decode step runs `nan_to_num_(-inf)` over the full\n`[rows, max_model_len]` logits workspace. On GLM-5.2 `max_model_len` is 258048,\nso this costs 43 us per call regardless of how short the context is.\n\nThe top-k only reads the front of each row. This PR sanitizes just that slice\nusing a small Triton kernel, and falls back to `nan_to_num_` below 10M elements\nwhere a kernel launch isn't worth it.\n\n## Safety\n\n- The sanitized region is a superset of what the top-k reads, so the result is\n  identical.\n- NaN/Inf handling matches `nan_to_num_(-inf)` exactly: NaN -> `-inf`,\n  `+inf` -> `FLT_MAX`, `-inf` -> `-FLT_MAX`.\n- The launch grid depends only on the row count (a shape), so CUDA graph capture\n  stays valid.\n\n## Results\n\nMI355X TP=4, ISL 1024 / OSL 256 / concurrency 256. Same image, same bench\narguments, with and without this commit.\n\nOne decode step, rank 0. Both panels show the same 130 us window:\n\n![decode-step timeline, before and after](https://raw.githubusercontent.com/amd-sriram/vllm/f87421d965c40b2d39e1cdbf1e4b5acac23348ff/figures/fig_pr7_timeline.png)\n\nThe sanitize sits between `paged_mqa_logits` and the top-k. Before: 44.4 us.\nAfter: 4.6 us.\n\nOver the whole run, rank 0:\n\n| | before | after |\n|---|---:|---:|\n| sanitize launches | 9,885 | 9,870 |\n| avg per call | 43.13 us | 4.59 us |\n| total GPU time | 426.3 ms | **51.9 ms** |\n| % of GPU kernel time | 1.82% | **0.21%** |\n\nSame launch count, each call does less work. Rank 1 matches (427 ms -> 50 ms).\n\n## Model-level e2e results\n\n### Test conditions\n\n| | |\n|---|---|\n| Hardware | MI355X (gfx950), TP=4 |\n| Model | `amd/GLM-5.2-MXFP4`, fp8 KV cache |\n| Stack | vLLM `cb810483`, AITER `c76aca0a` |\n| Server | `--async-scheduling --max-num-seqs 256 --max-num-batched-tokens 16384 --block-size 64 --no-enable-prefix-caching --max-model-len 258048`, `index_topk_freq=4` |\n| Env | `VLLM_ROCM_USE_AITER=1`, `VLLM_ROCM_USE_AITER_SPARSE_INDEXER=1",
      "labels": [
        "rocm"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-53844",
      "repository": "vllm-project/vllm",
      "number": 53844,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][Multimodal] Guard Qwen3-VL video sampling against zero source fps",
      "bodyExcerpt": "## Summary\n\n`Qwen3VLVideoBackend.compute_frames_index_to_sample` computes:\n\n```python\nnum_frames = int(total_frames_num / original_fps * fps)\n```\n\nwithout guarding `original_fps`. vLLM reports `original_fps == 0` for clips with an unknown/variable frame rate (VFR, malformed, streaming), so those inputs crash with an opaque `ZeroDivisionError` deep in the video loader.\n\nThe sibling `Qwen2VLVideoBackend` (same file) already handles this and raises a clear `ValueError` — its guard even documents that vLLM reports `original_fps == 0` for such clips. The Qwen3-VL backend was missing the same guard.\n\n## Reproduction (before fix)\n\nCalling both backends with a clip reporting `original_fps == 0`:\n\n```\nQwen2-VL backend (has guard):\n  -> ValueError: Qwen2-VL video sampling needs a known source fps, but the container reported 0 (variable or unknown frame rate).\nQwen3-VL backend (no guard):\n  -> ZeroDivisionError: float division by zero\n```\n\n## After fix\n\n```\nQwen2-VL backend (has guard):\n  -> ValueError: Qwen2-VL video sampling needs a known source fps, but the container reported 0 (variable or unknown frame rate).\nQwen3-VL backend (no guard):\n  -> ValueError: Qwen3-VL video sampling needs a known source fps, but the container reported 0 (variable or unknown frame rate).\n```\n\n## Fix\n\nAdd the same `original_fps <= 0` guard used by `Qwen2VLVideoBackend`, raising a descriptive `ValueError` instead of dividing by zero.\n\n## Tests\n\n```\n.venv/bin/python -m pytest tests/multimodal/test_video.py -k zero_original_fps -v\n# 2 passed  (Qwen2VLVideoBackend + Qwen3VLVideoBackend)\n```\n\nLinters: `ruff-check`, `ruff-format`, `mypy-3.12` all pass on the changed files.\n\n(Note: some `glm46v` cases in this test file fail locally only due to a missing optional `av`/PyAV dependency and HF network access in my sandbox — unrelated to this change, which only touches `Qwen3VLVideoBackend`.)\n\n## Not a duplicate\n\nNo open/closed PR adds an `original_fps`/zero-fps guard to the Qwen3-VL video backend. The two ",
      "labels": [
        "bug",
        "needs-rebase",
        "multi-modality",
        "qwen"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM",
        "GLM4"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-53885",
      "repository": "vllm-project/vllm",
      "number": 53885,
      "type": "pr",
      "category": "DOCS",
      "categoryValid": true,
      "ruleCategory": "DOCS",
      "classificationConsistent": true,
      "title": "[CI][ROCm][Disagg] Add GLM-5.2-FP8 to MoRIIO model catalog",
      "bodyExcerpt": "## Purpose\r\n\r\nAdd `GLM-5.2-FP8` to the AMD MoRIIO disaggregated CI model catalog for 1P1D TP8 and Wide-EP DP8 prefill/decode runs.\r\n\r\n## Changes\r\n\r\n* `.buildkite/amd-disagg/models.yaml` — add `GLM-5.2-FP8` entry:\r\n  * AITER env: `VLLM_ROCM_USE_AITER{,_MLA,_MOE,_RMSNORM}=1`, `VLLM_ROCM_USE_AITER_FUSION_SHARED_EXPERTS=0`\r\n  * TP8: `--trust-remote-code --kv-cache-dtype fp8 --gpu-memory-utilization 0.85`\r\n  * Wide-EP DP8: prefill `--gpu-memory-utilization 0.85 --enforce-eager`; decode `--gpu-memory-utilization 0.85` with PIECEWISE cudagraph\r\n\r\n## Test Plan\r\n\r\nMoRIIO 1P1D, proxy, `vllm/vllm-openai-rocm:nightly`.\r\n\r\n### GLM-5.2-FP8 — TP8 1P1D\r\n\r\n```bash\r\nexport VLLM_ROCM_USE_AITER=1\r\nexport VLLM_ROCM_USE_AITER_MLA=1\r\nexport VLLM_ROCM_USE_AITER_MOE=1\r\nexport VLLM_ROCM_USE_AITER_RMSNORM=1\r\nexport VLLM_ROCM_USE_AITER_FUSION_SHARED_EXPERTS=0\r\n\r\n# PREFILL — TP8\r\nvllm serve /data/models2/GLM-5.2-FP8 \\\r\n    --host $PREFILL_IP --port 8100 --tensor-parallel-size 8 \\\r\n    --trust-remote-code --kv-cache-dtype fp8 \\\r\n    --gpu-memory-utilization 0.85 \\\r\n    --kv-transfer-config '{\"kv_connector\":\"MoRIIOConnector\",\"kv_role\":\"kv_producer\",\r\n      \"kv_connector_extra_config\":{\"proxy_ip\":\"$PREFILL_IP\",\"proxy_ping_port\":\"36367\",\r\n      \"http_port\":\"8100\",\"handshake_port\":\"6301\",\"notify_port\":\"61005\"}}'\r\n\r\n# DECODE — TP8\r\nvllm serve /data/models2/GLM-5.2-FP8 \\\r\n    --host $DECODE_IP --port 8200 --tensor-parallel-size 8 \\\r\n    --trust-remote-code --kv-cache-dtype fp8 \\\r\n    --gpu-memory-utilization 0.85 \\\r\n    --kv-transfer-config '{\"kv_connector\":\"MoRIIOConnector\",\"kv_role\":\"kv_consumer\",\r\n      \"kv_connector_extra_config\":{\"proxy_ip\":\"$PREFILL_IP\",\"proxy_ping_port\":\"36367\",\r\n      \"http_port\":\"8200\",\"handshake_port\":\"6301\",\"notify_port\":\"61005\"}}'\r\n```\r\n\r\n### GLM-5.2-FP8 — Wide-EP DP8 1P1D\r\n\r\n```bash\r\nexport VLLM_ROCM_USE_AITER=1\r\nexport VLLM_ROCM_USE_AITER_MLA=1\r\nexport VLLM_ROCM_USE_AITER_MOE=1\r\nexport VLLM_ROCM_USE_AITER_RMSNORM=1\r\nexport VLLM_ROCM_USE_AITER_FUSION_SHARED_EXPERTS=0\r\n\r\n#",
      "labels": [
        "documentation",
        "rocm",
        "ready",
        "ci/build",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 7,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-53903",
      "repository": "vllm-project/vllm",
      "number": 53903,
      "type": "pr",
      "category": "OTHER",
      "categoryValid": true,
      "ruleCategory": "OTHER",
      "classificationConsistent": true,
      "title": "[NIXL][PCP] Report replicated-PCP ranks > 0 as done sending instead of hiding them",
      "bodyExcerpt": "## Purpose\r\n\r\nWith replicated KV under PCP only PCP rank 0 takes part in NIXL transfers, so `NixlConnector` on ranks > 0 cleared `done_sending` and `get_finished_count()` was lowered to `TP x PP` so the scheduler only waited for rank 0.\r\n\r\nThat breaks as soon as `NixlConnector` is wrapped in a `MultiConnector` with a sibling that saves on every rank (e.g. `OffloadingConnector`):\r\n\r\n- `MultiConnector.get_finished_count()` returns `None` when any child returns `None`, so the `KVOutputAggregator` expects `world_size` finishes per request;\r\n- `MultiConnector` records an extra async save per request and only reports it finished once *every* child on that worker has — and NIXL on ranks > 0 never does.\r\n\r\nRequests therefore never finish sending, their blocks are never freed and the prefill engine stalls at `Running: 0 reqs, Waiting: N reqs` with the KV cache full. Observed with GLM-5.2-FP8 PCP8 prefill, `MultiConnector = NixlConnector(kv_producer) + OffloadingConnector(TieringOffloadingSpec)`: 14/35 requests completed at concurrency 16, then nothing.\r\n\r\nFix: ranks > 0 report the requests they are asked to send as finished as soon as the connector metadata arrives, and `get_finished_count()` is back to `world_size`. Rank 0 still reports only when the consumer has read the blocks, so the aggregate completes exactly when it did before.\r\n\r\n## Test Plan\r\n\r\n- E2E: GLM-5.2-FP8, prefill `--prefill-context-parallel-size 8` + MultiConnector (NIXL producer + OffloadingConnector), DP8 decode; agentic trace benchmark at concurrency 16/32/64.\r\n- Plain NixlConnector under PCP8 (no MultiConnector) unchanged behaviour.\r\n\r\n## Test Result\r\n\r\nBefore: prefill stalls after ~14 requests (0 running / 16 waiting / KV 70%). After: pending — will update with the benchmark run.\r\n\r\n🤖 Generated with [Claude Code](https://claude.com/claude-code); reviewed by author\r\n",
      "labels": [
        "kv-connector"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-53953",
      "repository": "vllm-project/vllm",
      "number": 53953,
      "type": "pr",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[ROCm][Model][AMD][Performance]: Added AITER fusion on top of the existing TRITON fusion path for GLM-5.2-MXFP4 through deepseek_v32",
      "bodyExcerpt": "## Purpose\r\n\r\nAdds an AITER-backed fusion path for the ROCm `deepseek_v32` (DeepSeek Sparse\r\nAttention) forward, used by both DeepSeek-V3.2 and GLM-5.2. The existing\r\nshared-Triton fusion path is untouched and remains the default; the AITER path\r\nis selected at runtime per fusion via opt-in env flags, so behaviour on ROCm is\r\nunchanged unless a flag is set.\r\n\r\nThis PR is stacked on top of: https://github.com/vllm-project/vllm/pull/51915, so needs enablement to land first before this fusion add-on.\r\n\r\n### Fusions added\r\n\r\n**1. MLA dual RMSNorm** \r\nFuses the q and kv-a RMSNorms into one AITER call\r\n(`fused_mla_dual_rms_norm_out`). An out-variant was added so the norms write\r\nmodel-owned buffers rather than allocating per layer, which lets the call be\r\nhoisted out of the eager region and captured by cudagraph. Also wires the\r\nper-token-quant variant for the quantized case.\r\n\r\n**2. Fused QK RoPE + concat + MLA cache write** \r\n`rocm_aiter_fused_qk_rope_concat_and_cache_mla` replaces the separate RoPE,\r\nconcat and KV-cache-write steps on the MLA path. The uint8 KV cache allocated by\r\n`--kv-cache-dtype fp8` is viewed as `float8_e4m3fn` before the call, since the\r\nAITER op rejects the raw uint8 buffer.\r\n\r\n**3. Fused indexer QK RoPE + fp8 quant + cache write**\r\n`rocm_aiter_indexer_qk_rope_quant_and_cache` fuses the lightning-indexer key\r\nLayerNorm, RoPE, fp8 quantization, and cache write into one call for the 21\r\nindexer layers.\r\n\r\n**4. Fused all-reduce + RMSNorm**\r\nUses AITER's custom all-reduce with an in-kernel RMSNorm. Gated on custom-AR\r\neligibility (`should_custom_ar`) because the op asserts the kernel ran, and\r\noversized batches must fall back to the standard path — this is the check the\r\ntorch.compile pass performs at compile time and which an inline call has to do\r\nitself.\r\n\r\n**5. `VLLM_ROCM_USE_AITER_CUSTOM_AR`** exposes AITER's custom all-reduce\r\nindependently of the fused variant.\r\n\r\n## Test Plan\r\n\r\nTo switch between the two fusion paths, use the env var below:\r\n",
      "labels": [
        "rocm",
        "needs-rebase",
        "deepseek",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 7,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-54396",
      "repository": "vllm-project/vllm",
      "number": 54396,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][Multimodal] Guard GLMGA video sampling against zero source fps",
      "bodyExcerpt": "## Motivation\n\n`GLMGAVideoBackend.compute_frames_index_to_sample` divides by `source.original_fps` in two places — the duration fallback (`round(max_frame_idx / original_fps) + 1`) and `duration_per_frame = 1 / original_fps` — without validating it. vLLM reports `original_fps == 0` for clips with an unknown or variable frame rate (VFR, malformed containers, failed webcam captures), so those inputs crash with a bare `ZeroDivisionError: float division by zero`.\n\nEvery other fps-driven backend in `vllm/multimodal/video.py` already guards this (`Qwen2VLVideoBackend`, `Qwen3VLVideoBackend`, `GLM46VVideoBackend`, and `Molmo2VideoBackend` in fps mode); GLMGA is the remaining one without the check.\n\n## Modifications\n\n- Added the same `original_fps <= 0` guard to `GLMGAVideoBackend`, raising a descriptive `ValueError` that names the backend and the cause, mirroring the existing Qwen2-VL / Qwen3-VL behavior.\n- Added a regression test covering both crash paths (duration reported vs. duration missing).\n\n## Verification\n\n- `pytest tests/multimodal/test_video.py::test_glmga_video_backend_rejects_unknown_source_fps` -> 1 passed.\n- On unpatched `main`, the same test fails with `ZeroDivisionError: float division by zero` for both `duration=5.0` (at `duration_per_frame = 1 / original_fps`) and `duration=0.0` (at the duration fallback).\n- Neighbor sampling tests `pytest tests/multimodal/test_video.py -k glm46v`: 12 passed; the single failure (`test_video_processor_from_model_repo[glm46v]`) is an unrelated missing `av` module in my local environment and fails identically on unpatched `main`.\n\n## Duplicate check\n\nSearched open PRs and issues for `glmga`, `glmga fps`, and `video original_fps`: nothing covers the GLMGA backend. #53844 fixes the same bug class for `Qwen3VLVideoBackend` only and does not touch GLMGA; this PR is scoped to GLMGA so the two can land independently.\n\n## AI assistance\n\nThis change was developed with AI assistance (bug identification, patch, and regression test). ",
      "labels": [
        "bug",
        "multi-modality",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM",
        "GLM4"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-54702",
      "repository": "vllm-project/vllm",
      "number": 54702,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][Quark] Honor layer_quant_config for identity packed_modules_mapping entries",
      "bodyExcerpt": "## Purpose\r\n\r\nFixes #54641 — reported and diagnosed by @stefanskiasan.\r\n\r\nAn identity entry in `packed_modules_mapping` (a module mapped to itself, e.g.\r\n`\"gate_up_proj\": [\"gate_up_proj\"]`) silently disables `layer_quant_config` for that module. The\r\nlayer falls back to `global_quant_config`, so a mixed-precision Quark checkpoint is built with the\r\nwrong scheme and only fails later, at weight load, with a shape mismatch.\r\n\r\nBoth lookup paths in `QuarkConfig` treat `shard_name == layer_name` as \"no shard information\":\r\n\r\n```python\r\n# get_layer_quant_config_from_name\r\nif shard_name != layer_name:\r\n    config = self.get_layer_quant_config_from_name(shard_name)\r\nelse:\r\n    config = None              # identity entry -> no config\r\n\r\n# _find_matched_config\r\nif shard_name == layer_name:\r\n    config = fallback_config   # identity entry -> global scheme\r\n```\r\n\r\nThat guard is only there to stop the recursion, since substituting an identity entry is a no-op.\r\nFor such an entry the shard name *is* the layer name, so the lookup should resolve it rather than\r\ngive up — which is what the other two fused-name expanders already do:\r\n`quant_utils.is_layer_skipped` special-cases a direct fused-name match, and\r\n`quark.utils.should_ignore_layer` degenerates to the same lookup. Only `QuarkConfig` bails out.\r\n\r\nIdentity entries are an established in-tree pattern — `glm4_1v`, `plamo3`, `molmo`, `molmo2`,\r\n`mimo_v2`, `sarvam`, `jamba`, `lfm2`, `granitemoehybrid` all register at least one — and\r\n`get_layer_quant_config_from_name` is also consumed by `quantization/utils/config_utils.py` for\r\nthe fused-shared-expert check and the OCP-MX group-size helper.\r\n\r\n**Fix:** extract the `layer_quant_config` pattern match into `_match_layer_quant_config()` — a\r\nverbatim move of the existing `else`-branch body, so matching semantics are unchanged — and use it\r\nfor the identity branch in both methods. Genuine fusions, and identity entries that\r\n`layer_quant_config` does not name, behave exactly as before",
      "labels": [
        "bug",
        "quantization"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM",
        "GLM4"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-54756",
      "repository": "vllm-project/vllm",
      "number": 54756,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][KV Offload] Register mixed page sizes in one cache group",
      "bodyExcerpt": "## Purpose\r\n\r\nFix a startup crash of `SimpleCPUOffloadConnector` on sparse-MLA (DSA) models (DeepSeek-V3.2, GLM-5/5.2). With the connector enabled, the engine dies in `register_kv_caches` before serving (GLM-5.2-FP8, TP=8):\r\n\r\n```\r\nRuntimeError: shape '[-1, 42072, 8448]' is invalid for input of size 130343768064\r\n```\r\n\r\nRoot cause: registration reinterpreted the whole KV allocation with a single layer's block size,\r\n\r\n```python\r\nregions = raw[:tensors[0].size].view(-1, num_blocks, block_bytes)\r\n```\r\n\r\nwhich only divides evenly when every layer in the allocation has the same page size. DSA models break that assumption: a layer's MLA latent cache and its indexer key cache are both `MLAAttentionSpec` with the same block size, so they land in one `UniformTypeKVCacheSpecs` group whose page size is their sum, while the individual pages differ (576 vs 132 bytes/token, i.e. 36864 vs 8448 bytes at block size 64). With a layer-outermost layout the group's layers alias one allocation, and `num_layers * (mla_page + indexer_page)` is a multiple of neither page, so `view()` raises.\r\n\r\n`--kv-cache-dtype fp8` is not a precondition. The indexer key cache is always `uint8` (128 fp8 keys plus one fp32 scale per 128 elements), so it does not scale with the KV cache dtype; at bf16 the pages become 73728 / 8448 and `gcd` is still 768.\r\n\r\nRegistration now derives each region from the placement metadata #51718 put on `KVCacheTensor` (`layers`/`layer_stride`/`block_stride`/`offset`) instead of reinterpreting the allocation with one layer's block size:\r\n\r\n- a layer-outermost tensor splits into one `[num_blocks, block_bytes]` region per layer (per KV head group under LHBNC), using the tensor's own block stride;\r\n- a block-outermost tensor stays a single region, as before;\r\n- regions are deduplicated by address, so overlaid cache groups still register once.\r\n\r\nThis also fixes a latent misregistration in mixed groups where the division happens to succeed. Dedup was keyed on backing storage, so ",
      "labels": [
        "bug"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-54770",
      "repository": "vllm-project/vllm",
      "number": 54770,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][Quantization] Register Quark per-block FP8 scales as weight_scale",
      "bodyExcerpt": "## Summary\n- Register Quark per-block FP8 linear scales as `weight_scale`, matching what Quark exports, instead of `weight_scale_inv` (DeepSeek-V3 `Fp8LinearMethod` convention).\n- Drop the DeepSeek-V4 AMD mapper rewrite of non-expert `.weight_scale` → `.weight_scale_inv`. Loaders already alias to `_inv` only when that parameter is registered (`Fp8LinearMethod` / MXFP4 override).\n- Read block scales through `get_fp8_block_weight_scale()` so DSV4 ROCm preshuffle and MLA `wo_a` dequant work with Quark layers.\n\nThis is the quant-method fix discussed on #54556 / #52263 / #47972, rather than another model-specific remap.\n\n## Test plan\n- [x] `pytest tests/quantization/test_quark.py::test_quark_w8a8_fp8_per_block_registers_weight_scale`\n- [x] Load `amd/GLM-5.3-Quark-MXFP4` (or equivalent) without `KeyError` on `.weight_scale` — GSM8K 0.9158, see [this comment](https://github.com/vllm-project/vllm/pull/54770#issuecomment-5506943426)\n- [x] Load DeepSeek-V4 AMD Quark NVFP4 (`QuarkW8A8Fp8PerBlock`) and MXFP4 (`Fp8LinearMethod` + `_resolve_param_name`) — NVFP4 verified (GSM8K 0.9507, see [this comment](https://github.com/vllm-project/vllm/pull/54770#issuecomment-5506943426)); MXFP4 still pending\n- [x] Confirm native DeepSeek `.scale` → `weight_scale_inv` path is unchanged\n",
      "labels": [
        "bug",
        "rocm",
        "deepseek",
        "quantization",
        "verified",
        "DSv4"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-54929",
      "repository": "vllm-project/vllm",
      "number": 54929,
      "type": "pr",
      "category": "OTHER",
      "categoryValid": true,
      "ruleCategory": "OTHER",
      "classificationConsistent": true,
      "title": "[Attention] Portable Triton sparse-MLA fallback for SM12x (consumer Blackwell)",
      "bodyExcerpt": "## Purpose\r\n\r\nDSA models (DeepSeek-V3.2, GLM-5.2) currently have **no working sparse-attention backend on\r\nconsumer Blackwell** (SM12x: RTX 5090, GB10): the native FlashMLA sparse kernels\r\n(`vllm._flashmla_C`) only build for sm90/sm100, and the sole SM12x option,\r\n`FLASHINFER_MLA_SPARSE_SM120`, livelocks under sustained load — GPUs pin at 100 % with zero\r\noutput until the NCCL watchdog kills the server. Fixes #51921 (on SM12x).\r\n\r\nThis makes the `FLASHMLA_SPARSE` backend work on SM12x by binding portable **Triton**\r\nimplementations of `flash_mla_sparse_fwd` / `flash_mla_with_kvcache` when the native extension\r\ncan't serve the device. The kernels are from #49026 (@marksunner, validated there on\r\nsm_121/GB10) — this PR is that work rebased onto current main as a first-class code path\r\ninstead of that PR's runtime monkeypatch, with its model-side diffs dropped (main already has\r\nthat content). Supersedes/continues #49026 — happy to hand back or co-drive.\r\n\r\nChanges:\r\n- `ops/flashmla.py`: `_use_triton_sparse_mla()` (auto on SM12x; `VLLM_TRITON_MLA_SPARSE`\r\n  forces on/off) selects the Triton binding; `is_flashmla_sparse_supported()` reports True\r\n  there; `get_mla_metadata` returns `(None, None)` (the Triton kernels self-schedule).\r\n- New files: `sm12x_sparse_mla_attn.py`, `sparse_mla_kernels.py`, `sparse_mla_env.py`,\r\n  `ops/deepseek_v4_ops/*` — kernels + helpers from #49026, unchanged.\r\n- `flashmla_sparse.py`: capability 12 allowed on the Triton path; %16 fp8-decode head padding\r\n  there (64-head models at TP=8 have 8 heads/rank — 64-padding wastes 87 % of attention\r\n  compute); b12x fused fast path with clean fallback.\r\n- `sparse_attn_indexer.py`: DeepGEMM not hard-required on the Triton path (fallbacks exist).\r\n- `envs.py`: `VLLM_TRITON_MLA_SPARSE*` registered.\r\n\r\n**All changes are no-ops on sm90/sm100** unless `VLLM_TRITON_MLA_SPARSE=1` is set explicitly.\r\n\r\n## Test Plan\r\n\r\nEnd-to-end serving on 24× RTX 5090 (3 hosts, TP=8 × PP=3), GLM-5.2-NVFP4,\r\n`--kv-cache-dtype",
      "labels": [],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-54935",
      "repository": "vllm-project/vllm",
      "number": 54935,
      "type": "pr",
      "category": "NEW_SUPPORT",
      "categoryValid": true,
      "ruleCategory": "NEW_SUPPORT",
      "classificationConsistent": true,
      "title": "[Security] Cap GLMGA video sampling to prevent request-driven resource exhaustion",
      "bodyExcerpt": "## Summary\n\n- Add hard class-level upper bounds (`_MAX_FRAMES=640`, `_MAX_FPS=30`) to `GLMGAVideoBackend` so that request-controlled `fps` and `max_frames` values via `media_io_kwargs` cannot create disproportionately large intermediate list allocations.\n- Clamp `extract_t` to `total_frames_num`, ensuring the index list never exceeds the actual frame count of the video, regardless of sampling parameters.\n- Add regression tests verifying extreme values are bounded and normal operation is unchanged.\n\n## Root Cause\n\n`GLMGAVideoBackend.compute_frames_index_to_sample` computes `extract_t = min(int(duration * target_fps), max_frames)` where both `target_fps` and `max_frames` are controllable via request-level `media_io_kwargs`. When `total_frames_num < extract_t`, a list comprehension with `range(extract_t)` creates an attacker-sized intermediate allocation (e.g. 500k entries for a 2-frame video) that is subsequently deduplicated to just the actual frame count.\n\n## Fix\n\nThree layers of defense, following the pattern established by `GLM46VVideoBackend`:\n\n1. **Cap `target_fps`** to `_MAX_FPS` (30) -- request values above this are clamped.\n2. **Cap `max_frames`** to `_MAX_FRAMES` (640) -- request values above this are clamped.\n3. **Clamp `extract_t` to `total_frames_num`** -- the critical invariant: there can never be more unique frame indices than there are frames in the video.\n\n## Test Plan\n\n- [x] `TestGLMGASamplingCaps::test_extreme_values_bounded_by_total_frames` -- 2-frame video with fps=500k, max_frames=500k produces at most 2 indices.\n- [x] `TestGLMGASamplingCaps::test_class_cap_overrides_kwargs_max_frames` -- max_frames=100k from kwargs is clamped to 640.\n- [x] `TestGLMGASamplingCaps::test_class_cap_overrides_target_fps` -- fps=500k is clamped to 30.\n- [x] `TestGLMGASamplingCaps::test_normal_operation_unchanged` -- normal 1000-frame video with fps=2 produces expected results.\n- [x] Pre-commit hooks pass.\n\n\nMade with [Cursor](https://cursor.com)",
      "labels": [
        "ready",
        "multi-modality",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM",
        "GLM4"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-54951",
      "repository": "vllm-project/vllm",
      "number": 54951,
      "type": "pr",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[Perf][GLM-5.3-Flash] Shard long-context indexer prefill rows across TP",
      "bodyExcerpt": "## Summary\n\nThis PR shards the replicated GLM-5.3-Flash sparse-indexer prefill work across\ntensor-parallel (TP) ranks for long MQA prefills. Each rank computes a\ncost-balanced contiguous query-row slice, runs MQA logits/top-k locally, and\nexchanges only the final int32 index rows with one layout-preserving\n`all_gatherv` per indexer layer.\n\n## Scope and safety\n\n- The row planner uses compressed causal MQA key length as its cost, avoiding\n  an imbalanced “equal rows” split for ragged/long contexts.\n- GLM kpool expansion and incomplete-pool tail semantics are preserved; the\n  gathered width includes `topk_tokens + index_kpool - 1` tail columns.\n- The feature is gated to CUDA, TP > 1, long MQA prefills with at least 16,384\n  rows per rank (64K total rows at TP4). DCP/PCP, ROCm, disabled\n  PyNCCL/symmetric-memory,\n  batch-invariant, and FULL CUDA-graph modes retain the old path.\n- Decode, short-prefill, KV-cache, sampling, model-weight, and excluded\n  execution paths are unchanged. No padded/fixed-shape collective is used by\n  the upstream treatment path.\n\nThe branch is synchronized with the latest upstream `main` at\n`8b6de0eb9a09ef53f20cf06bd4d17ee264b9c2a` (which includes #53906). The sync\nis recorded by merge commit `95adf58f00ba16f33a3c69c439d51a86e4a79db2`;\n`git merge-base --is-ancestor origin/main HEAD` passes. The benchmark baseline\nremains the latest #53906 head `4500c80c080328dfe62435d083f4063e00d987df`\n(`refs/pull/53906/head`), while the optimization commits are `9bef8be552` and\n`a30eb3e734`; the kpool correctness follow-up is `e4185bbd90`, long-context\ncoverage is `d66da5c4a6`, and the short-path gate follow-up is `a3734b8bb5`.\nThe upstream sync changes no optimization semantics; compared with current\n`main`, the PR remains limited to the two indexer implementations and their\nfocused row-sharding test.\n\n## Stable 128-output-token A/B rescan (supersedes the earlier table)\n\nThis rescan uses the latest #53906 baseline above and the pre-gate PR head\n`d66da5c4a6` (",
      "labels": [
        "new-model",
        "rocm",
        "speculative-decoding",
        "torch.compile",
        "ci/build",
        "multi-modality",
        "kv-connector",
        "nvidia",
        "quantization",
        "mrv2",
        "scheduler",
        "kv-cache-manager",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55095",
      "repository": "vllm-project/vllm",
      "number": 55095,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix] Fall back to full decode graphs for noncompiled models",
      "bodyExcerpt": "- Apply the fallback only to platform-selected model paths without an active torch-compile boundary.\n- Preserve ROCm's compiled MRV1 defaults and the generic compiled implementations used by DeepSeek V3.2 and GLM DSA models.\n- Default unset graph modes to `FULL_DECODE_ONLY` at O2/O3 and `NONE` at O0/O1 when breakable graphs are unavailable.\n- Default DeepSeek V4 MRV2 to `NONE` on gfx950 because full-decode capture has an unresolved accuracy risk in [#52644](https://github.com/vllm-project/vllm/issues/52644).\n- Preserve safe explicit graph choices, reject compile/adaptive combinations that can require unavailable piecewise capture, and clear stale capture sizes.\n- Causal changes were [#52861](https://github.com/vllm-project/vllm/pull/52861), [#53155](https://github.com/vllm-project/vllm/pull/53155), and [#53183](https://github.com/vllm-project/vllm/pull/53183); [#54852](https://github.com/vllm-project/vllm/pull/54852) exposed the combination.\n- Draft [#54127](https://github.com/vllm-project/vllm/pull/54127) changes global breakable-graph policy but not this ROCm fallback, while [#52968](https://github.com/vllm-project/vllm/pull/52968), [#53166](https://github.com/vllm-project/vllm/pull/53166), and [#54038](https://github.com/vllm-project/vllm/pull/54038) are workload-specific overrides.\n\nThese model paths lack an active torch-compile boundary and normally rely on breakable CUDA graphs for piecewise capture. ROCm disables breakable graphs by default for performance, so O2 otherwise selects an unavailable `PIECEWISE` path. On gfx942 the fallback keeps uniform decode captured through `FULL_DECODE_ONLY`, while gfx950 DeepSeek V4 stays eager until its graph-pool correctness issue is resolved. Safe explicit modes remain available, while impossible piecewise and adaptive-verification combinations now fail early or normalize to an executable mode. All 69 focused cases and applicable pre-commit hooks passed, and the broader configuration run passed 289 tests with six skips af",
      "labels": [
        "bug",
        "speculative-decoding",
        "mrv2"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-55119",
      "repository": "vllm-project/vllm",
      "number": 55119,
      "type": "pr",
      "category": "NEW_SUPPORT",
      "categoryValid": true,
      "ruleCategory": "NEW_SUPPORT",
      "classificationConsistent": true,
      "title": "[Feat] Add EPLB support for GLM-5.3-Flash",
      "bodyExcerpt": "\r\n\r\n\r\n## Purpose\r\n\r\n## Test Plan\r\n```\r\nVLLM_ENGINE_READY_TIMEOUT_S=3600 vllm serve  /mnt/nvme/shared/models/ZhipuAI/GLM-5.3-Flash --tensor-parallel-size 4 --tool-call-parser glm47 --enable-auto-tool-choice --reasoning-parser glm45 --load-format instanttensor  -ep --no-enable-flashinfer-autotune --enable-eplb --eplb-config '{\"window_size\":100,\"step_interval\":100,\"num_redundant_experts\":8,\"use_async\":true}\r\n\r\n```\r\n## Test Result\r\nbefore\r\n```\r\n(EngineCore pid=174936) ERROR 09-03 17:06:47 [core.py:1385] EngineCore failed to start.\r\n(EngineCore pid=174936) ERROR 09-03 17:06:47 [core.py:1385] Traceback (most recent call last):\r\n(EngineCore pid=174936) ERROR 09-03 17:06:47 [core.py:1385]   File \"/home/chauncey/vllm/vllm/v1/engine/core.py\", line 1347, in run_engine_core\r\n(EngineCore pid=174936) ERROR 09-03 17:06:47 [core.py:1385]     engine_core = EngineCoreProc(*args, engine_index=dp_rank, **kwargs)\r\n(EngineCore pid=174936) ERROR 09-03 17:06:47 [core.py:1385]                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\r\n(EngineCore pid=174936) ERROR 09-03 17:06:47 [core.py:1385]   File \"/home/chauncey/vllm/vllm/tracing/otel.py\", line 178, in sync_wrapper\r\n(EngineCore pid=174936) ERROR 09-03 17:06:47 [core.py:1385]     return func(*args, **kwargs)\r\n(EngineCore pid=174936) ERROR 09-03 17:06:47 [core.py:1385]            ^^^^^^^^^^^^^^^^^^^^^\r\n(EngineCore pid=174936) ERROR 09-03 17:06:47 [core.py:1385]   File \"/home/chauncey/vllm/vllm/v1/engine/core.py\", line 1104, in __init__\r\n(EngineCore pid=174936) ERROR 09-03 17:06:47 [core.py:1385]     super().__init__(\r\n(EngineCore pid=174936) ERROR 09-03 17:06:47 [core.py:1385]   File \"/home/chauncey/vllm/vllm/v1/engine/core.py\", line 145, in __init__\r\n(EngineCore pid=174936) ERROR 09-03 17:06:47 [core.py:1385]     kv_cache_config = self._initialize_kv_caches(vllm_config)\r\n(EngineCore pid=174936) ERROR 09-03 17:06:47 [core.py:1385]                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\r\n(EngineCore pid=174936) ERROR 09",
      "labels": [
        "ready",
        "nvidia",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 7,
      "matchedTerms": [
        "GLM",
        "GLM4"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55201",
      "repository": "vllm-project/vllm",
      "number": 55201,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix] Reject invalid GLM kpool indices",
      "bodyExcerpt": "## Purpose\n\nGLM-5.3 kpool top-k can produce fewer valid entries than the allocated `select_k` width for short rows. The kernels do not promise to write the remaining slots, so allocating `pool_topk` with `torch.empty` can leave arbitrary positive values that are later treated as pool IDs and expanded into out-of-range sparse-attention token indices.\n\nThis change adds two defensive layers:\n\n1. Initialize the prefill and decode pool-topk destinations with the `-1` sentinel.\n2. Reject pool IDs outside the completed-pool count in the NVIDIA and AMD fused expansion kernels and in the current prefill fallback.\n\nThe regression covers valid pool IDs, negative sentinels, positive out-of-range IDs, and an incomplete tail. Only tokens from valid completed pools and the legitimate tail survive.\n\nThis was originally developed in https://github.com/ZJY0516/vllm/pull/8 while https://github.com/vllm-project/vllm/pull/53906 was still pending. Now that #53906 is merged, this is the upstream-main version.\n\n## Why this is not duplicating existing work\n\nBefore opening this PR, I searched open PRs for references to #53906, `GLM kpool invalid indices`, `pool_topk expand_pools`, and the broader `kpool` term. No open PR carries this exact sentinel initialization plus completed-pool bounds check.\n\nThe closest active changes are orthogonal: #54951 changes kpool row sharding but does not initialize unwritten top-k slots or validate expanded pool IDs, while #53969 concerns SM120 NoPE/effective-width behavior.\n\n## Test plan and results\n\nCommands run from an isolated Python 3.12 venv:\n\n```text\ngit diff --check\n.venv/bin/python -m compileall -q tests/kernels/test_kpool_decode_update_batched.py vllm/model_executor/layers/sparse_attn_indexer_kpool.py vllm/models/glm5next/amd/ops/kpool_compress.py vllm/models/glm5next/nvidia/ops/kpool_compress.py\nPRE_COMMIT_HOME=/private/tmp/vllm-precommit-cache .venv/bin/pre-commit run --files tests/kernels/test_kpool_decode_update_batched.py vllm/model_executor/lay",
      "labels": [
        "bug",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55214",
      "repository": "vllm-project/vllm",
      "number": 55214,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][Docs] Package glm5next nvidia subtree and fix its docstrings",
      "bodyExcerpt": "## Purpose\n\nThe docs build emits these warnings on `main`:\n\n```\nWARNING - api-autonav: Skipping implicit namespace package (without an __init__.py file) at .../vllm/models/glm5next/nvidia\nWARNING - griffe: vllm/models/glm5next/amd/ops/kpool_compress.py:634: No type or annotation for parameter 'key / slot_score'\nWARNING - griffe: vllm/models/glm5next/amd/ops/kpool_compress.py:634: Parameter 'key / slot_score' does not appear in the function signature\nWARNING - griffe: vllm/models/glm5next/amd/ops/kpool_compress.py:636: No type or annotation for parameter 'slot_mapping / positions'\nWARNING - griffe: vllm/models/glm5next/amd/ops/kpool_compress.py:636: Parameter 'slot_mapping / positions' does not appear in the function signature\n```\n\nThe first one is more than a docs nit. `vllm/models/glm5next/nvidia/` and `vllm/models/glm5next/nvidia/ops/` have no `__init__.py`, and `pyproject.toml` uses `[tool.setuptools.packages.find]` (regular packages, not namespace packages), so **the entire nvidia subtree is silently dropped from built wheels**:\n\n```python\n>>> from setuptools import find_packages\n>>> sorted(p for p in find_packages(include=[\"vllm*\"]) if \"glm5next\" in p)\n['vllm.models.glm5next',\n 'vllm.models.glm5next.amd',\n 'vllm.models.glm5next.amd.ops',\n 'vllm.models.glm5next.amd.ops.third_party',\n 'vllm.models.glm5next.amd.ops.third_party.kda']\n```\n\nNote the missing `vllm.models.glm5next.nvidia*` entries, including `nvidia/ops/third_party/kda` (which does have its own `__init__.py`, but is unreachable because its parents do not). Editable installs work by path so this is invisible locally.\n\n## Changes\n\n- Add `__init__.py` (SPDX header only, matching the sibling `amd` packages) to `vllm/models/glm5next/nvidia/` and `vllm/models/glm5next/nvidia/ops/`.\n- Split the combined `key / slot_score:` and `slot_mapping / positions:` Google-style docstring entries into one entry per parameter in both `amd/ops/kpool_compress.py` and `nvidia/ops/kpool_compress.py`. griffe treats the combine",
      "labels": [
        "bug",
        "nvidia",
        "build-docs",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55219",
      "repository": "vllm-project/vllm",
      "number": 55219,
      "type": "pr",
      "category": "OTHER",
      "categoryValid": true,
      "ruleCategory": "OTHER",
      "classificationConsistent": true,
      "title": "[KV Cache] GLM-5.3-Flash: use the generic packed KV layout; CircularBufferSpec tail with speculative ring slots",
      "bodyExcerpt": "## Purpose\n\nFollow-up to #53906 (GLM-5.3-Flash). Remove the model-specific KV cache grouping / tensor layout and the matching NIXL and Mooncake special cases by routing GLM through the generic packed (block-outermost) layout that DeepSeek-V4 and Qwen3.8-Next already use, and fix two issues in the kpool tail ring.\n\n- `KpoolTailSpec` is dropped; the tail is a plain `CircularBufferSpec` (its manager already was `CircularBufferManager`).\n- The ring is sized for speculative tokens, `kpool * cdiv(kpool + num_spec, kpool)` as the QSA ring does. With exactly `kpool` slots, a rejected pool-completing draft left the drafts behind it overwriting the committed keys the redo needs (covered by new CUDA kernel and index-level tests). The seed/decode tail kernels (NVIDIA and AMD) take the ring size and the tail view's strides.\n- GLM's MLA and indexer pages have incompatible sizes, which `_get_kv_cache_groups_glm5_next` / `_glm5_next_tensor_layout` handled with a hand-rolled layer-outer layout, `storage_block_size`, worker-side virtual block splitting and NIXL \"dense virtual transfer pages\". The generic packed path already handles mixed page sizes, but a block-outermost block cannot be split by the worker into the 32/64-row pages DeepGEMM paged-MQA and the TRT-LLM sparse MLA kernel take. This PR re-pages inside the kernels' callers instead: `kpool_page_geometry` / `_kpool_flat_page_view` / `_indexer_page_table` for the indexer (fed by a `block_stride_bytes` builder hook), `_kernel_paged_view` for the FlashInfer sparse MLA impls (TRT-LLM on SM100 now `MultipleOf(32)`, 32-row pages; SM120 64-row pages; SM90 uses page size 1 so it only needs the row stride and no longer forces the LBHNC layout), and the packed block stride is aligned to the lcm of the kernel pages (`_packed_block_alignment_bytes`, only when a layer needs re-paging; DeepSeek-V4 and Qwen are unaffected). The page size a backend re-pages to is declared by a new `AttentionBackend.get_kernel_page_rows()` and recorded on `ML",
      "labels": [
        "needs-rebase",
        "kv-connector",
        "nvidia",
        "mrv2",
        "kv-cache-manager",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 8,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55222",
      "repository": "vllm-project/vllm",
      "number": 55222,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix] GLM-5.3-Flash: fp8 plan dtype on SM90 sparse MLA, and right-size the indexer prefill workspace",
      "bodyExcerpt": "## Purpose\n\nFixes #55221.\n\nTwo independent fixes for GLM-5.3-Flash, both live on `main` after #53906 landed.\n\n**1. `--kv-cache-dtype fp8` cannot start on the SM90 sparse MLA backend.** An fp8 KV\ncache is allocated as `uint8` storage and `run()` views it as `float8_e4m3fn`, but\n`plan()` was handed the storage dtype, which the FlashInfer wrapper rejects:\n`ValueError: MLA kv_data_type torch.uint8 is not supported`. One-line dtype fix in\n`FlashInferMLASparseSM90Builder`.\n\n**2. The indexer prefill workspace is sized in the wrong unit.**\n`get_max_prefill_buffer_size()` returns a token count (`max_model_len * 40` entries of\n132 bytes), but this indexer's KV is pool-granular: the spec carries\n`compress_ratio == index_kpool`, and `split_indexer_prefill_chunks()` is fed compressed\nsequence lengths. `deepseek_v4/attention.py` already divides at the same call site.\nAt `--max-model-len 1048576` with `index_kpool = 4` this locks ~5.16 GiB per GPU during\nthe memory profile, straight out of the KV budget. Reported by @nood-co1 in #53906.\n\n## Not a duplicate\n\n- #51252 divides the *builder's* chunk budget by `compress_ratio` for DeepSeek; this is\n  the model-side `max_total_seq_len` for GLM-5.3. Per @nood-co1 the two compose.\n- #54743 scopes offload group configs to prefix-cacheable groups; different file,\n  different failure.\n- Searched open PRs for `get_max_prefill_buffer_size`, `max_total_seq_len` and the SM90\n  sparse backend; nothing else touches these call sites.\n\n## Testing\n\nNo unit test covers either path (both need a GLM-5.3 checkpoint and FlashInfer on\nHopper), so this was validated by serving. 2x H200 NVL, TP2, LibertAI NVFP4 weights,\n`--kv-cache-dtype fp8`, `--max-model-len 1048576`, MTP k=3, CUDA graphs on, CPU offload\nwith a 256 GiB region. Both fixes have been running in production here since 2026-08-29\nand 2026-09-01 respectively.\n\n- fp8 fix: without it the server does not start; with it `FLASHINFER_MLA_SPARSE_SM90` is\n  selected and the KV pool holds 4,503,445 tokens",
      "labels": [
        "bug",
        "nvidia",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 7,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55266",
      "repository": "vllm-project/vllm",
      "number": 55266,
      "type": "pr",
      "category": "OTHER",
      "categoryValid": true,
      "ruleCategory": "OTHER",
      "classificationConsistent": true,
      "title": "[XPU][UT] skip GLM-5.3-Flash test on XPU",
      "bodyExcerpt": "https://buildkite.com/vllm/intel-ci/builds/10505/canvas?jid=01a0699a-92e8-4868-adc4-fa11fcac2c99&tab=output\r\nUnsupported model on XPU.",
      "labels": [
        "intel-gpu",
        "multi-modality",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55268",
      "repository": "vllm-project/vllm",
      "number": 55268,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][MoE] Warm up the Triton fused MoE GEMMs",
      "bodyExcerpt": "## Purpose\n\n`fused_moe_kernel` is the expert GEMM whenever DeepGEMM declines the shape. The\ndominant case is the `N <= 512` early-out in `_valid_deep_gemm`\n(`experts/deep_gemm_moe.py`): a model with a small `moe_intermediate_size`\ncrosses that threshold as soon as TP is high enough, and from then on never uses\nDeepGEMM. GLM-5.2-FP8 at TP=8 gives `w2 = (256, 6144, 256)`, i.e. `N=256`, so its\nwhole MoE stack runs on Triton for every request.\n\nNothing in the startup path compiles that kernel for the variants real batches\nselect. Two things vary with the token count `M`:\n\n1. the tile config, chosen by `try_get_optimal_moe_config` from a tuned config\n   file or `get_default_config`;\n2. the 16-divisibility of `EM` and `num_valid_tokens`, which Triton attaches as\n   a `tt.divisibility` attribute and keys separately.\n\nProfiling and CUDA graph capture only reach a few of these combinations, so the\nfirst request landing on an uncompiled one JIT-compiles mid-serving. Measured on\nGLM-5.2-FP8 at TP=8, main compiles 4 distinct variants per rank during\ninference (32 events across 8 ranks). With `--jit-monitor-mode=error` that is a\nhard failure; with the default `warn` it is a latency spike.\n\n## Changes\n\n- New `vllm/model_executor/warmup/triton_moe_warmup.py`: walks the `M` values\n  that can select a distinct tile config, pairs each with a neighbour that flips\n  the 16-divisibility of `num_valid_tokens` so both variants compile, and runs\n  the expert GEMMs through `TritonExperts.apply` once per value.\n- Called from `kernel_warmup` inside the existing `enable_jit_warmup` block.\n\nDesign notes:\n\n- Warmup drives `TritonExperts.apply` rather than reconstructing the ~30 kernel\n  arguments. `invoke_fused_moe_triton_kernel` derives the strides, `EM`,\n  `SWAP_AB` and the effective `BLOCK_SIZE_K` from the tensors it is handed, so\n  going through the real path is the only way to guarantee the compiled key\n  matches the serving key. A compile-only draft built on `TritonWarmupTensor`\n  was disc",
      "labels": [
        "bug"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-55270",
      "repository": "vllm-project/vllm",
      "number": 55270,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix] GLM-5.3-Flash: launch the kpool paged MQA logits in the varlen mode its schedule was built with",
      "bodyExcerpt": "## Purpose\n\nMake the kpool indexer's DeepGEMM paged-MQA launch agree with the schedule metadata it was built with. On SM100 `DeepseekV32IndexerMetadataBuilder` schedules the paged MQA logits in varlen mode (per-token rows tagged with request indices, `get_paged_mqa_logits_metadata(..., indices=...)`), and `sparse_attn_indexer.py` passes `indices=decode_metadata.indices` to `fp8_fp4_paged_mqa_logits`. The kpool indexer did not, so DeepGEMM selects the non-varlen `next_n=1` kernel and runs it on a varlen schedule. Under MTP a request's verify rows share one request index, so the two decompositions differ whenever a request's rows straddle a 256-state split boundary (1024 tokens at `index_kpool=4`).\n\nWhat that mismatch does depends on the DeepGEMM revision, verified with a standalone kernel script (same fp8 cache, block table and weights; schedule built with `indices`; launched with and without `indices`; compared against a pure non-varlen reference):\n\n| DeepGEMM | launch without `indices` on a varlen schedule |\n|---|---|\n| 2.5.0 (891d57b, what a stale local FetchContent checkout builds) | deadlock: the exact-end-match scheduler walks past an end it can never hit; the TMA warp exits and the math warps spin on the Q-stage mbarrier. Reproduced 3x end to end (GLM-5.3-Flash MTP3 GSM8K hangs within 4 min, user-triggered CUDA core dumps show the single live CTA) and in the 40-row script. |\n| 2.6.1 (8b1392b9, the repo pin) | tolerated: no hang, logits bit-identical to the reference over 640 rows including 80 straddling requests; GLM MTP3 GSM8K on `main` runs fine (0.9704 / 0.9731). |\n\nSo with the pinned DeepGEMM this is a latent contract violation rather than a live bug. The one-line fix matches the non-kpool indexer, removes the dependence on the scheduler tolerating a mismatched launch, and keeps the kpool path working on DeepGEMM revisions with the strict scheduler.\n\nDuplicate check: #55201 touches the kpool top-k sentinel handling, not this call; no open PR passes `indice",
      "labels": [
        "bug",
        "ready",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55275",
      "repository": "vllm-project/vllm",
      "number": 55275,
      "type": "pr",
      "category": "NEW_SUPPORT",
      "categoryValid": true,
      "ruleCategory": "NEW_SUPPORT",
      "classificationConsistent": true,
      "title": "[Model] Add GLM and compressed-tensors support to MegaMoE",
      "bodyExcerpt": "## Purpose\n\nExtend the `deep_gemm_mega_moe` backend to the DeepSeek-derived GLM model\nimplementations and support compressed-tensors NVFP4 expert checkpoints used by\nGLM, DeepSeek V4, and Kimi K3.\n\nThis change:\n\n- adds MegaMoE adapters for DeepSeek V2/GLM, GLM-5-next, DeepSeek V4, and Kimi K3,\n  including their MTP paths;\n- selects the MegaMoE weight format per layer so dense and routed-expert layers\n  can use their checkpoint-native formats;\n- converts compressed-tensors NVFP4 routed-expert weights to DeepGEMM's\n  E2M1/UE8M0 FP4 representation while preserving non-expert input scales;\n- preserves grouped routing, deferred-reduction, and sequence-parallel semantics;\n- covers the model adapters and weight-format behavior with focused unit tests.\n\n## Duplicate-work check\n\nThis does not duplicate the open related work:\n\n- #40833 introduces the general DeepGEMM MegaMoE kernel integration; it does not\n  provide these GLM/DeepSeek/Kimi model adapters or compressed-tensors loading.\n- #42844 experiments with FP4 activation dispatch for DeepSeek V4 using an\n  external DeepGEMM package; this PR uses the existing FP8-activation/FP4-weight\n  MegaMoE path and focuses on checkpoint/model integration.\n- #53527 targets FP8 DeepSeek V4 on SM90; this PR targets compressed-tensors\n  NVFP4 models and was validated on GB200/SM100.\n\n## Test plan and results\n\nLocal checks:\n\n```text\npre-commit run --files <all eight changed files>\n# all hooks passed, including ruff, formatting, mypy, and configuration guards\n\n.venv/bin/python -m pytest tests/models/test_deep_gemm_bf16_mega_moe.py -q\n# 13 passed\n```\n\nGB200 kernel correctness:\n\n- Random FP8 x FP4 MegaMoE, four ranks: relative L2 0.0252-0.0276 and cosine\n  similarity greater than 0.9996.\n- Exact GLM topology (EP8 across two GB200 nodes, hidden size 6144,\n  intermediate size 2048, 256 experts, top-k 8): all ranks produced finite\n  outputs.\n\nModel evaluation:\n\n- Model: `RedHatAI/GLM-5.2-NVFP4-FP8`\n- Topology: aggregated TP8/EP8 across two four-",
      "labels": [
        "deepseek",
        "DSv4",
        "kimi",
        "k3",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55277",
      "repository": "vllm-project/vllm",
      "number": 55277,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][SM120][MLA] Support NoPE sparse MLA (GLM-5.3-Flash) on the FlashInfer SM120 backend",
      "bodyExcerpt": "## Purpose\n\nMake the `FLASHINFER_MLA_SPARSE_SM120` backend usable for GLM-5.3-Flash (`glm5_next`, #53906), whose sparse MLA layers are NoPE (`qk_rope_head_dim=0`) with a kpool-widened top-k buffer. Two independent breakages are fixed:\n\n1. **`concat_and_cache` fp8_ds_mla rejects NoPE rows.** The packed fp8_ds_mla row layout is unchanged for NoPE models (512B latent + 16B tile scales + 128B reserved RoPE/padding = 656B), but the host check required `pe_dim == 64` and the kernel's RoPE warp assumed 64 tail elements. Relax the check to `pe_dim ∈ {64, 0}` and have the RoPE warp zero the reserved bytes when `pe_dim == 0`, so consumers of the DS-shaped row (e.g. `cp_gather_and_upconvert_fp8_kv_cache` on the chunked-context path) read exact zeros rather than stale memory — a zero rope lane is bit-exact NoPE for `q_pe · k_pe`. The FlashInfer GLM53_NOPE kernels never read the tail either way. A sentinel-filled regression test covers the NoPE row layout.\n\n2. **Sparse page-table sized by `topk_tokens` instead of the actual buffer width.** GLM-5.3-Flash widens the top-k buffer past `index_topk` with a kpool tail (2048 → 2176, rounded to a multiple of 128), so `topk_indices_physical.shape[1] != attn_metadata.topk_tokens` and FlashInfer rejects `block_tables (B, 1, 2176)` against `sparse_mla_top_k=2048`. Mirror the generic `flashinfer_mla_sparse` backend: derive `sparse_mla_top_k`/`max_seq_len` from the converted buffer width; the kernel treats it as page-table capacity and bounds the active length by the `-1` padding.\n\nNo behavior change for DSv3.2/DSv4 (RoPE, kpool=1): the packed row write is identical when `pe_dim == 64`, and `shape[1] == topk_tokens` when there is no kpool tail.\n\nRequires FlashInfer with GLM53_NOPE support plus the companion check-scoping fix flashinfer-ai/flashinfer#4947 — both ride the same post-flashinfer#4802 release boundary, so no additional wait.\n\n## Relation to #53969\n\n#53969 made this model servable before the native GLM53_NOPE kernel existed, by zero",
      "labels": [
        "bug",
        "nvidia",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55346",
      "repository": "vllm-project/vllm",
      "number": 55346,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][ROCm] Saturate bf16->fp16 conversion in QuickReduce",
      "bodyExcerpt": "## Purpose\n\nFixes #55345.\n\nQuickReduce packs bf16 inputs into fp16 before the codec runs\n(`quick_reduce_impl.cuh`, the `cast_bf2half` branch). bf16 covers the fp32\nexponent range; fp16 stops at 65504. `__float22half2_rn` does not saturate, so\nany activation above that becomes `inf` and propagates through the reduction,\npoisoning the whole line — silently, with no warning and no numerical check.\n\nThe conversion sits *ahead* of the codec, so this affects every quantization\nmode (INT4/INT6/INT8 **and** FP), not only the quantized ones.\n\n## Test Plan\n\nTwo ranks on MI355X (gfx950), INT8 codec, one out-of-fp16-range value on rank 0\nand 1.0 everywhere else:\n\n```python\nx = torch.full((n,), 1.0, dtype=torch.bfloat16, device=dev)\nif rank == 0:\n    x[0] = 1.0e30           # representable in bf16, not in fp16\nout = qr.quick_all_reduce(x)\nprint(out[0].float().item(), torch.isfinite(out).all().item())\n```\n\nSame container image in both runs, only the built extension differs, so the\ndifference is attributable to this change and nothing else.\n\n## Test Result\n\n```\nbefore: out[0] = inf     torch.isfinite(out).all() = False\nafter:  out[0] = 65536   torch.isfinite(out).all() = True\n```\n\nOther elements unchanged at 2.0 in both runs.\n\nEnd-to-end on GLM-5.3 (TP4, MXFP4 + fp8 attention, 8192-token chunks) the clamp\nis not measurable against run-to-run noise — the surrounding loop is\nbandwidth-bound and the two `fminf`/`fmaxf` pairs ride along in registers.\n\n## Scope\n\nThis is a mitigation, not a complete fix, and I would rather state the limit\nthan oversell it: with N ranks each contributing a clamped 65504, the fp16\naccumulation can still saturate. What it removes is the case where a single\nout-of-range value destroys the entire reduction. Keeping bf16 end to end for\nbf16 inputs would be the complete answer, but that is a larger change with its\nown cost, and worth deciding separately.",
      "labels": [
        "bug",
        "rocm"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-55347",
      "repository": "vllm-project/vllm",
      "number": 55347,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][ROCm] Keep the AITER allreduce compile range within AITER's runtime cutoff",
      "bodyExcerpt": "## Purpose\n\nFixes #55344.\n\n`vllm/config/vllm.py` derives the compile range of the fused allreduce+RMSNorm\npass from `AiterCustomAllreduce.effective_max_size()`, which only looks at\n`MAX_SIZE`. AITER's runtime eligibility check is `min(_car_max_size, max_size /\n2)`, where `_car_max_size` comes from `AITER_CUSTOM_AR_MAX_SIZE` and defaults to\na fixed 64 MiB that does not grow with the registered pool.\n\nWhen the two disagree, the pass is compiled for message sizes AITER rejects.\n`custom_fused_ar_rms()` then returns `None`, and\n`_rocm_aiter_fused_allreduce_rmsnorm_impl` asserts on it — the server fails to\nstart with a bare `AssertionError` instead of falling back to RCCL. An env var\nmeant to *narrow* custom-AR usage should not prevent startup.\n\nThis makes `effective_max_size()` return what AITER will actually accept, so the\npass is never compiled for sizes that get rejected.\n\n## Test Plan\n\nMI355X (gfx950), TP4, GLM-5.3 (`hidden_size=6144`, bf16),\n`--max-num-batched-tokens 8192`, AITER custom AR and `fuse_allreduce_rms`\nenabled. Same image, same model, same flags in both runs — only the env var and\nthis patch differ:\n\n```\nAITER_CUSTOM_AR_MAX_SIZE=1048576 vllm serve <model> -tp 4 --max-num-batched-tokens 8192\n```\n\n## Test Result\n\nBefore (unmodified tree):\n\n```\ncompile_ranges_endpoints: [5461, 8192]\nERROR _aiter_ops.py:956 in _rocm_aiter_fused_allreduce_rmsnorm_impl\n      assert result is not None\nAssertionError\nRuntimeError: Engine core initialization failed.\n-> died after 160 s\n```\n\nAfter:\n\n```\ncompile_ranges_endpoints: [85, 8192]      # 1 MiB / (6144 * 2) = 85 tokens\n-> serving after 340 s, zero AssertionErrors\nGSM8K 49/50 = 98.0 %\n```\n\nThe range now matches the cutoff exactly, which is the intended behavior: with a\n1 MiB cutoff the fusion is simply out of scope for realistic batch sizes, and\nthose fall back to RCCL as they should.\n\nNo change with the default (`AITER_CUSTOM_AR_MAX_SIZE` unset): `min(MAX_SIZE //\n2, 64 MiB)` is `min(64 MiB, 64 MiB)`, i.e. the previous valu",
      "labels": [
        "bug",
        "rocm",
        "torch.compile"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-55348",
      "repository": "vllm-project/vllm",
      "number": 55348,
      "type": "pr",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[Perf][Feat] GLM5.2/5.3 de-JITification",
      "bodyExcerpt": "",
      "labels": [
        "deepseek",
        "mrv2",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55358",
      "repository": "vllm-project/vllm",
      "number": 55358,
      "type": "pr",
      "category": "OTHER",
      "categoryValid": true,
      "ruleCategory": "OTHER",
      "classificationConsistent": true,
      "title": "[Refactor][GLM-5.3-Flash] Move sparse_attn_indexer_kpool into the model folder and split AMD/NVIDIA",
      "bodyExcerpt": "## What\r\n\r\nMoves `sparse_attn_indexer_kpool` out of `vllm/model_executor/layers/` and into the glm5next model folder, split by hardware backend.\r\n\r\n## Test\r\n\r\ngsm8k 0.9371\r\naime25 pass@4=0.9333\r\n\r\n\r\n## AI assistance\r\n\r\nThis change was implemented with AI assistance (Kimi Code CLI). Opened as a draft so the human submitter can review every changed line before marking it ready.",
      "labels": [
        "rocm",
        "ready",
        "nvidia",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55367",
      "repository": "vllm-project/vllm",
      "number": 55367,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix] Detect NVFP4 in compressed-tensors mixed-precision checkpoints",
      "bodyExcerpt": "## Purpose\n\n`ModelConfig.is_nvfp4_quantized()` detects NVFP4 by looking for `\"nvfp4\"` in the quantization config's **top-level** `format` string. Compressed-tensors **mixed-precision** checkpoints declare per-group schemes instead — the top-level format is `\"mixed-precision\"` and never names nvfp4 even when groups plainly are.\n\nConcrete case: `unsloth/Qwen3.8-27B-NVFP4` (`format: \"mixed-precision\"`, `config_groups.group_1` = W4A4 float, `tensor_group`, `group_size 16`, per-group `format: \"nvfp4-pack-quantized\"`, targeting `mlp.(gate|up|down)_proj` — 56 of 64 MLP layers). The check returns False → `enable_act_fusion()` returns False → `pass_config.fuse_act_quant` stays False at **every** optimization level → `ActivationQuantFusionPass` never runs → the fused CUDA op `silu_and_mul_nvfp4_quant` is unreachable (it is referenced only from that pass) and Inductor handles silu+mul+fp4-quant — which `enable_act_fusion`'s own docstring says it cannot: *\"FP4 quant is always custom so Inductor cannot fuse it.\"*\n\nFix: when the top-level format is mixed, inspect `config_groups`. A group counts as NVFP4 if its per-group `format` names nvfp4, **or** it matches the NVFP4 scheme exactly (W4A4 float + `tensor_group` + `group_size 16` + symmetric), mirroring `CompressedTensorsConfig._is_nvfp4_format`. The strictness is load-bearing: **MXFP4 is also W4A4 float** (strategy `group`, group_size 32) — e.g. `INCModel3/GLM-5.3-Flash-MXFP4-Mixed-CT-AutoRound` — and must not be misclassified. Non-dict (preset-name) groups are guarded; non-mixed formats never consult groups, so e.g. pure `mxfp4-pack-quantized` behavior is unchanged.\n\nDesign note, pre-flagged: a mixed-precision model is only *partly* NVFP4, so a model-level boolean is arguably the wrong shape for this question; if maintainers prefer keying off resolved per-layer schemes, I'm glad to rework — this PR is the minimal fix at the existing seam (single call site: `enable_act_fusion`).\n\n## Test Plan\n\n- `pytest tests/config/test_nvfp4_d",
      "labels": [
        "bug"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-55378",
      "repository": "vllm-project/vllm",
      "number": 55378,
      "type": "pr",
      "category": "OTHER",
      "categoryValid": true,
      "ruleCategory": "OTHER",
      "classificationConsistent": true,
      "title": "[ROCm] Hold the router weight in fp32 when fp32 routing is requested",
      "bodyExcerpt": "## Purpose\n\nFixes #55351.\n\n`_get_moe_router_dtype()` returns `torch.float32` unconditionally for\n`glm_moe_dsa`, and GLM-5.3's own config carries `\"moe_router_dtype\": \"float32\"`.\nBut `GateLinear` is constructed without `params_dtype`, so only the *output* is\nfp32 — the weight stays in the model dtype (bf16).\n\n`GateLinear` gates every fp32 router GEMM on `self.weight.dtype ==\ntorch.float32`, so those kernels are unreachable for DeepSeek-family models. That\nincludes the gfx950 kernel added in #54845, whose\n`ROCM_FP32_ROUTER_GEMM_SUPPORTED_SHAPES` explicitly lists `(6144, 256)` — exactly\nGLM-5.3's router shape. A log line at construction time:\n\n```\ngfx950=True, shape (6144, 256) supported=True, weight=torch.bfloat16\n  -> fp32 path OFF\n```\n\nvLLM already has the mechanism for this. `force_fp32_compute` stores the weight\nin fp32 *only* when the platform has no specialized kernel expecting the model\ndtype, so it is a no-op wherever such a kernel exists. `nemotron_h.py` uses it;\nthe DeepSeek path did not.\n\n## Test Plan\n\nMI355X (gfx950), TP4, GLM-5.3 (MXFP4 experts + fp8 attention),\n`--max-model-len 1048576`, MTP k=3. Same image, same flags, only this line\ndiffering.\n\n## Test Result\n\nThe path is reached:\n\n```\ngfx950=True, shape (6144, 256) supported=True, weight=torch.float32\n  -> fp32 path ON\n```\n\n| | path off | path on |\n|---|---|---|\n| single stream (median of 4) | 171.3 tok/s | 175.6 |\n| throughput, 64 concurrent | 2005 / 2219 tok/s | 1969 / 2198 |\n| GSM8K (150) | 98.0 % | 98.7 % |\n| GPU KV cache | — | 2,843,584 tokens |\n\nI would rather be straight about this: **on my setup it is noise in both\ndirections**, and it costs ~230 MB per rank for the fp32 copies of the router\nweights. So I am not claiming a speedup.\n\nWhat the patch fixes is the inconsistency: a model that declares fp32 routing\ngets an fp32 output from a bf16 weight, and the kernels written for that case\nnever run. Platforms where the specialized fp32 kernel is a larger win over the\ngeneric fallback should benef",
      "labels": [
        "rocm",
        "deepseek"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-55385",
      "repository": "vllm-project/vllm",
      "number": 55385,
      "type": "pr",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[perf] wire FA and FlashMLA for sm90 GLM5Next NoPE SparseMLA",
      "bodyExcerpt": "GLM-5.3-Flash - Sparse MLA Attention Backend Comparison (NoPE, head_size=512)\r\n\r\n## Setup\r\n\r\n- Model: zai-org/GLM-5.3-Flash (bf16, ~306 GB weights; NoPE sparse MLA: kv_lora_rank=512, qk_rope_head_dim=0, index_topk=2048, index_kpool=4, bf16 KV cache)\r\n\r\n- Hardware: 4× NVIDIA H200 143GB (SM90), TP=4\r\n\r\n- Backends: FlashInfer SM90 (current default), FlashMLA (https://github.com/vllm-project/FlashMLA/pull/17), FA3 (https://github.com/vllm-project/flash-attention/pull/172)\r\n\r\n- Tool: vllm bench serve (random dataset, --ignore-eos); all requests succeeded (96/96, 384/384)\r\n\r\n- Note: both workloads have prefill lengths > topk (2048), so prefill and decode both exercise each backend's sparse-MQA kernel this is a head-to-head of the sparse attention paths.\r\n\r\nWorkload 1: long context - 32k input / 512 output, concurrency 32 (96 requests)\r\n\r\n<table border=\"1\"><tr><td>Backend</td><td>Output tok/s</td><td>Δ vs FI</td><td>Total tok/s</td><td>TTFT mean(ms)</td><td>TTFT p99(ms)</td><td>TPOT mean(ms)</td><td>TPOT p99(ms)</td><td>ITL p99(ms)</td></tr><tr><td>FlashInfer SM90</td><td>289.6</td><td>-</td><td>18,827</td><td>10,660</td><td>44,250</td><td>89.3</td><td>127.1</td><td>323.8</td></tr><tr><td>FlashMLA</td><td>336.5</td><td>+16.2%</td><td>21,871</td><td>9,613</td><td>40,427</td><td>75.9</td><td>90.5</td><td>318.6</td></tr><tr><td>FA3</td><td>341.1</td><td>+17.8%</td><td>22,173</td><td>9,602</td><td>39,949</td><td>74.7</td><td>89.2</td><td>316.9</td></tr></table>\r\n\r\n- TPOT mean: -15.0% (FlashMLA) / -16.3% (FA3); TPOT p99: -29% (127.1 -> ~90)\r\n\r\n- Wall-clock for the full batch: 169.7s -> 146.1s (FlashMLA) / 144.1s (FA3)\r\n\r\nWorkload 2: mid context, high concurrency - 4k input / 1k output, concurrency 128 (384 requests)\r\n\r\n<table border=\"1\"><tr><td>Backend</td><td>Output tok/s</td><td>Δ vs FI</td><td>Req/s</td><td>TTFT mean(ms)</td><td>TTFT p99(ms)</td><td>TPOT mean(ms)</td><td>TPOT p99(ms)</td></tr><tr><td>FlashInfer SM90</td><td>2,034</td><td>-</td><td>2.0</td><td>7,867</td><td>2",
      "labels": [
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 8,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "TRAINING",
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55389",
      "repository": "vllm-project/vllm",
      "number": 55389,
      "type": "pr",
      "category": "OTHER",
      "categoryValid": true,
      "ruleCategory": "OTHER",
      "classificationConsistent": true,
      "title": "[Model] Extend device-side mm normalization to GLM4V/GLM5Next",
      "bodyExcerpt": "\r\n\r\n\r\n## Purpose\r\n\r\nExtend the device-side multimodal input normalization (#50411) to GLM-4.1V / GLM-4.5V / GLM-4.6V / GLM-5.3-Flash model family, following the same patterns as in #51289 and #55370.\r\n\r\nE2E and accuracy benchmark will be added following that of the original PR.\r\n\r\n## Test Plan\r\n\r\n1. E2E benchmark using vllm bench serve --dataset-name random-mm\r\n2. accuracy test using vql_ppl_test\r\n\r\n## Test Result\r\n\r\nTBA\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [x] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [x] The test plan, such as providing test command.\r\n- [x] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [x] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n",
      "labels": [
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 7,
      "matchedTerms": [
        "GLM",
        "GLM-4",
        "GLM4",
        "GLM-4.5"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55398",
      "repository": "vllm-project/vllm",
      "number": 55398,
      "type": "pr",
      "category": "DOCS",
      "categoryValid": true,
      "ruleCategory": "DOCS",
      "classificationConsistent": true,
      "title": "[5/N] HiSparse: support direct GPU landing for P/D transfers",
      "bodyExcerpt": "## Why\n\nHiSparse P/D should receive directly into final GPU-resident pages when the decoder has capacity, while retaining host landing when it does not. This keeps the decision per request and avoids forcing every imported prefix through host memory.\n\nThis does not duplicate an existing PR; no open adaptive HiSparse P/D landing PR was found.\n\n## Status\n\nThis is a draft with an intermediate NIXL host-registration diagnostic committed. The direct/host P/D smoke is still being completed.\n\n## Tests\n\n- `python -m pytest tests/v1/kv_connector/unit/test_nixl_connector_hma.py::test_nixl_keeps_device_block_count_with_hisparse_host_pool tests/v1/kv_connector/unit/test_nixl_connector_hma.py::test_hisparse_host_import_keeps_host_blocks_out_of_gpu_regions -q` (2 passed)\n- GLM P/D harness startup exercised on 8 B300 GPUs; currently exposes the remaining host DRAM registration ownership issue before request transfer.\n\nAI assistance was used. Every changed line and final GPU validation must be reviewed by the human submitter before this PR is marked ready.",
      "labels": [
        "documentation",
        "speculative-decoding",
        "torch.compile",
        "ci/build",
        "deepseek",
        "cpu",
        "kv-connector",
        "nvidia",
        "mrv2",
        "dflash",
        "scheduler",
        "kv-cache-manager",
        "glm",
        "minimax"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-55408",
      "repository": "vllm-project/vllm",
      "number": 55408,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix] Pass CPU sequence lengths to TRT-LLM ragged prefill",
      "bodyExcerpt": "## Purpose\n\nThe B200 LM Eval PCP lane fails deterministically for both GLM-5.2 configs when the DeepSeek-v3.2 sparse-MLA prefill path calls FlashInfer's `trtllm_ragged_attention_deepseek`. Without trusted host sequence lengths, FlashInfer derives them from CUDA indptrs and evaluates `.any().item()`, which vLLM correctly rejects as a GPU-to-CPU synchronization.\n\nThis change:\n\n- preserves the already-computed CPU query lengths in `MLACommonPrefillMetadata`;\n- passes trusted CPU query/KV lengths to FlashInfer for new-token prefill;\n- passes the corresponding host query/context lengths for context chunks.\n\nThis avoids the synchronization without disabling vLLM's sync guard or reverting PCP. It addresses the current synchronization failure in issue #49810; it does not claim to resolve every PCP issue tracked there.\n\nDuplicate-work checks found no open PR referencing #49810 and no open PR matching the TRT-LLM ragged/CPU-sequence-length fix. The older PR #49196 is a much broader PCP revert and is not equivalent.\n\nAI assistance was used to investigate the failure, prepare the code and tests, and draft this description. The human submitter reviewed every changed line and confirmed running the relevant tests before PR submission.\n\n## Test Plan\n\n```bash\n.venv/bin/python -m pytest tests/v1/attention/test_mla_backends.py -q\n.venv/bin/python -m pytest \\\n  tests/v1/attention/test_mla_context_chunks.py \\\n  tests/v1/attention/test_mla_prefill_registry.py -q\n.venv/bin/pre-commit run --files \\\n  tests/v1/attention/test_mla_backends.py \\\n  vllm/model_executor/layers/attention/mla_attention.py \\\n  vllm/v1/attention/backends/mla/prefill/trtllm_ragged.py\ngit diff --check origin/main...HEAD\n```\n\nPost-fix model evaluation requires B200 hardware and was not available locally. Request the exact `B200: LM Eval (PCP)` lane on this PR; the fix should not be considered proven until both GLM-5.2 PCP configs complete successfully.\n\n## Test Result\n\n- `test_mla_backends.py`: **42 passed, 292 skipped*",
      "labels": [
        "bug",
        "nvidia"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-55423",
      "repository": "vllm-project/vllm",
      "number": 55423,
      "type": "pr",
      "category": "NEW_SUPPORT",
      "categoryValid": true,
      "ruleCategory": "NEW_SUPPORT",
      "classificationConsistent": true,
      "title": "[Spec Decode] Enable GLM-5.3-Flash DFlash2 on NVIDIA",
      "bodyExcerpt": "## Purpose\n\nDraft for applying and testing the complete local e2e enablement patch. This is\nnot a claim of merge readiness or full model correctness certification.\n\nEnable the existing DFlash2 implementation to use\n`zai-org/GLM-5.3-Flash` as its target on NVIDIA GPUs, with\n`incoai/GLM-5.3-Flash-DFlash2` as the seven-token drafter.\nThis does not introduce a new DFlash2 implementation.\n\nThe patch has two parts:\n\n1. Expose the EAGLE3 auxiliary-hidden-state interface on the GLM text model and\n   multimodal wrapper. Materialize completed mHC layer outputs for the drafter\n   without modifying the target's deferred post/pre state, and gather selected\n   activations when sequence parallelism is enabled. Do not add the residual\n   twice for ordinary layers.\n2. Extend GLM's specialized MLA/indexer/Mamba KV-cache layout to include the\n   external drafter's sliding-window groups. Align their logical block span with\n   the target before padding physical pages, preserve independent block tables,\n   and account for draft groups in shared-slot allocation and capacity checks.\n   New draft groups honor the existing `use_eagle_block_drop()` setting.\n\nNo CUDA/C++ kernels, global speculative-cache defaults, or native MTP settings\nare changed. The tested DFlash2 configuration explicitly sets\n`disable_eagle_block_drop=true`.\n\n### Related work / duplicate checks\n\nOpen PR and issue searches on 2026-09-05 found no open PR implementing this\nGLM-specific combination of target hidden-state support and draft cache layout.\nSearches included `DFlash2`, `glm5next`, `glm5_next`, `GLM DFlash`, and issue\nreferences; issue comments and relevant PR file lists were inspected.\n\n- Related: [#54451](https://github.com/vllm-project/vllm/issues/54451) reports the\n  missing target interface on ROCm. This change targets the NVIDIA implementation\n  and does **not** claim to fix the ROCm backend failure reported there.\n- [#54163](https://github.com/vllm-project/vllm/pull/54163) changes the general\n  DFlash/DSpark",
      "labels": [
        "nvidia",
        "dflash",
        "kv-cache-manager",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    }
  ]
}
