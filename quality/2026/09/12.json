{
  "schemaVersion": 1,
  "scope": "daily-incremental",
  "date": "2026-09-12",
  "generatedAt": "2026-09-12T04:51:51.112Z",
  "seed": "2026-09-12",
  "samplePerStratum": 2,
  "summary": {
    "population": 53,
    "sampled": 53,
    "strata": 7,
    "structuralFailures": 0,
    "sourceMismatches": 0,
    "sourceChecksSkipped": 0,
    "classificationDrifts": 0,
    "relevanceFailures": 0,
    "reviewWarnings": 32
  },
  "items": [
    {
      "id": "mindspeed-llm-pr-5039",
      "repository": "Ascend/MindSpeed-LLM",
      "number": 5039,
      "type": "pr",
      "category": "DOCS",
      "categoryValid": true,
      "ruleCategory": "DOCS",
      "classificationConsistent": true,
      "title": "refactor: Q3 Model Sunset Plan",
      "bodyExcerpt": "Refs #1813\n\n## What this PR does / why we need it?\n\n1. Retire the model examples and PoC scripts listed in phase four of the sunset plan across mcore, FSDP2, and MindSpore: LLaMA2-13B, Qwen2.5-72B, Qwen3 Dense (0.6B/1.7B/4B/8B/14B/32B), Gemma2, Phi3.5 mini/MoE, Magistral-24B, PLM-1.8B, Qwen3-Next, Qwen3-Coder-Next, DeepSeek-V2-Lite, Ling-mini-2.0, Ring-1T, GLM4.5-Air, Step3.5-Flash, GPT-OSS-20B, MiniMax-M2.7, and Mamba3. Remove the two Qwen3 MoE DPO scripts explicitly listed in the issue.\n2. Remove 231 files under `examples` and `tests/poc`: 209 shell scripts, 17 accompanying YAML files, and 5 obsolete example READMEs. Preserve model sizes outside the sunset plans, regular Qwen3 MoE workflows, Ling-flash-100B examples, and shared LLaMA2/Qwen2.5 conversion helpers.\n3. Mark the final supported version as `26.1.0` for the retired models in the Chinese and English PyTorch model tables, following the archival pattern in !4571. Update documentation links to the retired scripts, configurations, and example directories to point to `26.1.0`. The deleted Qwen3 README did not exist in that release, so its reference points to the historical Qwen3 example directory instead.\n4. Clean up 11 remaining PoC scripts for models retired in earlier sunset plans (#943, #1224, and #1436): LLaMA2-70B, LLaMA3.1-70B, LLaMA3/3.1-405B, Mixtral-8x7B, and Qwen2-57B-A14B. Remove their directories and the three empty PoC directories left by phase four.\n5. Keep the umbrella sunset issue open with `Refs #1813`; this PR covers scripts and their documentation, without removing model implementations.\n\n## Does this PR introduce any user-facing change?\n\nYes. The retired examples and PoC scripts are no longer available on master. Their documentation links now lead to version `26.1.0`, and the model support tables explicitly identify that final supported version. Historical commands should be run with the corresponding release.\n\n## How was this patch tested?\n\n1. `git diff --cached --check`: passed. Verified",
      "labels": [
        "ascend-cla/yes",
        "docs-ci-pipeline-success"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM",
        "GLM4"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "verl-pr-7483",
      "repository": "volcengine/verl",
      "number": 7483,
      "type": "pr",
      "category": "NEW_SUPPORT",
      "categoryValid": true,
      "ruleCategory": "NEW_SUPPORT",
      "classificationConsistent": true,
      "title": "[vllm] feat: Support LoRA for DeepSeek V4 & GLM5.3 Flash",
      "bodyExcerpt": "### What does this PR do?\r\n\r\nDepends on:\r\n- https://github.com/vllm-project/vllm/pull/51368\r\n- https://github.com/vllm-project/vllm/pull/52552\r\n- https://github.com/vllm-project/vllm/pull/52626\r\n- https://github.com/vllm-project/vllm/pull/53361\r\n- https://github.com/vllm-project/vllm/pull/56327\r\n- https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/6044\r\n- https://github.com/NVIDIA/Megatron-LM/pull/7054\r\n\r\nAdds DeepSeek V4 (DSV4) LoRA support to verl's vLLM weight-sync path. Previously a merge=False LoRA base sync orphaned the `.base_layer.` suffix on wrapped modules and mishandled the mxfp4 quant path, so DSV4 LoRA diverged from the actor.\r\n\r\nThis reworks weight-name reconciliation to be runtime-probed (strict vs flat loader) instead of model-type-based, unwraps the LoRA-installed mxfp4 quant-method wrapper, and threads `peft_config` through the FP8/mxfp4 load path so the same reconciliation applies there.\r\n\r\nTested with Megatron backend, GPU E2E (DSV4 merge=F GRPO, 2-node): step1/step2 pearson 0.9970/0.9950, aligned to run15 merge=T 0.9952/0.9944.\r\n\r\n### Checklist Before Starting\r\n\r\n- [X] Search for similar PRs. Query: https://github.com/verl-project/verl/pulls?q=deepseek+v4+lora\r\n- [X] Format the PR title as `[{modules}] {type}: {description}` (This will be checked by the CI)\r\n  - `{modules}` include `fsdp`, `megatron`, `veomni`, `sglang`, `vllm`, `rollout`, `trainer`, `ci`, `training_utils`, `recipe`, `hardware`, `deployment`, `ray`, `worker`, `single_controller`, `misc`, `perf`, `model`, `algo`, `env`, `tool`, `ckpt`, `doc`, `data`, `cfg`, `reward`, `fully_async`, `one_step_off`\r\n  - If this PR involves multiple modules, separate them with `,` like `[megatron, fsdp, doc]`\r\n  - `{type}` is in `feat`, `fix`, `refactor`, `chore`, `test`\r\n  - If this PR breaks any API (CLI arguments, config, function signature, etc.), add `[BREAKING]` to the beginning of the title.\r\n  - Example: `[BREAKING][fsdp, megatron] feat: dynamic batching`\r\n\r\n### Test\r\n\r\n```bash\r\npytest test",
      "labels": [],
      "filterDisposition": "eligible",
      "relevanceScore": 8,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "TRAINING",
        "RL"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "verl-pr-7775",
      "repository": "volcengine/verl",
      "number": 7775,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[vllm, hardware] fix: patch vllm-ascend for GLM-5.2 RL training on Ascend NPUs",
      "bodyExcerpt": "### What does this PR do?\r\n\r\nAdd an opt-in compatibility patch enabled by `VERL_VLLM_ASCEND_GLM52_PATCH=1` to support GLM-5.2 RL training with vllm-ascend.\r\n\r\n- Use explicit residual addition before RMS normalization.\r\n- Preserve SFA kv_b_proj weights for subsequent weight updates.\r\n- Apply the missing SFA indexer weight scaling.\r\n- Synchronize pending NPU work before CaMem sleep unmaps memory.\r\n- Load the routed-experts capture patch.\r\n\r\nSkip fixes already present and reject unsupported patch targets. Remove this temporary patch once vllm-ascend incorporates these changes.\r\n\r\n### Checklist Before Starting\r\n\r\n- [x] Search for similar PRs. Paste at least one query link here: ...\r\n- [x] Format the PR title as `[{modules}] {type}: {description}` (This will be checked by the CI)\r\n  - `{modules}` include `fsdp`, `megatron`, `veomni`, `sglang`, `vllm`, `rollout`, `trainer`, `ci`, `training_utils`, `recipe`, `hardware`, `deployment`, `ray`, `worker`, `single_controller`, `misc`, `perf`, `model`, `algo`, `env`, `tool`, `ckpt`, `doc`, `data`, `cfg`, `reward`, `fully_async`, `one_step_off`\r\n  - If this PR involves multiple modules, separate them with `,` like `[megatron, fsdp, doc]`\r\n  - `{type}` is in `feat`, `fix`, `refactor`, `chore`, `test`\r\n  - If this PR breaks any API (CLI arguments, config, function signature, etc.), add `[BREAKING]` to the beginning of the title.\r\n  - Example: `[BREAKING][fsdp, megatron] feat: dynamic batching`\r\n\r\n### Test\r\n\r\nGLM5.2 RL training working on Ascend A3.\r\n",
      "labels": [],
      "filterDisposition": "eligible",
      "relevanceScore": 8,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "TRAINING",
        "RL"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-issue-54924",
      "repository": "vllm-project/vllm",
      "number": 54924,
      "type": "issue",
      "category": "BUG",
      "categoryValid": true,
      "ruleCategory": "BUG",
      "classificationConsistent": true,
      "title": "[Bug]: GLM-5.3 accuracy collapses on ROCm (GSM8K 91.6% -> 14.9%) after #53155 forces MRV1",
      "bodyExcerpt": "## Your current environment\n\n- vLLM: bisected across `44fe2a392..40824284b`, also reproduced on `main` @ `1b4b2a18b`\n- Platform: ROCm, AMD MI350/MI355 (`gfx950`), TP=4\n- Model: [`amd/GLM-5.3-Quark-MXFP4-AttnFP8`](https://huggingface.co/amd/GLM-5.3-Quark-MXFP4-AttnFP8) (`architectures: [\"GlmMoeDsaForCausalLM\"]`)\n- Env: `VLLM_ROCM_USE_AITER=1`, `VLLM_ROCM_USE_AITER_FP8BMM=0`, `VLLM_ROCM_USE_AITER_FP4BMM=0`\n\n## Describe the bug\n\n`ce2e343be` (\"[ROCm] Keep GLM-5.2 on MRV1 and disable default breakable cudagraph\", #53155)\nadded `GlmMoeDsaForCausalLM` to `ROCM_DEFAULT_MRV1_ARCHITECTURES`:\n\n```python\nROCM_DEFAULT_MRV1_ARCHITECTURES = frozenset(\n    {\"DeepseekV32ForCausalLM\", \"DeepseekV4ForCausalLM\", \"GlmMoeDsaForCausalLM\"}\n)\n```\n\nSince GLM-5.2 and GLM-5.3 share the same architecture string, this also switches\n**GLM-5.3** from MRV2 to MRV1 on ROCm. MRV1 produces corrupted generations for this\narchitecture, and GSM8K 5-shot accuracy drops from **91.6% to 14.9%**.\n\nThe offending entry is still present on `main` (`1b4b2a18b`).\n\n## Reproduction\n\n```bash\nMODEL=amd/GLM-5.3-Quark-MXFP4-AttnFP8\n\nVLLM_ROCM_USE_AITER=1 \\\nVLLM_ROCM_USE_AITER_FP8BMM=0 \\\nVLLM_ROCM_USE_AITER_FP4BMM=0 \\\npython -m lm_eval --model vllm \\\n  --model_args \"pretrained=$MODEL,tensor_parallel_size=4,dtype=auto,\\\nquantization=quark,max_model_len=32768,trust_remote_code=True,max_num_seqs=32\" \\\n  --tasks gsm8k \\\n  --batch_size auto\n```\n\nAdding `VLLM_USE_V2_MODEL_RUNNER=1` to the exact same command restores accuracy.\n\n## Results\n\n| Build | flexible-extract | strict-match | Runner |\n| --- | --- | --- | --- |\n| `40824284b` (default) | 0.1486 | 0.1418 | MRV1 |\n| `40824284b` + `VLLM_USE_V2_MODEL_RUNNER=1` | **0.9158** | **0.9143** | MRV2 |\n| `44fe2a392` (before the regression) | 0.9158 | 0.9151 | MRV2 |\n| model card reference | 0.9128 | - | - |\n\nThe model card's own reproduction instructions pin the image to\n`vllm/vllm-openai-rocm:nightly-44fe2a392b71d52a8d72faf2f8278834379482c9` with\n#54556 applied, i.e. exactly the thir",
      "labels": [
        "rocm",
        "quantization",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 8,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "TRAINING",
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-issue-56428",
      "repository": "vllm-project/vllm",
      "number": 56428,
      "type": "issue",
      "category": "BUG",
      "categoryValid": true,
      "ruleCategory": "BUG",
      "classificationConsistent": true,
      "title": "[Bug]: Reasoning still returned in /responses while include_reasoning is set to false",
      "bodyExcerpt": "### Your current environment\n\nAny\n\n### 🐛 Describe the bug\n\n```\ncurl -X POST \"$LLM_ENDPOINT/responses\" \\\n  -H \"Authorization: Bearer $LLM_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"model\": \"glm\",\n    \"input\": \"hi\",\n    \"reasoning\": {\n      \"effort\": \"max\"\n    },\n    \"include_reasoning\": false\n  }'\n\n```\n\ninclude reasoning must strip reasoning tokens yet it doesnt in /responses, in /chat/completions and /completions it does\n```\n    \"reasoning\": {\n      \"effort\": \"none\"\n    },\n    \"include_reasoning\": false\n```\ndoesnt work either, in models like glm 5.3+ that come with no switch to turn thinking off it will merge the reasoning into text content.\n\ninclude_reasoning must support /responses too \n\n### Before submitting a new issue...\n\n- [x] Make sure you already searched for relevant issues, and asked the chatbot living at the bottom right corner of the [documentation page](https://docs.vllm.ai/en/latest/), which can answer lots of frequently asked questions.",
      "labels": [
        "bug",
        "tool-calling"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-issue-56470",
      "repository": "vllm-project/vllm",
      "number": 56470,
      "type": "issue",
      "category": "BUG",
      "categoryValid": true,
      "ruleCategory": "BUG",
      "classificationConsistent": true,
      "title": "[Bug] MTP speculative decoding crashes with PP>1 due to missing record_stream on idx_mapping",
      "bodyExcerpt": "# Bug: MTP speculative decoding crashes with PP>1 due to missing record_stream on idx_mapping\n\n## Environment\n- Model: GLM-5.3-SplitQuant (MTP enabled)\n- Config: TP=2, PP=3\n- Hardware: A100 (SM80)\n- vLLM: rebased tree (commit bf33e54548 contains the fix)\n\n## Problem\nMTP speculative decoding crashes with `EngineDeadError` / `illegal memory access` when running with pipeline parallelism (PP>1). The crash occurs in the broadcast-drafts path on the last PP rank.\n\n## Root Cause\nIn `vllm/v1/worker/gpu/pp_utils.py`, the `broadcast_drafts` function indexes `draft_tokens` by `input_batch.idx_mapping` — a per-step async_copy_to_gpu allocation — inside the `broadcast_stream` context. The gather consumes the tensor on a side stream, but its block was never `record_stream`'d, so the caching allocator is free to reuse it for a later allocation (e.g., the next step's H2D payload) while the read is still in flight.\n\nThe receiver path (`receive()`) already `record_stream`s the same class of tensors; the sender-side broadcast never did.\n\n## Crash Signature\n```\nIndexing.cu: indexSelectSmallIndex (inside the gather that consumes idx_mapping)\n```\n\nThe corruption victim is `idx_mapping` itself, consumed on the side stream while the caching allocator has already reallocated its block.\n\n## Reproducer\n```bash\n# MTP with PP>1, default config (VLLM_PP_BCAST_RECORD_STREAM=0)\nvllm serve <model> \\\n  --pipeline-parallel-size 3 \\\n  --tensor-parallel-size 2 \\\n  --speculative-config '{\"method\": \"mtp\", \"num_speculative_tokens\": 1}'\n\n# Under load (100K tokens, 16 concurrent streams)\n# Crash occurs ~90s into the drive\n```\n\n## Validation\nA/B test with discriminator window (2026-09-10):\n- **D1 (with fix)**: `VLLM_PP_BCAST_RECORD_STREAM=1` → Survived 100K-token, 16-stream drive (222 reqs, 708s, 151 tok/s, zero errors, MTP_LTI_MON 34,825 steps oob=0 ihit=0 rhit=0)\n- **D0 (control)**: All flags off → Crashed ~90s into identical drive (EngineDeadError, cudacoredump captured)\n\n## Fix\nAdd `record_stream` call ",
      "labels": [
        "speculative-decoding"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-issue-56563",
      "repository": "vllm-project/vllm",
      "number": 56563,
      "type": "issue",
      "category": "BUG",
      "categoryValid": true,
      "ruleCategory": "BUG",
      "classificationConsistent": true,
      "title": "[Bug]: FLASH_ATTN_MLA_SPARSE emits 'Failed to initialize the TMA descriptor 1' every step for GLM-5.3-Flash (zero-extent descriptors; output correct)",
      "bodyExcerpt": "### Your current environment\n\n<details>\n<summary>vllm collect-env (abridged)</summary>\n\n```\nOS                           : Ubuntu 24.04.3 LTS (x86_64)\nPyTorch version              : 2.13.0+cu130\nCUDA runtime version         : 13.0.88\nGPU                          : 8x NVIDIA H100 80GB HBM3 (single node, TP=8)\nNvidia driver version        : 595.71.05\nvLLM Version                 : 0.28.1rc1.dev580+g385dce36b (git sha: 385dce36b)\nDocker image                 : vllm/vllm-openai:glm53-flash @ sha256:819ec9c063412e5730d1b0e82046ba540d1bf991f3c4f661a849aae8a0c52374\n[pip3] flashinfer-python==0.6.18\n[pip3] torch==2.13.0+cu130\n[pip3] triton==3.7.1\n```\n</details>\n\n### 🐛 Describe the bug\n\n**Summary:** With `zai-org/GLM-5.3-Flash` and `--attention-backend FLASH_ATTN_MLA_SPARSE` on H100 (TP=8, MTP speculative decoding), every forward step emits ~88 copies of\n\n```\nError: Failed to initialize the TMA descriptor 1\n```\n\nfrom the FA3 sparse-MLA kernel (`vllm/vllm_flash_attn/_vllm_fa3_C.abi3.so`). The dumped descriptors all have `gmem_address 0` and a zero leading extent, i.e. the kernel is asked to build TMA descriptors for **empty (zero-token) segments**. CUDA rejects them with `CUDA_ERROR_INVALID_VALUE` (1). Generation output is correct (verified on short prompts, a 10k-token needle-count prompt, and greedy essays), so the failure is on an empty segment with nothing to compute — but it is emitted on every step in both prefill and decode, first appears during CUDA-graph capture, and produces roughly 2 GB/hour of container log at light single-user load.\n\n**Count matches layers x ranks:** 854 messages for one 60-token completion (~11 MTP steps) and 283 for a `max_tokens=1` request => ~80-90 per step = 11 sparse-MLA layers x 8 TP ranks. This looks like the same class of bug as the FlashMLA sparse \"phantom / zero-query-length chunk\" reports (#49922, #51486, #52339; partial fix in #51489), except FA3 prints and continues instead of asserting.\n\n**Descriptor dumps (representative):**\n\n```\n",
      "labels": [
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 7,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-issue-56564",
      "repository": "vllm-project/vllm",
      "number": 56564,
      "type": "issue",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[Performance]: GLM-5.3-Flash on H100: auto-selected FLASHINFER_MLA_SPARSE_SM90 is 36-70% slower than FLASH_ATTN_MLA_SPARSE",
      "bodyExcerpt": "### Your current environment\n\n<details>\n<summary>vllm collect-env (abridged)</summary>\n\n```\nOS                           : Ubuntu 24.04.3 LTS (x86_64)\nPyTorch version              : 2.13.0+cu130\nCUDA runtime version         : 13.0.88\nGPU                          : 8x NVIDIA H100 80GB HBM3 (single node, TP=8)\nNvidia driver version        : 595.71.05\nvLLM Version                 : 0.28.1rc1.dev580+g385dce36b (git sha: 385dce36b)\nDocker image                 : vllm/vllm-openai:glm53-flash @ sha256:819ec9c063412e5730d1b0e82046ba540d1bf991f3c4f661a849aae8a0c52374\n[pip3] flashinfer-python==0.6.18\n[pip3] torch==2.13.0+cu130\n[pip3] triton==3.7.1\n```\n</details>\n\n### 🐛 Describe the bug\n\n**Summary:** For `zai-org/GLM-5.3-Flash` on H100 the auto-selected sparse-MLA attention backend is `FLASHINFER_MLA_SPARSE_SM90` (log: `Using FLASHINFER_MLA_SPARSE_SM90 attention backend out of potential backends: ['FLASHINFER_MLA_SPARSE_SM90', 'FLASH_ATTN_MLA_SPARSE']`). It is **~36% slower at concurrency 1 and ~70% slower at concurrency 8** than the other candidate, `FLASH_ATTN_MLA_SPARSE`, measured with `vllm bench serve` on the same server flags. MTP draft acceptance is identical across the two, so the gap is per-step attention cost, not speculative-decoding behaviour. Forcing `--attention-backend FLASH_ATTN_MLA_SPARSE` recovers it.\n\n**How we found it:** we had been serving from an image built on 2026-08-26 from the `glm-release` branch (base `487ecf187`, FlashInfer 0.6.17). That build logs `Using FLASH_ATTN_MLA_SPARSE attention backend out of potential backends: ['FLASH_ATTN_MLA_SPARSE', 'FLASHMLA_SPARSE']`. After moving to the current `vllm/vllm-openai:glm53-flash` (nightly `385dce36b`, FlashInfer 0.6.18) with identical flags, the backend line changed to `FLASHINFER_MLA_SPARSE_SM90` and single-stream throughput dropped by roughly a third. The preference appears to come from #53906 (`031c899d` \"[Model] Use FlashInfer for GLM-5.3-Flash NoPE MLA\", `e34f7f71` \"[Kernel] Use FlashInfer rc10 fo",
      "labels": [
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 7,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-38476",
      "repository": "vllm-project/vllm",
      "number": 38476,
      "type": "pr",
      "category": "DOCS",
      "categoryValid": true,
      "ruleCategory": "DOCS",
      "classificationConsistent": true,
      "title": "[Feature] TRITON_MLA_SPARSE backend for SM8x/11x/12x DSA Sparse MLA Support",
      "bodyExcerpt": "## Purpose\r\n\r\nCloses #38006. Enables sparse MLA models (GLM-5, DeepSeek-V3.2) on SM80 (A100/A800) and SM121 (GB10/DGX Spark), where DeepGEMM / FlashMLA-Sparse / FlashInfer-MLA-Sparse are unavailable.\r\n\r\n## Changes\r\n\r\n1. **Dispatch guard.** `is_deep_gemm_supported()` (SM90+ check) replaces `has_deep_gemm()` in `sparse_attn_indexer.py` / `indexer.py`. Stops DeepGEMM kernels from being invoked on SM80/SM121.\r\n2. **Triton `fp8_mqa_logits` for the indexer.** `mqa_logits_triton.py` reproduces DeepGEMM's prefill + paged MQA logits. Prefill takes bf16 q/k (pre-decoded from FP8 in the Python wrapper) and feeds a straight `tl.dot`; paged decode keeps a 256-entry bf16 LUT for in-kernel FP8 decode. K-side scale applied to the fp32 dot output, per-row K-tile early-exit on the chunked-prefill path.\r\n3. **`TRITON_MLA_SPARSE` backend.** `triton_mla_sparse_kernel.py` adds a split-KV decode with N-way online-softmax merge plus a single-pass fast path. Autotune is warmed at init using indexer-derived `(n_head, head_dim)`. Masked-out sentinel is `-1e30` to avoid `NaN` from `(-inf) − (-inf)` on all-masked tiles.\r\n4. **Cudagraph support.** `TritonMLASparseMetadataBuilder` advertises `AttentionCGSupport.UNIFORM_BATCH`; flips A100 TP=8 back to `FULL_AND_PIECEWISE`.\r\n5. **MXFP4 link stubs.** `mxfp4_experts_quant` / `silu_and_mul_mxfp4_experts_quant` stubs in `nvfp4_quant_entry.cu`. Real impls are SM10.x-only in CMake but `torch_bindings.cpp` references them unconditionally, which breaks source builds on SM 8.x.\r\n\r\n## Benchmarks\r\n\r\n8×A100 SXM TP=8, `lukealonso/GLM-5.1-NVFP4`, single prompt, decode 200 tokens. `cold` = first request on a fresh prompt; `warm` = repeat (prefix cache hit):\r\n\r\n| context | prefix | TTFT | TPOT median | output tok/s |\r\n|---|---|---|---|---|\r\n| short (1,744 in)   | cold | 0.72 s  | 21.7 ms | 39.7 |\r\n| short (1,744 in)   | warm | 0.18 s  | 21.8 ms | 44.3 |\r\n| mid   (65,536 in)  | cold | 37.0 s  | 24.3 ms | 4.8  |\r\n| long  (127,744 in) | cold | 98.1 s  | 25.2 ms | 1.9",
      "labels": [
        "documentation",
        "rocm",
        "intel-gpu",
        "needs-rebase",
        "v1",
        "nvidia"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-47335",
      "repository": "vllm-project/vllm",
      "number": 47335,
      "type": "pr",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[ROCm][Perf] Enable fused indexer-Q RoPE+quant kernel for DeepSeek/GLM sparse attention",
      "bodyExcerpt": "## Summary\r\n\r\nEnables the existing fused indexer-Q kernel (`fused_indexer_q_rope_quant`) on\r\nROCm for the DeepSeek Sparse Attention (DSA) indexer used by GLM-5.2 /\r\nDeepSeek-V3.2-style models. On CUDA this kernel already collapses the indexer's\r\nquery-side RoPE + FP8 quantization + weight-scale fold into a single launch; on\r\nROCm the same work was falling back to a chain of small Triton/elementwise\r\nkernels per sparse layer per decode token.\r\n\r\nThis is a low-risk, 2-line enablement — the kernel is already on vllm and is\r\narchitecture-aware. It **removes redundant kernels and HBM round-trips** on the\r\nROCm decode path (4 Q-side kernels → 1), yielding a small but consistent decode\r\nimprovement with **no CUDA-side change** and no measured regression.\r\n\r\n## Kernel-level evidence (torch.profiler, GLM-5.2-FP8, MI325X / gfx942, TP8, rank 0)\r\n\r\nBefore/after self-CUDA over the same steady-state decode capture. The indexer\r\ndecode kernels are the ones with ~21.5k calls (per decoded token × sparse\r\nlayers).\r\n\r\n**Fused away** (present in baseline, gone after):\r\n\r\n| Kernel | Role | Self CUDA | Calls |\r\n|---|---|---|---|\r\n| `per_token_group_quant_8bit_kernel` | q → FP8 (ue8m0) quant | 99.8 ms | 21546 |\r\n| `triton_poi_fused_3` | RoPE/cat elementwise | 95.8 ms | 21526 |\r\n| `triton_poi_fused_2` | RoPE/cat elementwise | 89.0 ms | 20521 |\r\n| `triton_poi_fused_mul_slice_unsqueeze_view_4` | weights scale-fold | 85.3 ms | 20500 |\r\n\r\n**Introduced**:\r\n\r\n| Kernel | Role | Self CUDA | Calls |\r\n|---|---|---|---|\r\n| `_fused_indexer_q_rope_quant_kernel` | fused q RoPE + FP8 quant + weight-fold (decode) | 103.7 ms | 21546 |\r\n| `_fused_indexer_q_rope_quant_kernel_0` | same, prefill variant | 6.3 ms | 42 |\r\n| `triton_poi_fused_add_copy_index_select_mul_slice_…` | residual K-side RoPE (now its own kernel) | 90.6 ms | 20500 |\r\n\r\nThe Q-side collapses from **4 dedicated kernels → 1** (the two `triton_poi_fused_2/_3`\r\nRoPE kernels did q *and* k together; after the change q's RoPE is absorbed and\r\nonly ",
      "labels": [
        "rocm",
        "ready",
        "deepseek",
        "quantization",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-49845",
      "repository": "vllm-project/vllm",
      "number": 49845,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix] Pick a KV block size supported by every attention backend",
      "bodyExcerpt": "## Purpose\n\nFixes #48286.\n\n`Platform.update_block_size_for_backend()` picks the KV cache block size from the **first** non-SSM attention backend only. Models that mix attention backends with disjoint block-size support break: GLM-DSA / DeepSeek-V3.2-style models pair a sparse-MLA main backend (`FLASHINFER_MLA_SPARSE`, supports `[32, 64]`, prefers 32) with `DeepseekV32IndexerBackend` (supports only `[64]`). The auto-selected 32 then fails much later in `select_common_block_size()` with the opaque\n\n```\nValueError: No common block size for 32.\n```\n\nand the only workaround is a manual `--block-size 64` (undocumented, as #48286 notes).\n\n## Changes\n\n- `Platform._find_non_ssm_backends()`: collect all distinct non-SSM backends in layer order (existing `_find_non_ssm_backend()` now delegates to it; the CPU platform caller is unchanged).\n- `Platform._preferred_block_size_for_backends()`: start from the first backend's preferred size and extend by least common multiple whenever another backend's `supports_block_size()` rejects the candidate. Since a block size satisfies a backend when it is a multiple of one of the backend's supported kernel sizes, the lcm extension preserves already-satisfied backends. Single-backend behavior is unchanged.\n- Phase 1 of `update_block_size_for_backend()` uses the new selection. Phases 2/3 still use the first backend as before.\n\n## Test Plan\n\nUnit tests added in `tests/v1/worker/test_gpu_model_runner.py`:\n- `test_preferred_block_size_satisfies_all_backends` — `[32,64]` + `[64]` → 64 (the #48286 shape); `[32,64]` alone still → 32.\n- `test_preferred_block_size_single_backend_keeps_default` — `MultipleOf(16)` → 16.\n- `test_preferred_block_size_extends_to_common_multiple` — `[32]` + `[48]` → 96.\n\n## Test Result\n\n- New unit tests + the 3 existing `select_common_block_size` tests pass.\n- End-to-end on GLM-5.2 NVFP4 (`GlmMoeDsaForCausalLM`), PP=2 / TP=8 / `--enable-expert-parallel`, Ray executor, 2 nodes × 8× B200: previously crashed at KV-cache init w",
      "labels": [
        "bug",
        "v1",
        "cpu"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-51156",
      "repository": "vllm-project/vllm",
      "number": 51156,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][Spec Decode] Bound draft max_model_len by the target before logging",
      "bodyExcerpt": "Fixes #29945\n\n## The bug\n\nWith MTP or any other in-checkpoint drafter, startup prints two different max\nmodel lengths for the same run:\n\n```\n[model.py:554] Resolved architecture: Qwen3_5ForConditionalGeneration\n[model.py:1684] Using max model len 131072\n[model.py:554] Resolved architecture: Qwen3_5MTP\n[model.py:1684] Using max model len 262144\n```\n\nUsers read the second line as the drafter being sized for a longer context than\nthe target and reasonably conclude that KV cache is being over-allocated. The\nissue has collected repeat reports on Qwen3-Next, Qwen3.5 and GLM-4.5-Air since\nDecember, including cases where an explicit `--max-model-len 8192` still printed\n`Using max model len 262144` for the draft.\n\n@benchislett investigated twice and confirmed there is no over-allocation, since\n`SpeculativeConfig._maybe_override_draft_max_model_len` clamps the draft to the\ntarget immediately after the model config is built, and invited a PR to fix the\nlog.\n\n## Root cause\n\n`ModelConfig` for the draft is constructed with\n`spec_target_max_model_len=target_model_config.max_model_len`, but\n`_get_and_verify_max_len` only consults that argument inside the\n`derived_max_model_len == inf` branch, which is the fallback for checkpoints\nthat declare no length key at all. Every real MTP or EAGLE checkpoint does\ndeclare one, so the argument is dead in practice and the draft resolves to its\nown declared length, logs it, and is only corrected one step later by the\nclamp in `SpeculativeConfig`.\n\nSo the bound exists twice, in two different places, and the value that gets\nlogged is the one from before the bound is applied.\n\n## The fix\n\nApply `spec_target_max_model_len` as an upper bound in `_get_and_verify_max_len`\nwhenever the length is being derived, so the draft resolves and logs the value\nit will actually run with. The later clamp in `SpeculativeConfig` becomes a\nno-op, which also stops its `Overriding draft model max model len` line from\nfiring on a config that was never wrong.\n\nThe bound i",
      "labels": [
        "bug",
        "needs-rebase"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM",
        "GLM-4",
        "GLM-4.5"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-51314",
      "repository": "vllm-project/vllm",
      "number": 51314,
      "type": "pr",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[ROCm][Perf] Skip cleaning sparse prefill MQA logits",
      "bodyExcerpt": "## Summary\n\nThe sparse prefill path creates the MQA-logits buffer with `torch.full(..., -inf)`.\nThis launches an FP32 fill before every logits kernel.\n\nThis PR passes `clean_logits=False` to AITER, so the buffer uses `torch.empty`\nand the fill is removed.\n\n## Safety\n\nThe only consumer is `top_k_per_row_prefill`. It reads each row only inside\n`[cu_seqlen_ks, cu_seqlen_ke)`. Its vectorized path also guards the final partial\nload, so padding values are never read. The top-k result is unchanged.\n\n## Results\n\nMI355X, TP=4, rank 0, comparable before/after captures:\n\n| metric | before | after |\n|---|---:|---:|\n| FP32 fill launches | 18,162 | **0** |\n| FP32 fill GPU time | 1,250.2 ms | **0 ms** |\n| MQA-logits launches | 18,108 | 17,930 |\n\nThe removed fill was **0.89% of total GPU kernel time**. In the sample below it\ntakes 74.6 µs and is fully serialized before the logits kernel.\n\n![Before: FP32 fill before MQA logits](https://raw.githubusercontent.com/amd-sriram/vllm/ce9490285bbd8099e2d37c5aec8b3d24b7a20785/figures/fig_pr4_before.png)\n\n![After: fill removed](https://raw.githubusercontent.com/amd-sriram/vllm/ce9490285bbd8099e2d37c5aec8b3d24b7a20785/figures/fig_pr4_after.png)\n\n## Model-level e2e results\n\n### Test conditions\n\n| | |\n|---|---|\n| Hardware | MI355X (gfx950), TP=4 |\n| Model | `amd/GLM-5.2-MXFP4`, fp8 KV cache |\n| Stack | vLLM `69715823`, AITER `4a1cc77`, flydsl `0.3.0` |\n| Server | `--async-scheduling --max-num-seqs 256 --max-num-batched-tokens 16384 --block-size 64 --no-enable-prefix-caching`, `index_topk_freq=4` |\n| Env | `VLLM_ROCM_USE_AITER=1`, `VLLM_ROCM_USE_AITER_SPARSE_INDEXER=1`, `AITER_USE_FLYDSL_MOE_SORTING=1`, `VLLM_ROCM_QUICK_REDUCE_QUANTIZATION=INT4` |\n| Perf | `vllm bench serve`, random dataset, `--ignore-eos --request-rate inf`, ISL 1024 and 8192, OSL 1024, concurrency 1-256 (16 points) |\n| Accuracy | `lm_eval` gsm8k 5-shot chat template with thinking enabled, `max_gen_toks=8192`; RULER `niah_single_2` at 16k |\n| Sampling | checkpoint default (tempe",
      "labels": [
        "rocm"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-51315",
      "repository": "vllm-project/vllm",
      "number": 51315,
      "type": "pr",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[ROCm][Perf] Fuse the DSA indexer prologue with AITER",
      "bodyExcerpt": "## Summary\n\nThe DSA indexer prologue uses four kernels for K normalization, Q/K RoPE,\nFP8 quantization, and the K-cache write.\n\nThis PR uses AITER's fused `indexer_qk_rope_quant_and_cache` kernel instead.\nThe fused path is enabled only for its supported BF16/FP8 shape; other\nconfigurations keep the existing path.\n\n## Results\n\nMI355X, TP=4, rank 0:\n\n| indexer stage | before | after | change |\n|---|---:|---:|---:|\n| launches | 4 | 1 | **-75%** |\n| decode time | 17.4 µs | 5.9 µs | **-66%** |\n| prefill time | 163.9 µs | 270.6 µs | **+65%** |\n\nThe fused kernel is faster for decode but slower for prefill. Across the full\ntrace, prologue GPU time changes from 1,093.6 ms to 1,041.4 ms (**-4.8%**).\n\nOne decode indexer stage at the same zoom:\n\n![Before: four prologue kernels](https://raw.githubusercontent.com/amd-sriram/vllm/ce9490285bbd8099e2d37c5aec8b3d24b7a20785/figures/fig_pr3_before.png)\n\n![After: one fused prologue kernel](https://raw.githubusercontent.com/amd-sriram/vllm/ce9490285bbd8099e2d37c5aec8b3d24b7a20785/figures/fig_pr3_after.png)\n\nEnd-to-end geomean:\n\n| metric | before | after | change |\n|---|---:|---:|---:|\n| throughput | 811.75 | 817.58 | **+0.72%** |\n| TPOT | 24.12 ms | 23.96 ms | **-0.66%** |\n| ITL | 23.85 ms | 23.69 ms | **-0.67%** |\n\n## Model-level e2e results\n\n### Test conditions\n\n| | |\n|---|---|\n| Hardware | MI355X (gfx950), TP=4 |\n| Model | `amd/GLM-5.2-MXFP4`, fp8 KV cache |\n| Stack | vLLM `69715823`, AITER `4a1cc77`, flydsl `0.3.0` |\n| Server | `--async-scheduling --max-num-seqs 256 --max-num-batched-tokens 16384 --block-size 64 --no-enable-prefix-caching`, `index_topk_freq=4` |\n| Env | `VLLM_ROCM_USE_AITER=1`, `VLLM_ROCM_USE_AITER_SPARSE_INDEXER=1`, `AITER_USE_FLYDSL_MOE_SORTING=1`, `VLLM_ROCM_QUICK_REDUCE_QUANTIZATION=INT4` |\n| Perf | `vllm bench serve`, random dataset, `--ignore-eos --request-rate inf`, ISL 1024 and 8192, OSL 1024, concurrency 1-256 (16 points) |\n| Accuracy | `lm_eval` gsm8k 5-shot chat template with thinking enabled, `max_gen_tok",
      "labels": [
        "rocm",
        "deepseek"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-51754",
      "repository": "vllm-project/vllm",
      "number": 51754,
      "type": "pr",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[MoE][NVIDIA] Add shared expert fusion for TRTLLM FP8",
      "bodyExcerpt": "## Summary\n\n- Extend the FusedMoE abstraction so shared experts can be represented as always-on routed expert slots on NVIDIA GPUs.\n- Enable the path for the NVIDIA DeepSeek-V3.2/GLM-5.2 model when routed and shared expert storage formats match.\n- Run fused FP8 shared experts through the modular FlashInfer TRTLLM backend, including expert-row padding required by the kernel.\n- Append and pack constant shared-expert routes in one Triton kernel, avoiding the extra top-k concatenation that regressed single-token decode.\n- Preserve ROCm AITER's native fused-shared-expert routing behavior.\n\n## Motivation\n\nShared-expert fusion was effectively limited to ROCm/AITER. NVIDIA models therefore launched the routed MoE and shared MLP separately even when both used compatible FP8 storage.\n\nFlashInfer's native shared-expert entry point is not usable for this FP8 configuration, but its externally routed TRTLLM FP8 kernel handles the shared expert correctly when the shared rows and always-on route are included in the routed problem. This change adds that representation to FusedMoE and keeps the single-token path efficient by synthesizing the shared route while packing top-k IDs and weights.\n\n## Duplicate-work check\n\nI searched open PRs for `shared expert fusion`, `FusedMoE shared experts NVIDIA`, and `FlashInfer shared expert`, and inspected the related issue and PRs.\n\n- #51695 standardizes model-level FSE enablement/quantization compatibility; it does not add NVIDIA routing, weight preparation, or FlashInfer execution. This PR may need a small rebase if that selection refactor lands first.\n- #50963 tracks FlashInfer's native FP4 shared-expert API. This PR implements the externally routed TRTLLM **FP8** path and does not close that FP4 issue.\n- #51632 fixes ROCm Triton expert-count alignment and does not implement the NVIDIA path.\n\nNo open PR found in those searches implements this change.\n\n## Correctness\n\nReal-weight model evaluation:\n\n- Model: GLM-5.2-FP8 under `models/deepseek_v32",
      "labels": [
        "needs-rebase",
        "deepseek",
        "nvidia",
        "quantization"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-51915",
      "repository": "vllm-project/vllm",
      "number": 51915,
      "type": "pr",
      "category": "NEW_SUPPORT",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[ROCm][Model][Bugfix] Enable GLM-5.2-MXFP4 on the deepseek_v32 path and fix sparse attention correctness",
      "bodyExcerpt": "## Purpose\r\n\r\nEnables GLM-5.2 (`GlmMoeDsaForCausalLM`) end-to-end on `vllm/models/deepseek_v32/amd/`\r\nfor gfx942/gfx950. Routing is opt-in via `--model-class-overrides`; the registry entry\r\nis unchanged, so the default path for GLM-5.2 and DeepSeek-V3.2 is untouched. \r\n\r\nThe following issues were also fixed as a result of this overall enablement since it surfaced dormant bugs from a prior deepseek_v32 porting work:\r\n\r\n- **`launch_pdl` forwarded to a Triton runtime that doesn't accept it** — the shared\r\n  `common/kernels.py` passed this CUDA-only kwarg unconditionally; ROCm's Triton rejects\r\n  it by name even when `False`, so the engine died at startup with `KeyError`. Now passed\r\n  only when the platform actually supports PDL.\r\n\r\n- **fp4 bmm invoked with mismatched positional args** — the output tensor landed in the\r\n  kernel's `dtype` slot (`Expected 'Optional[int]' for 'dtype' but found Tensor`). Routed\r\n  through the existing `rocm_aiter_ops.batched_gemm_a16wfp4` wrapper rather than calling\r\n  aiter directly, so the argument contract lives in one place.\r\n\r\n- **fp4 MLA bmm silently wrong for non-power-of-two K** — `batched_gemm_a16wfp4` only\r\n  supports power-of-two K, and returns wrong values without erroring otherwise. GLM-5.2's\r\n  `qk_nope_head_dim=192` lands in the unsupported set; DeepSeek-V3.2's 128 does not.\r\n  Guarded on both `qk_nope_head_dim` and `kv_lora_rank` to fall back to the fp8 bmm, with\r\n  a warning. `W_K`/`W_K_scale` are also made contiguous after the transpose, which\r\n  otherwise produced NaN.\r\n\r\n- **Indexer-K cache written in the plain layout while its reader expects the shuffled\r\n  one** — affects DeepSeek-V3.2 identically, not just GLM-5.2. The AMD path writes this\r\n  cache from the shared `fused_norm_rope`, which only implemented\r\n  `[n_blocks, blk, head_dim]`, while the reader selects `SHUFFLE` above block size 1: the\r\n  write kept storing row-major, but the reader expected shuffled, so the indexer scored\r\n  garbage K and output degenerate",
      "labels": [
        "bug",
        "rocm",
        "deepseek",
        "verified",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 8,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "TRAINING",
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-51966",
      "repository": "vllm-project/vllm",
      "number": 51966,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][Model][CI/Build] Fix CUDA graph capture in GLM-OCR / GLM4-MoE-Lite MTP layers",
      "bodyExcerpt": "## Purpose\r\n\r\nFIX #48749\r\n\r\n`GlmOcrMultiTokenPredictorLayer.forward` and `Glm4MoeLiteMultiTokenPredictorLayer.forward`\r\nmask the position-0 embedding with an in-place boolean-mask assignment:\r\n\r\n```python\r\ninputs_embeds[positions[0] == 0] = 0     # glm_ocr_mtp.py\r\ninputs_embeds[positions == 0] = 0        # glm4_moe_lite_mtp.py\r\n```\r\n\r\nA boolean-mask `index_put_` calls `nonzero()`, whose output shape is data-dependent, so it\r\nsynchronizes device to host to size the allocation. That is not allowed inside an active\r\nCUDA graph capture, and the V2 model runner captures the MTP prefill graph\r\n(`speculator.capture()` -> `prefill_cudagraph_manager.capture()`), so engine startup fails:\r\n\r\n```\r\nFile \"vllm/model_executor/models/glm_ocr_mtp.py\", line 87, in forward\r\n    inputs_embeds[positions[0] == 0] = 0\r\ntorch.AcceleratorError: CUDA error: operation not permitted when stream is capturing\r\n```\r\n\r\nIt was reported on A100-SXM4-80GB in #48749.\r\n\r\nThe equivalent capture-safe form is already used by the other two MTP implementations:\r\n\r\n- `vllm/model_executor/models/deepseek_mtp.py:113`\r\n- `vllm/model_executor/models/glm4_moe_mtp.py:113`\r\n\r\nboth of which read `inputs_embeds = torch.where(positions.unsqueeze(-1) == 0, 0, inputs_embeds)`.\r\nThis PR applies the same form to the two remaining implementations. GLM-OCR keeps\r\n`positions[0]` because it is an MRoPE model, where `positions` has shape `(3, seq_len)`.\r\n\r\nDuplicate-work checks were performed before opening: no open PR references #48749 or\r\ntouches `glm_ocr_mtp.py`. #39483 and #43747 fix different code paths that surface the same\r\nCUDA error.\r\n\r\nRelated: #49869 fixes an unrelated GLM-OCR MTP weight-loading regression (#49856). On main\r\nas of 3ee2df303 with the HF checkpoint, startup fails there before reaching graph capture,\r\nso that patch was applied locally to reproduce this crash. The crash fixed here reproduces\r\nwith no local patches on v0.25.1. The two fixes are independent and can merge in either\r\norder.\r\n\r\n## Test Plan\r",
      "labels": [
        "bug",
        "speculative-decoding",
        "needs-rebase",
        "nvidia",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 7,
      "matchedTerms": [
        "GLM",
        "GLM4"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-52538",
      "repository": "vllm-project/vllm",
      "number": 52538,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][ROCm][Model Runner V2] Capture FULL CUDA graphs on the graph_capture stream",
      "bodyExcerpt": "## Summary\n- MRV2 FULL graph capture called `torch.cuda.graph(graph, pool)` without `stream=`. On ROCm, PyTorch then records onto a private side stream instead of the stream `graph_capture()` bound custom allreduce / aiter to.\n- CUDAGraphWrapper and the custom-allreduce tests already pass that stream. This makes `CudaGraphManager` do the same **on ROCm only**: `stream=cap_ctx.stream`. CUDA keeps PyTorch's default capture stream.\n- This is not a duplicate of open PRs around FULL capture (e.g. #51700 DBO microbatch graphs, #51966 GLM MTP capture). Those do not fix the missing `stream=` argument on MRV2 FULL capture.\n\n## Test plan\n- [x] `.venv/bin/python -m pytest tests/v1/cudagraph/test_cudagraph_manager.py tests/v1/cudagraph/test_breakable_cudagraph.py -q` — 16 passed (CUDA and ROCm `stream=` cases)\n- [ ] Serving / TP allreduce replay on ROCm FULL graphs (decode after capture)\n\nServing eval was not completed here: this host's FlyDSL MoE first-touch currently memory-faults before graph capture, independent of this change.\n\n## AI assistance\nAI assistance was used to investigate the capture-stream mismatch and to draft this patch. A human reviewed the changed lines and ran the unit tests above.",
      "labels": [
        "bug",
        "rocm",
        "ready",
        "needs-rebase",
        "nvidia",
        "mrv2"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-53263",
      "repository": "vllm-project/vllm",
      "number": 53263,
      "type": "pr",
      "category": "DOCS",
      "categoryValid": true,
      "ruleCategory": "DOCS",
      "classificationConsistent": true,
      "title": "[7/N][KV Connector][NIXL] Stage host KV reads through device memory",
      "bodyExcerpt": "## Summary\n\n- Stage same-node NIXL reads into registered device buffers when every destination region is host memory.\n- Pipeline the completed device-to-host copies without reporting a request complete until both transfer legs finish.\n- Bound shutdown and retain staging slots through asynchronous copy completion, including failure and abort paths.\n\nThis avoids UCX's slow TCP-loopback path for direct device-to-host NIXL reads while leaving device-resident and mixed layouts on their existing path.\n\n## Duplicate check\n\nOpen-PR searches found no other PR implementing same-node NIXL host-read staging. This is a generic NIXL prerequisite used by the HiSparse stack, rather than a HiSparse-specific path.\n\n## Tests\n\n```bash\nsource .venv/bin/activate\npython -m pytest tests/v1/kv_connector/unit/test_nixl_connector.py -k host_stager -q\n```\n\nResult: 4 passed. Changed-file pre-commit hooks also passed.\n\n## Evaluation\n\nOn 8xB300 with GLM-5.2-FP8 in a 4P+4D NIXL setup, the isolated 32K-token import improved from about 17.7 s to 3.6 s. A GSM8K run remained stable (0.807 to 0.817) with no staging errors.\n\n## AI assistance\n\nAI assistance was used for implementation, tests, review, and documentation. The human submitter is responsible for reviewing every changed line and defending the change end-to-end.\n",
      "labels": [
        "documentation",
        "speculative-decoding",
        "ready",
        "torch.compile",
        "ci/build",
        "deepseek",
        "cpu",
        "kv-connector",
        "nvidia",
        "mrv2",
        "dflash",
        "scheduler",
        "kv-cache-manager",
        "glm",
        "minimax"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-53427",
      "repository": "vllm-project/vllm",
      "number": 53427,
      "type": "pr",
      "category": "NEW_SUPPORT",
      "categoryValid": true,
      "ruleCategory": "NEW_SUPPORT",
      "classificationConsistent": true,
      "title": "feat(PCP)(Spec Decode): Support MTP and DSpark with PCP",
      "bodyExcerpt": "## Summary\r\n\r\nAdd speculative decoding support to the V2 GPU model runner when Prefill Context Parallelism (PCP) is enabled, covering both MTP and DSpark drafters.\r\n\r\n- Keep the target model PCP-sharded while running the drafter on the replicated global batch.\r\n- Restore target hidden states to the global layout before `speculator.propose()`, including MTP pre-HC residuals and DSpark auxiliary hidden states.\r\n- Build autoregressive draft-prefill attention metadata inside `AutoRegressiveSpeculator.propose()` from the drafter's own batch descriptor and persistent buffers.\r\n- Re-gather global block tables for replicated DFlash-family drafters so context-KV slot mappings follow the global request layout.\r\n- Exclude replicated draft attention layers from target-side PCP backend compatibility checks.\r\n- Keep speculative verification requests as multi-token decode rows with GPU-corrected positions and sequence lengths.\r\n- Support greedy and probabilistic MTP draft sampling and `num_speculative_tokens_per_batch_size` dynamic draft lengths.\r\n- Support PIECEWISE CUDA graphs with the persistent PCP capture-buffer and graph-padding contract from [#53515](https://github.com/vllm-project/vllm/pull/53515), now included in `main`.\r\n\r\n## Implementation\r\n\r\nFor replicated PCP autoregressive drafting, `AutoRegressiveSpeculator.propose()` prepares draft-prefill metadata after its own input buffers and CUDA graph dispatch shape are ready. The metadata therefore uses the drafter's global batch layout and its selected token padding, while target execution remains PCP-sharded.\r\n\r\nMTP restores the target model's pre-HC hidden-state buffer before drafting. DSpark restores the auxiliary hidden states consumed by the drafter and refreshes DFlash block tables for the global request mapping. Draft layers remain replicated and do not participate in target PCP attention sharding.\r\n\r\n## Validation\r\n\r\n### GLM-4.7-Flash full GSM8K\r\n\r\nEnvironment: 4x NVIDIA H20, `/home/weight/GLM-4.7-Flash`, `FLASH_ATT",
      "labels": [
        "speculative-decoding",
        "needs-rebase",
        "mrv2",
        "dflash"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM",
        "GLM-4"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-53592",
      "repository": "vllm-project/vllm",
      "number": 53592,
      "type": "pr",
      "category": "DOCS",
      "categoryValid": true,
      "ruleCategory": "DOCS",
      "classificationConsistent": true,
      "title": "[8/N] HiSparse: add DeepSeek V4 support",
      "bodyExcerpt": "## Summary\n\n- Integrate DeepSeek V4 C4 sparse MLA with HiSparse resident and host-backed cache paths.\n- Add the compressed slot/block geometry needed by DeepSeek V4 while keeping non-C4 cache groups resident on GPU.\n- Preserve the split FP8 value/scale page layout during host backup and hot-buffer staging.\n- Resolve C4 FlashMLA decode selections through HiSparse and stage compressed prefill cache rows.\n\nThis support was extracted from #51323 so the validated GLM/DeepSeek-V3.2 HiSparse implementation can be reviewed and merged without DeepSeek V4's compressed cache geometry. It does not duplicate another open PR; searches for DeepSeek V4 HiSparse work found only the parent HiSparse PR from which this change was split.\n\n## Tests\n\n- `cmake --build --preset release --target _C_stable_libtorch` (passed)\n- `python -m pytest tests/v1/attention/test_sparse_mla_backends.py -q -k hisparse` (22 passed)\n- `python -m pytest tests/v1/core/test_kv_cache_utils.py tests/v1/worker/test_utils.py -q` (100 passed)\n- Pre-commit hooks passed for all files changed while splitting the generic and DeepSeek V4 paths.\n\n## Model evaluation\n\nThe latest DeepSeek V4 B300 throughput sweep exposed a substantial high-concurrency regression and an allocator failure under saturation. The sweep also used a metrics revision with invalid hot-path counter instrumentation, so those numbers are not suitable as final performance evidence. This PR remains draft until DeepSeek V4 performance and saturation robustness are resolved and re-evaluated.\n\nAI assistance was used for implementation, review, testing, and preparation of this PR.\n",
      "labels": [
        "documentation",
        "speculative-decoding",
        "torch.compile",
        "ci/build",
        "deepseek",
        "cpu",
        "kv-connector",
        "nvidia",
        "mrv2",
        "DSv4",
        "dflash",
        "scheduler",
        "kv-cache-manager",
        "glm",
        "minimax"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-53692",
      "repository": "vllm-project/vllm",
      "number": 53692,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[CI][Docs] Fix decode/prefill consistency test prefix construction; add GLM-4-9B and Phi-4 to batch-invariant tested models",
      "bodyExcerpt": "## Purpose\r\n\r\nTwo related changes, both following the review on this PR:\r\n\r\n1. **Fix a soundness bug in `tests/v1/determinism/test_batch_invariance.py::test_decode_logprobs_match_prefill_logprobs`.** The test rebuilt each prefill prefix from *text* and re-encoded it; `encode(decode(ids))` is not lossless for BPE tokenizers, so the \"prefill\" could evaluate a different token sequence than decode did and report false mismatches. The prefix is now built directly from token ids (`prompt_token_ids + decode_token_ids[:i]`), which also removes an O(N²) extra `llm.generate` per decoded token. This is the fix from #43317 (@Evangade), carried here at the maintainer's request after that PR went stale; the standalone tokenizer regression test from #43317 is dropped per the review there. Original report by @xRay2016 in #27433.\r\n2. **Add `zai-org/glm-4-9b-chat` and `microsoft/phi-4` to the tested-model list**, following the [invitation to submit model validations without a separate issue](https://github.com/vllm-project/vllm/pull/53247#issuecomment-5374419058).\r\n\r\nWhy the fix matters here: GLM was originally removed from this PR because the official suite reported a decode/prefill mismatch. Root cause (see the comments below): the model emits `'.'` + `'Here'` as `[13, 8419]`, which re-encodes to the single token `89329` (`'.Here'`), so the text-based prefix diverged from position 11 onward. With token-id prefixes GLM is fully green. Any model whose greedy output hits a BPE merge point can false-fail the unfixed test the same way.\r\n\r\nNot a duplicate: #43317 proposes the same test fix but has had no author activity since the May review and is marked stale; this PR carries that commit with its original authorship and sign-off instead of opening another PR.\r\n\r\n## Test Plan\r\n\r\nOfficial suite, `VLLM_BATCH_INVARIANT=1`, vLLM 0.28.0 (cu129 wheel), torch 2.13.0, Triton 3.7.1, RTX 4090 D (sm89) and H20 (sm90):\r\n\r\n```bash\r\nVLLM_TEST_MODEL=<model> pytest tests/v1/determinism/test_batch_invari",
      "labels": [
        "documentation",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM",
        "GLM-4"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-53848",
      "repository": "vllm-project/vllm",
      "number": 53848,
      "type": "pr",
      "category": "NEW_SUPPORT",
      "categoryValid": true,
      "ruleCategory": "NEW_SUPPORT",
      "classificationConsistent": true,
      "title": "[Model][Quantization] Support GLM W4AFP8 checkpoints with Humming",
      "bodyExcerpt": "## Purpose\n\nThis PR adds end-to-end support for GLM W4AFP8 MoE checkpoints through the existing Humming backend. It enables the public [PhalaCloud/GLM-5.2-W4AFP8](https://huggingface.co/PhalaCloud/GLM-5.2-W4AFP8) checkpoint by:\n\n- loading packed signed INT4 expert weights and converting them to the Humming uint4 layout;\n- loading BF16 group-128 weight scales and static FP8 activation scales;\n- mapping checkpoint scales for GEMM1 and GEMM2;\n- integrating the quantization method with Modular FusedMoE.\n\nThe initial scope is NVIDIA Hopper, group size 128, top-k 8, and expert parallel size 1.\n\nThis replaces the dedicated CUTLASS approach from #49979 following maintainer feedback. It reuses the existing Humming kernels and does not add another CUDA/CUTLASS kernel to vLLM. Duplicate-work searches for issue #49977 and GLM/W4AFP8/Humming found no open PR implementing this checkpoint integration.\n\nAI assistance was used to prepare this PR. I reviewed every changed line and ran the tests and model evaluation reported below.\n\nRelated to #49977.\n\n## Test Plan\n\n```bash\n.venv/bin/python -m pytest \\\n  tests/quantization/test_w4afp8.py \\\n  tests/quantization/test_humming_mxfp4_block_fp8.py \\\n  tests/kernels/moe/test_humming_w4afp8.py -v\n\npre-commit run --from-ref upstream/main --to-ref HEAD\n```\n\nEnd-to-end serving was tested with TP=8, FP8 KV cache, and two MTP speculative tokens using the public GLM-5.2-W4AFP8 checkpoint.\n\n## Test Result\n\n- All three pytest files passed.\n- All applicable pre-commit hooks passed.\n- End-to-end startup and serving passed on 8x NVIDIA H20 (SM90), CUDA 12.9, PyTorch 2.13.0+cu129, and Humming 0.1.12.\n- Full GSM8K evaluation (1,319 questions, MTP enabled): 0.945 accuracy, 0.000 invalid-response rate, 133,360 output tokens, and 392.425 output tokens/s.",
      "labels": [
        "quantization",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 7,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-53867",
      "repository": "vllm-project/vllm",
      "number": 53867,
      "type": "pr",
      "category": "NEW_SUPPORT",
      "categoryValid": true,
      "ruleCategory": "NEW_SUPPORT",
      "classificationConsistent": true,
      "title": "[Feature][PCP] Support decode-only FULL CUDA graphs",
      "bodyExcerpt": "## Purpose\r\n\r\nEnable prefill context parallelism (PCP) to use decode-only FULL CUDA graphs through `FULL_DECODE_ONLY` and `FULL_AND_PIECEWISE`.\r\n\r\nThis change:\r\n\r\n- keeps PCP graph capture and replay on persistent rank-local input buffers;\r\n- selects PCP persistent capture inputs from the graph mode and typed `PCPManager` in one linear branch;\r\n- keeps standard block-table, slot-mapping, and DCP preparation together in the common capture path;\r\n- passes the batch execution descriptor through the PCP adapter and derives mode-specific token and request padding in one place;\r\n- preserves graph-padded request and token capacities after PCP batch partitioning;\r\n- prepares PCP-local block tables, slot mappings, and attention metadata for FULL capture and dummy runs;\r\n- keeps pure `FULL` mode restricted because PCP prefill uses a separately dispatched execution path;\r\n- adds focused unit and GSM8K E2E coverage for configuration validation, prefill rejection, request padding, persistent PCP buffers, and combined CUDA graph mode.\r\n\r\n## Test Plan\r\n\r\nFull GPU accuracy validation:\r\n\r\n- NVIDIA H100 80GB HBM3 x2, GPU 4/5\r\n- GLM-4.7-Flash, BF16\r\n- TP=1, PCP=2, EP=2\r\n- max model length 8192; max sequences 32; max batched tokens 32768\r\n- GSM8K, all 1319 questions, 5-shot, concurrency 32, temperature 0, seed 42, max output tokens 4096\r\n- eager, `PIECEWISE`, `FULL_DECODE_ONLY`, and `FULL_AND_PIECEWISE`\r\n\r\n## Test Result\r\n\r\nAll four services completed the full 1319-question dataset with zero invalid completions and zero request errors.\r\n\r\n| Mode | Correct / Total | Accuracy | Delta vs eager | Invalid | Errors |\r\n| --- | ---: | ---: | ---: | ---: | ---: |\r\n| Eager | 1077/1319 | 81.6528% | baseline | 0 | 0 |\r\n| `PIECEWISE` | 1082/1319 | 82.0318% | +0.3791 pp | 0 | 0 |\r\n| `FULL_DECODE_ONLY` | 1075/1319 | 81.5011% | -0.1516 pp | 0 | 0 |\r\n| `FULL_AND_PIECEWISE` | 1085/1319 | 82.2593% | +0.6065 pp | 0 | 0 |\r\n\r\nThe graph-mode deltas remain within 0.61 percentage points of eager on this evalua",
      "labels": [
        "ready",
        "nvidia",
        "mrv2"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM",
        "GLM-4"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-54074",
      "repository": "vllm-project/vllm",
      "number": 54074,
      "type": "pr",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[ROCm][Perf] Use the fused clamped SwiGLU kernel on ROCm",
      "bodyExcerpt": "## Purpose\n\n`SiluAndMulWithClamp` routes ROCm to `forward_native`, which evaluates the activation as six eager kernels — two clamps, a sigmoid, two multiplies and an add. The fused op it skips, `torch.ops._C.silu_and_mul_with_clamp`, carries no HIP-specific guard in `csrc/libtorch_stable/activation_kernels.cu` and is built and numerically correct on gfx950. `SiluAndMul`, a few classes up in the same file, already takes the `is_cuda_alike()` path on ROCm — this exclusion looks like an oversight rather than a guard.\n\n```diff\n- if current_platform.is_rocm() or current_platform.is_xpu():\n+ if current_platform.is_xpu():\n      self._forward_method = self.forward_native\n  elif current_platform.is_cuda_alike():\n      self.op = torch.ops._C.silu_and_mul_with_clamp\n```\n\nXPU keeps its existing native path.\n\n## Test Plan\n\nMeasured on 8× MI355X (gfx950), ROCm 7.2.3, with GLM-5.3-Flash — a 320B/18B multimodal MoE that clamps the SwiGLU gate in both its dense and MoE MLPs (`swiglu_limit=10.0`). TP8, 1M context, bf16 KV cache.\n\n1. **Numerical check** — fused output vs `forward_native` over the model's shapes.\n2. **Isolated kernel timing** — both arms interleaved inside a single process, so the board's shared power budget (which drifts an unchanged kernel by up to ~13% between processes here) cannot bias one arm against the other.\n3. **End to end** — `vllm bench serve`, each arm on a freshly started server so prefix-cache state matches.\n\n## Test Result\n\nNumerics: fused matches `forward_native` within bf16 rounding (max abs diff 3.1e-02 at values up to the clamp limit of 10).\n\nIsolated kernel, interleaved:\n\n| shape | tokens | native | fused | speedup |\n|---|---|---|---|---|\n| dense MLP d=1536 | 64 | 54.67 µs | 23.95 µs | 2.28× |\n| MoE d=256 | 64 | 55.06 µs | 23.89 µs | 2.31× |\n| dense MLP d=1536 | 512 | 57.75 µs | 24.58 µs | 2.35× |\n| MoE d=256 | 512 | 54.01 µs | 23.55 µs | 2.29× |\n\nA decode-phase torch profile attributed 1.94 ms per step to the six eager kernels across 45 layers — e",
      "labels": [
        "rocm",
        "verified"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-54496",
      "repository": "vllm-project/vllm",
      "number": 54496,
      "type": "pr",
      "category": "NEW_SUPPORT",
      "categoryValid": true,
      "ruleCategory": "NEW_SUPPORT",
      "classificationConsistent": true,
      "title": "[NIXL][PCP][DCP] Support matching DCP layouts with PCP producers",
      "bodyExcerpt": "Replaced by https://github.com/PatrykSaffer/vllm/pull/3, targeting the branch behind https://github.com/vllm-project/vllm/pull/56157. The replacement contains only the simplified NIXL connector change; GitHub cannot retarget an existing PR across repositories.\n\nThe description below is retained for historical context. Updated implementation scope and validation are in the replacement PR.\n\n---\n\n## Summary\n\nSupersedes #49342 with a reduced port on current `main`, after #50611 and #52779 landed.\n\nThis change allows a TP1 PCP producer whose DCP group spans the PCP group to transfer its distinct DCP KV shards to a consumer with the same DCP size. Replicated PCP behavior remains unchanged.\n\n- expose every PCP rank as a distinct transfer rank only for PCP+DCP\n- match producer and consumer workers by DCP rank\n- publish and count every distinct producer DCP shard\n- retain the canonical PCP-rank-0 path when DCP is disabled\n- reject partial PCP/DCP groups, unequal DCP layouts, decoder-side PCP, and bidirectional PCP transfer\n\n## Scope\n\nThe supported topology is a `TP1 + PCPN + DCPN` KV producer and a PCP-disabled KV consumer with matching `DCPN`. Unequal-DCP remapping and decoder PCP remain out of scope.\n\nAttention execution is deliberately outside this connector PR. End-to-end validation stacks the separate PCP-spanning DCP attention implementation so execution reaches the NIXL handoff.\n\n## Validation\n\n- Python compilation of all changed modules: passed\n- `git diff --check`: passed\n- focused pytest: pending in the CUDA/Linux development environment\n- live GB200 P/D smoke: pending\n\nPlanned live smoke topology: one four-GPU `TP1+PCP4+DCP4` GLM prefiller to one four-GPU `TP1+DP4+DCP4` decoder, followed by a deterministic long-prompt request, second-turn prefix reuse, transfer-completion/log inspection, and clean drain. The exact manifest, request, and result will be added before marking ready.\n\n## AI assistance\n\nOpenAI Codex assisted with the current-main port, conflict audit, v",
      "labels": [
        "needs-rebase",
        "deepseek",
        "kv-connector",
        "mrv2"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-54527",
      "repository": "vllm-project/vllm",
      "number": 54527,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][Multimodal] Honor modality-scoped mm_processor_kwargs in every model that reads them",
      "bodyExcerpt": "Follow-up to #53808.\n\n## Background\n\n#53808 taught `get_merged_mm_kwargs` to overlay HF-style nested\n`images_kwargs` / `videos_kwargs` / `audio_kwargs` onto the flat namespace, and\nthreaded a `modality` argument through Qwen2-VL and Qwen3-VL. Its docstring\nstates the rule:\n\n> When `modality` is set, HF-style nested `images_kwargs` / `videos_kwargs` /\n> `audio_kwargs` are overlaid onto the flat namespace for vLLM-side reads\n> (token budgets, dummy inputs). Processor construction and HF `__call__`\n> should omit `modality` so the nested dicts still reach the HF processor.\n\nEvery model outside the Qwen family that predicts sizes or token counts from\nthose kwargs was left reading the flat namespace only. The HF processor honors\nthe nested dict in its `__call__`, so vLLM and the processor disagree about how\nmany tokens an item expands to.\n\n## Scope: every call site audited\n\nPer review this now covers every multi-modal model, not just the VL three.\nI enumerated every `get_merged_mm_kwargs` call site and classified each (40 when\nthis PR was opened, 44 on current `main`).\n\n### Fixed (vLLM-side reads)\n\n| model | site | modality |\n| --- | --- | --- |\n| `glm4_1v` | `_get_image_max_pixels`, `_get_video_max_pixels` | static |\n| `ernie45_vl`, `keye`, `mimo_v2_omni` | `_get_vision_info`, via `get_num_{image,video}_tokens` | threaded, as Qwen2-VL |\n| `cohere_compass` | `_get_vision_info`, via `get_num_image_tokens` | threaded, as Qwen2-VL |\n| `transformers/multimodal` | both `_get_num_multimodal_tokens` reads | `image` |\n| `cohere2_vision`, `idefics3`, `interns1` | merged dict passed as HF's `images_kwargs` positional arg | `image` |\n| `lfm2_vl`, `mistral3`, `paddleocr_vl` | flat reads of `size` / patch geometry | `image` |\n| `glm5next` | `_get_image_max_pixels`, `_get_video_max_pixels`, inherited from `glm4_1v` | static |\n\nTwo are worth calling out:\n\n- **`mimo_v2_omni`** is the only one needing a new parameter; it is a copy of\n  `qwen2_vl.py` with the `modality` plumbing stripped, ",
      "labels": [
        "bug",
        "intel-gpu",
        "ready",
        "ci/build",
        "multi-modality",
        "qwen",
        "mistral",
        "glm",
        "cohere"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM",
        "GLM4"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-54929",
      "repository": "vllm-project/vllm",
      "number": 54929,
      "type": "pr",
      "category": "OTHER",
      "categoryValid": true,
      "ruleCategory": "OTHER",
      "classificationConsistent": true,
      "title": "[Attention] Portable Triton sparse-MLA fallback for SM12x (consumer Blackwell)",
      "bodyExcerpt": "## Purpose\r\n\r\nDSA models (DeepSeek-V3.2, GLM-5.2) currently have **no working sparse-attention backend on\r\nconsumer Blackwell** (SM12x: RTX 5090, GB10): the native FlashMLA sparse kernels\r\n(`vllm._flashmla_C`) only build for sm90/sm100, and the sole SM12x option,\r\n`FLASHINFER_MLA_SPARSE_SM120`, livelocks under sustained load — GPUs pin at 100 % with zero\r\noutput until the NCCL watchdog kills the server. Fixes #51921 (on SM12x).\r\n\r\nThis makes the `FLASHMLA_SPARSE` backend work on SM12x by binding portable **Triton**\r\nimplementations of `flash_mla_sparse_fwd` / `flash_mla_with_kvcache` when the native extension\r\ncan't serve the device. The kernels are from #49026 (@marksunner, validated there on\r\nsm_121/GB10) — this PR is that work rebased onto current main as a first-class code path\r\ninstead of that PR's runtime monkeypatch, with its model-side diffs dropped (main already has\r\nthat content). Supersedes/continues #49026 — happy to hand back or co-drive.\r\n\r\nChanges:\r\n- `ops/flashmla.py`: `_use_triton_sparse_mla()` (auto on SM12x; `VLLM_TRITON_MLA_SPARSE`\r\n  forces on/off) selects the Triton binding; `is_flashmla_sparse_supported()` reports True\r\n  there; `get_mla_metadata` returns `(None, None)` (the Triton kernels self-schedule).\r\n- New files: `sm12x_sparse_mla_attn.py`, `sparse_mla_kernels.py`, `sparse_mla_env.py`,\r\n  `ops/deepseek_v4_ops/*` — kernels + helpers from #49026, unchanged.\r\n- `flashmla_sparse.py`: capability 12 allowed on the Triton path; %16 fp8-decode head padding\r\n  there (64-head models at TP=8 have 8 heads/rank — 64-padding wastes 87 % of attention\r\n  compute); b12x fused fast path with clean fallback.\r\n- `sparse_attn_indexer.py`: DeepGEMM not hard-required on the Triton path (fallbacks exist).\r\n- `envs.py`: `VLLM_TRITON_MLA_SPARSE*` registered.\r\n\r\n**All changes are no-ops on sm90/sm100** unless `VLLM_TRITON_MLA_SPARSE=1` is set explicitly.\r\n\r\n## Test Plan\r\n\r\nEnd-to-end serving on 24× RTX 5090 (3 hosts, TP=8 × PP=3), GLM-5.2-NVFP4,\r\n`--kv-cache-dtype",
      "labels": [],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-54951",
      "repository": "vllm-project/vllm",
      "number": 54951,
      "type": "pr",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[Perf][GLM-5.3-Flash] Shard long-context indexer prefill rows across TP",
      "bodyExcerpt": "## TL;DR\n\n**How it works / why it is faster:** Instead of repeating sparse-indexer prefill MQA scoring and top-k on every TP rank, partition independent query rows into cost-balanced contiguous slices. Each rank scores its rows against the full available key history; `all_gatherv` reassembles the final int32 indices. This removes redundant computation in exchange for fixed-width index communication, so savings grow with context length. There is no attention approximation. Decode kernels are unchanged; smaller batches retain the existing path (activation requires at least `16,384 × TP` scheduled prefill query rows).\n\n**Performance, baseline → optimized:** main parent `5893426b88f7` → PR head `16934c6e76e5`; GLM-5.3-Flash on 4× H200, TP4, C1, 256 output tokens, 65,536-token batch budget, legacy runner + explicit FA3 backend. Four fresh-process A/B pairs in ABBA/BAAB order; warmups excluded. Throughput below counts input + output tokens at C1, **not saturated serving throughput**.\n\n| Input context | Server TTFT, seconds (B → O) | TTFT reduction | Total tok/s (B → O) | Throughput increase |\n|---|---|---|---|---|\n| 128K | 4.782 → 4.605 | 3.69% | 20,269.73 → 20,835.80 | 2.79% |\n| 256K | 10.154 → 9.372 | 7.70% | 22,090.93 → 23,651.89 | 7.07% |\n| 512K | 22.760 → 19.418 | 14.68% | 21,379.03 → 24,752.65 | 15.78% |\n| 1M (1,000,000) | 54.154 → 40.388 | 25.42% | 17,856.28 → 23,675.18 | 32.59% |\n\nServer-side TPOT at 1M: **6.631 → 6.633 ms/token**. Across the full 1K–1M scan, absolute TPOT percentage-change point estimates are <0.04%; every paired 95% interval includes zero. This does not prove equivalence. The 2K TTFT point estimate is 2.84% slower (interval crosses zero); we do not claim blanket short-input non-regression.\n\n**Correctness / targeted accuracy, baseline → optimized:** Across 33 distinct retrieval probes spanning 1K–1M, repeated over four processes per arm, expected-answer presence is **132/132 → 132/132**; strict answer-only accuracy is **127/132 → 129/132**. Exact",
      "labels": [
        "new-model",
        "rocm",
        "speculative-decoding",
        "torch.compile",
        "ci/build",
        "multi-modality",
        "kv-connector",
        "nvidia",
        "quantization",
        "mrv2",
        "scheduler",
        "kv-cache-manager",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 7,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55053",
      "repository": "vllm-project/vllm",
      "number": 55053,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][KV Connector][ROCm] Fix MoRI-IO KV block-offset for speculative decoding (MTP)",
      "bodyExcerpt": "## Summary\n`compute_block_transfer_offsets` in the MoRI-IO KV connector raised `ValueError: local_block_ids longer than remote_block_ids` whenever the producer's local block count exceeded the consumer's. Under **speculative decoding (e.g. MTP)** with `block_size=1`, the prefill (producer) KV-cache manager reserves `num_speculative_tokens` lookahead slots that the decode (consumer) side does not allocate, so `local` exceeds `remote` by exactly the spec-token count (e.g. **8003 vs 8000** at n=3). This crashed the first prefill→decode KV write and made MTP unusable over MoRI-IO PD-disaggregation.\n\n## Fix\nBlock order is positional (`block_size=1`), so the prompt KV is the prefix and the extra local blocks are empty lookahead scratch. Clamp `local_block_ids` to `local[:len(remote)]` and transfer only the prompt prefix — symmetric with the existing shorter-local handling already documented in the same function.\n\n## Tests\n`tests/v1/kv_connector/unit/test_moriio_kv_layout.py`:\n- Replaces the now-obsolete longer-local *raise* test with **clamp-to-prompt-prefix** tests for both the interleaved (`transfers_per_block=1`) and separated split-KV (`transfers_per_block=2`) layouts, asserting the result is identical to transferring `local[:len(remote)]`.\n- Adds a shorter-local regression guard (pre-existing READ-mode partial transfer unchanged).\n\n## Validation\nGLM-5.3-FP8 WideEP **1P1D / 1P2D PD-disagg** (vLLM v0.27.1 + AITER v0.1.19), MTP n=3:\n- KV transfer succeeds (clamp fires, e.g. `8003 -> 8000` / `8 -> 5`); no more ValueError.\n- Coherent output, **0 token-0 (`!`) corruption**, FULL cudagraph engaged.\n- 8k/8k decode-heavy shows the expected MTP TPOT win (1.36–1.45×) at low concurrency.\n\n## AI assistance\nThis PR was prepared with AI assistance (Cursor). All changed lines and tests were reviewed, and the behavior was validated end-to-end by the submitter on GLM-5.3-FP8 WideEP PD-disaggregated serving (1P1D/1P2D). Attribution is recorded via the `Co-authored-by: Cursor` commit tr",
      "labels": [
        "bug",
        "rocm",
        "kv-connector"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-55081",
      "repository": "vllm-project/vllm",
      "number": 55081,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][Spec Decode] Implement SupportsPP on remaining MTP drafters (14 classes)",
      "bodyExcerpt": "## Summary\n\nCompletes the SupportsPP sweep started in #46994 (DeepSeek + Qwen3.5), #53408 (DSv4), and #54635 (Kimi-K3, MiniMax-M3, Glm4-MoE) by adding the mixin + `make_empty_intermediate_tensors_factory` wire-up to the 14 remaining outer MTP draft classes.\n\n## Files (12)\n\n| file | class |\n|---|---|\n| `vllm/model_executor/models/ernie_mtp.py` | `ErnieMTP` |\n| `vllm/model_executor/models/exaone_moe_mtp.py` | `ExaoneMoeMTP` (`Exaone4_5_MTP` inherits) |\n| `vllm/model_executor/models/gemma4_mtp.py` | `Gemma4MTP` |\n| `vllm/model_executor/models/hy_v3_mtp.py` | `HYV3MTP` |\n| `vllm/model_executor/models/longcat_flash_mtp.py` | `LongCatFlashMTP` |\n| `vllm/model_executor/models/mimo_mtp.py` | `MiMoMTP` |\n| `vllm/model_executor/models/mimo_v2_mtp.py` | `MiMoV2MTP` (`MiMoV2OmniMTP` inherits) |\n| `vllm/model_executor/models/openpangu_mtp.py` | `OpenPanguMTP` |\n| `vllm/model_executor/models/step3p5_mtp.py` | `Step3p5MTP` |\n| `vllm/models/deepseek_v32/amd/mtp.py` | `DeepseekV32MTP` |\n| `vllm/models/deepseek_v32/nvidia/mtp.py` | `DeepseekV32MTP` (`Dots3NoteMTP` inherits) |\n| `vllm/models/hy_v4/nvidia/mtp.py` | `HYV4MTP` (docstring already noted \"always runs on a single rank\") |\n| `vllm/models/inkling/amd/mtp.py` | `InklingMTP` (already has `SupportsMultiModalEmbeddings`) |\n| `vllm/models/inkling/nvidia/mtp.py` | `InklingMTP` (same) |\n\n## Rationale\n\nSame as prior PRs: the drafter is only ever built on the last PP stage (`is_last_pp_rank` guard in `model_runner.py`), so the SupportsPP-shaped `forward` is never called. The mixin + factory just satisfy the config verification path that copies `pipeline_parallel_size` from the target into `draft_parallel_config`, so `vllm serve <model> --pipeline-parallel-size N --speculative-config method:mtp` does not fail during `create_engine_config` with `NotImplementedError: Pipeline parallelism is not supported for this model. Supported models implement the SupportsPP interface.`\n\n## Duplicate-work check\n\nSearched open PRs for each target file, ",
      "labels": [
        "bug",
        "deepseek",
        "inkling"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM",
        "GLM4"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-55095",
      "repository": "vllm-project/vllm",
      "number": 55095,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix] Fall back to full decode graphs for noncompiled models",
      "bodyExcerpt": "- Apply the fallback only to platform-selected model paths without an active torch-compile boundary.\n- Preserve ROCm's compiled MRV1 defaults and the generic compiled implementations used by DeepSeek V3.2 and GLM DSA models.\n- Default unset graph modes to `FULL_DECODE_ONLY` at O2/O3 and `NONE` at O0/O1 when breakable graphs are unavailable.\n- Default DeepSeek V4 MRV2 to `NONE` on gfx950 because full-decode capture has an unresolved accuracy risk in [#52644](https://github.com/vllm-project/vllm/issues/52644).\n- Preserve safe explicit graph choices, reject compile/adaptive combinations that can require unavailable piecewise capture, and clear stale capture sizes.\n- Causal changes were [#52861](https://github.com/vllm-project/vllm/pull/52861), [#53155](https://github.com/vllm-project/vllm/pull/53155), and [#53183](https://github.com/vllm-project/vllm/pull/53183); [#54852](https://github.com/vllm-project/vllm/pull/54852) exposed the combination.\n- Draft [#54127](https://github.com/vllm-project/vllm/pull/54127) changes global breakable-graph policy but not this ROCm fallback, while [#52968](https://github.com/vllm-project/vllm/pull/52968), [#53166](https://github.com/vllm-project/vllm/pull/53166), and [#54038](https://github.com/vllm-project/vllm/pull/54038) are workload-specific overrides.\n\nThese model paths lack an active torch-compile boundary and normally rely on breakable CUDA graphs for piecewise capture. ROCm disables breakable graphs by default for performance, so O2 otherwise selects an unavailable `PIECEWISE` path. On gfx942 the fallback keeps uniform decode captured through `FULL_DECODE_ONLY`, while gfx950 DeepSeek V4 stays eager until its graph-pool correctness issue is resolved. Safe explicit modes remain available, while impossible piecewise and adaptive-verification combinations now fail early or normalize to an executable mode. All 69 focused cases and applicable pre-commit hooks passed, and the broader configuration run passed 289 tests with six skips af",
      "labels": [
        "bug",
        "rocm",
        "speculative-decoding",
        "needs-rebase",
        "mrv2"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-55270",
      "repository": "vllm-project/vllm",
      "number": 55270,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix] GLM-5.3-Flash: launch the kpool paged MQA logits in the varlen mode its schedule was built with",
      "bodyExcerpt": "> [!NOTE]\r\n>  Update: the original hang issue is caused by a stale deepgemm 2.5.0 version. Updating to 2.6.1 resolved it. This PR is still a little bit helpful to get metadata aligned, but it did not fix anything (there is nothing to fix)\r\n\r\n## Purpose\r\n\r\nMake the kpool indexer's DeepGEMM paged-MQA launch agree with the schedule metadata it was built with. On SM100 `DeepseekV32IndexerMetadataBuilder` schedules the paged MQA logits in varlen mode (per-token rows tagged with request indices, `get_paged_mqa_logits_metadata(..., indices=...)`), and `sparse_attn_indexer.py` passes `indices=decode_metadata.indices` to `fp8_fp4_paged_mqa_logits`. The kpool indexer did not, so DeepGEMM selects the non-varlen `next_n=1` kernel and runs it on a varlen schedule. Under MTP a request's verify rows share one request index, so the two decompositions differ whenever a request's rows straddle a 256-state split boundary (1024 tokens at `index_kpool=4`).\r\n\r\nWhat that mismatch does depends on the DeepGEMM revision, verified with a standalone kernel script (same fp8 cache, block table and weights; schedule built with `indices`; launched with and without `indices`; compared against a pure non-varlen reference):\r\n\r\n| DeepGEMM | launch without `indices` on a varlen schedule |\r\n|---|---|\r\n| 2.5.0 (891d57b, what a stale local FetchContent checkout builds) | deadlock: the exact-end-match scheduler walks past an end it can never hit; the TMA warp exits and the math warps spin on the Q-stage mbarrier. Reproduced 3x end to end (GLM-5.3-Flash MTP3 GSM8K hangs within 4 min, user-triggered CUDA core dumps show the single live CTA) and in the 40-row script. |\r\n| 2.6.1 (8b1392b9, the repo pin) | tolerated: no hang, logits bit-identical to the reference over 640 rows including 80 straddling requests; GLM MTP3 GSM8K on `main` runs fine (0.9704 / 0.9731). |\r\n\r\nSo with the pinned DeepGEMM this is a latent contract violation rather than a live bug. The one-line fix matches the non-kpool indexer, removes t",
      "labels": [
        "bug",
        "ready",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55275",
      "repository": "vllm-project/vllm",
      "number": 55275,
      "type": "pr",
      "category": "NEW_SUPPORT",
      "categoryValid": true,
      "ruleCategory": "NEW_SUPPORT",
      "classificationConsistent": true,
      "title": "[Model] Add GLM and compressed-tensors support to MegaMoE",
      "bodyExcerpt": "## Purpose\r\n\r\nExtend the `deep_gemm_mega_moe` backend to the DeepSeek-derived GLM model\r\nimplementations and support compressed-tensors NVFP4 expert checkpoints used by\r\nGLM, DeepSeek V4, and Kimi K3.\r\n\r\nThis change:\r\n\r\n- adds MegaMoE adapters for DeepSeek V2/GLM, GLM-5-next, DeepSeek V4, and Kimi K3,\r\n  including their MTP paths;\r\n- selects the MegaMoE weight format per layer so dense and routed-expert layers\r\n  can use their checkpoint-native formats;\r\n- converts compressed-tensors NVFP4 routed-expert weights to DeepGEMM's\r\n  E2M1/UE8M0 FP4 representation while preserving non-expert input scales;\r\n- preserves grouped routing, deferred-reduction, and sequence-parallel semantics;\r\n- covers the model adapters and weight-format behavior with focused unit tests.\r\n\r\n## Duplicate-work check\r\n<details><summary>Details</summary>\r\n<p>\r\n\r\nThis does not duplicate the open related work:\r\n\r\n- #40833 introduces the general DeepGEMM MegaMoE kernel integration; it does not\r\n  provide these GLM/DeepSeek/Kimi model adapters or compressed-tensors loading.\r\n- #42844 experiments with FP4 activation dispatch for DeepSeek V4 using an\r\n  external DeepGEMM package; this PR uses the existing FP8-activation/FP4-weight\r\n  MegaMoE path and focuses on checkpoint/model integration.\r\n- #53527 targets FP8 DeepSeek V4 on SM90; this PR targets compressed-tensors\r\n  NVFP4 models and was validated on GB200/SM100.\r\n</p>\r\n</details> \r\n\r\n## Test plan and results\r\n\r\n<details><summary>Details</summary>\r\n<p>\r\n\r\n\r\n\r\n\r\nLocal checks after merging current `main`:\r\n\r\n```text\r\npre-commit run --files <all nine PR files>\r\n# all hooks passed, including ruff, formatting, mypy, and configuration guards\r\n\r\n.venv/bin/python -m pytest tests/models/test_deep_gemm_bf16_mega_moe.py -q\r\n# 15 passed\r\n```\r\n\r\nGB200 kernel correctness:\r\n\r\n- Random FP8 x FP4 MegaMoE, four ranks: relative L2 0.0252-0.0276 and cosine\r\n  similarity greater than 0.9996.\r\n- Exact GLM topology (EP8 across two GB200 nodes, hidden size 6144,\r\n  intermedi",
      "labels": [
        "deepseek",
        "nvidia",
        "DSv4",
        "kimi",
        "k3",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55398",
      "repository": "vllm-project/vllm",
      "number": 55398,
      "type": "pr",
      "category": "DOCS",
      "categoryValid": true,
      "ruleCategory": "DOCS",
      "classificationConsistent": true,
      "title": "[6/N] HiSparse: support direct GPU landing for P/D transfers",
      "bodyExcerpt": "## Why\n\nHiSparse P/D should receive directly into final GPU-resident pages when the decoder has capacity, while retaining host landing when it does not. This keeps the decision per request and avoids forcing every imported prefix through host memory.\n\nThis does not duplicate an existing PR; no open adaptive HiSparse P/D landing PR was found.\n\n## Status\n\nThis is a draft with an intermediate NIXL host-registration diagnostic committed. The direct/host P/D smoke is still being completed.\n\n## Tests\n\n- `python -m pytest tests/v1/kv_connector/unit/test_nixl_connector_hma.py::test_nixl_keeps_device_block_count_with_hisparse_host_pool tests/v1/kv_connector/unit/test_nixl_connector_hma.py::test_hisparse_host_import_keeps_host_blocks_out_of_gpu_regions -q` (2 passed)\n- GLM P/D harness startup exercised on 8 B300 GPUs; currently exposes the remaining host DRAM registration ownership issue before request transfer.\n\nAI assistance was used. Every changed line and final GPU validation must be reviewed by the human submitter before this PR is marked ready.",
      "labels": [
        "documentation",
        "speculative-decoding",
        "torch.compile",
        "ci/build",
        "deepseek",
        "cpu",
        "kv-connector",
        "nvidia",
        "mrv2",
        "dflash",
        "scheduler",
        "kv-cache-manager",
        "glm",
        "minimax"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-55450",
      "repository": "vllm-project/vllm",
      "number": 55450,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][Core] Retire Mamba states across null gaps",
      "bodyExcerpt": "## Purpose\r\n\r\nFix align-mode Mamba retirement stopping at null gaps and leaking older states. Track the retired prefix to avoid rescanning it.\r\n\r\nSplit from #55435. #54076 changes chunk splitting; #53803 changes checkpoint retention. Neither fixes this leak. AI-assisted contribution.\r\n\r\n## Test Plan\r\n\r\n```bash\r\n.venv/bin/python -m pytest \\\r\n  tests/v1/core/test_single_type_kv_cache_manager.py \\\r\n  tests/v1/core/test_deferred_block_free.py \\\r\n  tests/v1/core/test_swa_inflight_window_free.py \\\r\n  tests/v1/core/test_mamba_align_chunk_split.py \\\r\n  tests/v1/core/prefix_cache -q --tb=short\r\npre-commit run --files vllm/v1/core/single_type_kv_cache_manager.py tests/v1/core/test_single_type_kv_cache_manager.py\r\n```\r\n\r\n## Test Result\r\n\r\n- 147 passed; pre-commit passed.\r\n- Manager tests on DGX Spark (CPU-only): main `144e79c` **3 failed, 19 passed** → PR **22 passed**.\r\n- 480k-token prefill replay, 8192 batch budget, MTP5: with 3584-token blocks and 1/2 in-flight chunks, peak states per group **71 → 8/9**. With 4608-token blocks, unchanged at **8/9**.\r\n\r\nDGX Spark replay with the real GLM-5.3-Flash TP2/MTP5 cache layout, 3,584-token blocks and two in-flight chunks:\r\n\r\n| | Peak retained blocks per Mamba group | Logical pool capacity held |\r\n|---|---:|---:|\r\n| Base `9ea8f3f` | 71 | 2,006.44 MiB |\r\n| PR `98ae7c0` | 9 | 254.34 MiB |\r\n\r\nEach shared pool block accounts for 29,632,512 bytes (2,351,104 bytes per padded Mamba layer page), recovering 1,752.11 MiB/group. Both arms return all blocks on cleanup; \r\n",
      "labels": [
        "bug",
        "ready",
        "kv-cache-manager"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-55528",
      "repository": "vllm-project/vllm",
      "number": 55528,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][KV Cache][MLA] Align packed block strides for V3.2 sparse MLA",
      "bodyExcerpt": "## Problem\n\nOrdinary V3.2 sparse MLA and its indexer can resolve to `BLHNC` even when the packed block stride is not a whole number of every segment's physical rows. Binding/warmup then asserts; sparse row indexing requires the same invariant. This addresses the ordinary native-64-row allocation problem in #55431 and the geometry prerequisite discussed in #55434.\n\n## Root cause\n\nThe allocator sums logical page bytes without encoding sparse consumers' row-addressing requirements. Physical rows include format metadata: 656 bytes for DS-FP8, 352 for DS-NVFP4, and 132 for the FP8 indexer. A heterogeneous sum need not divide by each row width.\n\n## Fix\n\nDeclare `MLAAttentionSpec.block_stride_alignment_bytes` from the physical row widths and align the total packed block stride to their LCM. Carry that stride through allocation, capacity checks, overrides, auto-fit, and PP re-planning; merge requirements by LCM.\n\nPreserve logical page sizes, integer-row index units, and layer-compact behavior. Declare BLHNC alongside existing supported layer-compact layouts; retain rejection of other unvalidated block-outermost layouts and unsupported manager/kernel splitting. No kernel, NIXL, or OffloadingConnector implementation changes.\n\n## Padding / capacity cost\n\nPadding is inventory-dependent and bounded by one alignment quantum. For a **synthetic 51-MLA / 50-indexer inventory**, native 64-token blocks, and a fixed **64 GiB** allocation budget:\n\n| MLA format | Content bytes/block | Aligned stride | Tail bytes | Usable-capacity loss |\n|---|---:|---:|---:|---:|\n| BF16 | 4,182,528 | 4,194,432 | 11,904 | 0.2861% |\n| Ordinary FP8 | 2,302,464 | 2,306,304 | 3,840 | 0.1675% |\n| DS-FP8 | 2,563,584 | 2,576,112 | 12,528 | 0.4887% |\n| DS-NVFP4 | 1,571,328 | 1,571,328 | 0 | 0% |\n\nThe comparison disables/enables only the row-alignment requirement and subtracts the reserved null block from usable capacity. This inventory is not a measured production GLM configuration. Smaller inventories can cost mo",
      "labels": [
        "bug",
        "documentation",
        "deepseek",
        "kv-connector",
        "nvidia",
        "kv-cache-manager"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 5,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55543",
      "repository": "vllm-project/vllm",
      "number": 55543,
      "type": "pr",
      "category": "OTHER",
      "categoryValid": true,
      "ruleCategory": "OTHER",
      "classificationConsistent": true,
      "title": "[Attention][MLA] fp8_ds_mla for rope-free NoPE models (head_dim 512) on SM90 via zero-padded-rope envelope",
      "bodyExcerpt": "## 🚀 Description\n\nEnables `fp8_ds_mla` KV cache for rope-free NoPE sparse-MLA models (`qk_rope_head_dim=0`, head_dim 512, e.g. GLM-5.3-Flash) on **SM90/Hopper** via the FlashMLA sparse backend, served through a zero-padded 576/656B envelope.\n\nToday on H200, GLM-5.3-Flash must either use bf16 KV (~2x memory) or the plain per-tensor-fp8 `FLASHINFER_MLA_SPARSE_SM90` backend, which returns no LSE and therefore cannot run DCP (decode context parallel) or compose with the MTP fused-draft path. The only SM90 sparse-MLA backend with `fp8_ds_mla` support (`FLASHMLA_SPARSE`, #47090) hard-rejects head_dim 512 because the FlashMLA kernels assume the fixed 576 geometry.\n\n**Approach**: keep the 656B packed row layout unchanged and zero-pad the rope section:\n- The rope bytes [640:768] of every cached token are written as bf16 zeros, and the query-side `q_pe` is zero-padded to 64 dims. Since RoPE is baked into the cache at write time, `q_pe · 0 = 0` exactly in IEEE arithmetic — the padding contributes nothing to any logit. This was verified end-to-end with a standalone kernel probe on H20 (SM90): output rel-err vs a bf16 NoPE-512 reference matches the fp8 block-quant noise floor (~2.5e-2), and a garbage-rope control diverges, proving the test has teeth.\n- `FLASHMLA_SPARSE.get_supported_head_sizes()` → `[576, 512]`; `supports_combination` accepts 512 **only** for quantized DS-MLA dtypes (`fp8_ds_mla`/`nvfp4_ds_mla`), so bf16 NoPE-512 traffic keeps flowing to the FlashInfer/TRITON backends.\n- A shared helper in `mla_attention.py` promotes the rope dims of rope-free MLA layers to the padded 576 geometry when the effective KV dtype is quantized DS-MLA on `FLASHMLA_SPARSE`, emitting persistent zero buffers (CUDA-graph capture stable). Models with rope dims > 0 are untouched.\n- `SparseMLACommonMetadataBuilder` now derives its MLA dims from the instantiated runtime layers instead of the raw HF config, so the chunked-prefill workspace is sized from the promoted (576) geometry — without th",
      "labels": [
        "needs-rebase",
        "nvidia"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-55737",
      "repository": "vllm-project/vllm",
      "number": 55737,
      "type": "pr",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[Perf][GLM-5.3-Flash] Use FlashKDA for KDA chunked prefill (1.7-3.8x faster than the Triton chunk path)",
      "bodyExcerpt": "## Purpose\r\n\r\nOne of three independent GLM-5.3-Flash perf PRs \r\n\r\nUse **FlashKDA** (`vllm._flashkda_C`, already built and used by Kimi-K3) for GLM-5.3-Flash's KDA chunked prefill instead of the ~15-kernel Triton `chunk_kda_with_fused_gate` path.\r\n\r\nFlashKDA implements the same bounded-gate KDA recurrence GLM-5.3-Flash uses (`lower_bound * sigmoid(exp(A_log) * (g + dt_bias))`, in-kernel q/k l2norm, raw beta logits), so it is a drop-in. On GB300 per KDA layer (H=16/rank, D=128, `bench/flashkda_vs_chunk.py`):\r\n\r\n| shape | Triton chunk path | FlashKDA |\r\n|---|---|---|\r\n| 8 x 2048 tokens | 183 us | 49 us |\r\n| 4 x 8192 | 555 us | 146 us |\r\n| 1 x 16384 | 1371 us | 787 us |\r\n\r\nSelection: automatic when supported (SM90/SM10x/SM12x, bf16, head_dim 128, bounded gate); `additional_config.kda_prefill_backend = triton` keeps the old path (`flashkda` forces it). The output is written straight into the layer buffer (no merge copy) and the pre-sigmoid beta cast is not needed on this path. Spec-decode tokens themselves keep the Triton recurrent kernel (`num_accepted_tokens` rollback); in a step that carries both, FlashKDA runs the prefill segment into a workspace buffer and the outputs are scattered back by `non_spec_token_indx` / `spec_token_indx` (same scheme as the Kimi-K3 KDA layer; the merge writes straight into `core_attn_out`).\r\n\r\n## Test Plan\r\n\r\n- Unit / kernel tests listed under \"Tests\" below.\r\n- Serving benchmark, all builds back-to-back in one session on 4x GB300 (SM100), TP4, prefix caching disabled:\r\n  ```bash\r\n  vllm serve zai-org/GLM-5.3-Flash -tp 4 --attention-backend FLASHINFER_MLA_SPARSE \\\r\n    --max-model-len 69632 --max-num-seqs 256 --max-num-batched-tokens 16384 \\\r\n    --gpu-memory-utilization 0.9 --no-enable-prefix-caching\r\n  vllm bench serve --backend vllm --model glm53-flash --dataset-name random --random-range-ratio 0 \\\r\n    --ignore-eos --seed 0 --random-input-len <in> --random-output-len <out> --max-concurrency <c> \\\r\n    --num-prompts <n> --num-warmups <c>",
      "labels": [
        "ready",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 7,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55738",
      "repository": "vllm-project/vllm",
      "number": 55738,
      "type": "pr",
      "category": "PERFORMANCE",
      "categoryValid": true,
      "ruleCategory": "PERFORMANCE",
      "classificationConsistent": true,
      "title": "[Perf][GLM-5.3-Flash] Dense/masked-MHA sparse prefill for the NoPE (256, 0, 256) layout + skip the NoPE K concat",
      "bodyExcerpt": "## Purpose\n\nOne of three independent GLM-5.3-Flash perf PRs \n\nOn main, GLM-5.3-Flash's MLA layout (qk_nope 256, qk_rope 0, v 256) is unknown to the FlashAttention prefill backend and to the masked-MHA allow-list, so `MLACommonImpl` logs `No MLA prefill backend supports this model` and **every prefill token goes through the per-token top-k MQA kernel**. Profiling shows that kernel is KV-gather bound at the HBM roofline: 5.9 ms/layer per 16k-token chunk, and still 1.6 ms/layer for 8x2048 where dense MHA needs ~0.2 ms.\n\nThree commits:\n1. **Register the (256, 0, 256) dims with `FlashAttnPrefillBackend`** (7 lines). Same lines as in the SM90 wiring PR vllm-project/vllm#55385; kept separate so it can be dropped once that lands. This alone gives the dense-MHA prefill path for sequences ≤ index_topk (2048).\n2. **Masked-MHA allow-list**: `_is_masked_mha_available` only knew (128, 512, 128, 64, 128) and (64, 512, 192, 64, 256); GLM-5.3-Flash is (64, 512, 256, 0, 256). The kernels are identical for qk_head_dim 256 / v 256, so sequences between 2k and the existing FLASHINFER_MLA_SPARSE thresholds (36k at TP4, 64k at TP8) now use FA4 masked MHA instead of the per-token top-k MQA kernel.\n3. **NoPE K concat**: `_concat_k_nope_k_pe` returns `k_nope` directly when there is no RoPE part instead of allocating and copying a same-sized tensor (134 MB/layer per 16k chunk).\n\n## Test Plan\n\n- Unit / kernel tests listed under \"Tests\" below.\n- Serving benchmark, all builds back-to-back in one session on 4x GB300 (SM100), TP4, prefix caching disabled:\n  ```bash\n  vllm serve zai-org/GLM-5.3-Flash -tp 4 --attention-backend FLASHINFER_MLA_SPARSE \\\n    --max-model-len 69632 --max-num-seqs 256 --max-num-batched-tokens 16384 \\\n    --gpu-memory-utilization 0.9 --no-enable-prefix-caching\n  vllm bench serve --backend vllm --model glm53-flash --dataset-name random --random-range-ratio 0 \\\n    --ignore-eos --seed 0 --random-input-len <in> --random-output-len <out> --max-concurrency <c> \\\n    --num-prompt",
      "labels": [
        "ready",
        "needs-rebase",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 7,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55802",
      "repository": "vllm-project/vllm",
      "number": 55802,
      "type": "pr",
      "category": "OTHER",
      "categoryValid": true,
      "ruleCategory": "OTHER",
      "classificationConsistent": true,
      "title": "[Attention] Remove DCP indexer interleave guard and test TP1 output parity",
      "bodyExcerpt": "## Purpose\n\nRemove the sparse indexer's rejection of DCP with `cp_kv_cache_interleave_size > 1`, and add direct attention-output regression tests against complete, unsharded TP1 KV. This enables review of configurations such as DCP with NIXL, which uses interleave 64.\n\n**Draft: the tested attention outputs agree within the stated FP8 tolerances. Full-model outputs are numerically sensitive, and accuracy equivalence is not established.** The original single GSM8K pair scored 11 questions lower at interleave64. Three further controlled pairs scored **23 higher, 3 higher, and 10 lower**; the lower score is not consistent across repeats. The investigation below causally identifies attention rounding and index-order variability as sources of changed generations.\n\nThe production change is exactly the nine-line guard deletion in `DeepseekV32IndexerMetadataBuilder`. It is generic because this builder implements interleave/ownership arithmetic independently of model identity or KV-transfer connector. There is no GLM/NIXL allowlist or backend capability override. Separate backend and MTP compatibility checks remain unchanged. The evidence is limited to the tested implementations/configurations, not every configuration admitted by deleting the guard.\n\nThe original guard in #46076 (`c5200d3565`) cited GSM8K failure. **That historical failure has not been root-caused.** The pure-DCP defects reported in #50095 and fixed by #50005, and the non-owner-rank prefill fix in #54908, are plausible contributors; both fixes are in this branch's base. Neither establishes why interleave1 passed while larger interleaves failed. The historical result used GLM-5.2-FP8/lm_eval at concurrency128; the current GLM-5.3-NVFP4 evaluation is not a direct reproduction.\n\nDuplicate-work searches for DCP/interleave and #50095 found no other open PR combining this guard deletion with TP1 attention-output comparisons. #50005/#54908 are merged; #54541 is a backport and #52472 adds a different backend.\n\n## Tes",
      "labels": [
        "ready",
        "ci/build",
        "nvidia",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 5,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-55866",
      "repository": "vllm-project/vllm",
      "number": 55866,
      "type": "pr",
      "category": "NEW_SUPPORT",
      "categoryValid": true,
      "ruleCategory": "NEW_SUPPORT",
      "classificationConsistent": true,
      "title": "[Attention] Enable FA4 sparse MQA decode on Blackwell",
      "bodyExcerpt": "This adds `FLASH_ATTN_MLA_SPARSE_FA4` as the preferred backend for supported BF16 sparse MLA workloads at 16 or fewer query heads per rank on Blackwell; above that head count it stays opt-in. Decode batches use FA4 directly on the flat KV cache; batches containing prefill use FlashInfer. The backend maps request-local top-k indices to physical cache rows, passes each row's valid length to FA4, and registers the required warmup and selection logic.\r\n\r\nTests cover routing, backend priority order, per-row top-k lengths across the kernel's 128-wide index blocks, empty index rows and top-k buffers wider than the configured indexer width. The companion FlashAttention change supplies the decode kernel. The full GLM-5.2 TP4/TP8 serving comparison completed successfully; no model-quality evaluation was run.\r\n\r\nValidation: `pytest tests/v1/attention/test_sparse_mla_backends.py -k 'fa4_sparse or (test_sparse_backend_decode_correctness and FlashAttnFA4 and auto and (mixed_small or large_q_pure_prefill or mixed_long_q))'` — 24 passed, 18 skipped. `pytest tests/v1/attention/test_sparse_mla_priority.py` — 3 passed.\r\n\r\nFor Perf Results see PR in FA repo: vllm-project/flash-attention#195\r\n\r\nAI-assisted. The September 7 upstream search found no matching open FA4 sparse-MLA integration PR; related platform-specific work does not provide this SM100 backend.\r\n",
      "labels": [
        "needs-rebase",
        "ci/build",
        "nvidia"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-55973",
      "repository": "vllm-project/vllm",
      "number": 55973,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][CUDA] Fix the FlashInfer's fused all-reduce issue where NVLink multicast isn't available",
      "bodyExcerpt": "## Purpose\r\nFollow-up to #52861\r\nThat PR routes GLM DSA and DeepSeek V3.2 through the NVIDIA DeepSeek V3.2\r\nimplementations and adds them to `DEFAULT_BREAKABLE_CUDAGRAPH_ARCHITECTURES`,\r\nwhich sets `CompilationMode.NONE`. `fused_allreduce_rms_norm` now kicks into action so those\r\nmodels now probe the FlashInfer workspace for every layer.\r\nIf NVLink multicast is not available for any reason, FlashInfer's fused all-reduce workspace can't be created, and the attempt fails with CUDA error 801.\r\nget_fi_ar_workspace() caches only success: its early return is if `_fi_ar_workspace` is not None, so a failure is never remembered and every subsequent call re-issues a real fabric allocation. Callers handle the None fine by falling back to a plain collective, so the failures are harmless — until one of those retries happens on a stream that is capturing a CUDA graph. The failing driver call invalidates the capture, and the next allocation on that stream dies with `cudaStreamCaptureStatusInvalidated`. \r\n\r\nThe fix is to cache the failures so that the later calls on the same config would return None immediately. Also to explicitly not create during graph capture (is_current_stream_capturing)\r\n\r\nTo replicate: Run GLM-5.2 or Kimi-K3, or similar on a multi-node GB300 (i.e. tp=8 on 2x4GPU) on 0.29 or above (0.28 works through the deepseek_v2 rather than vllm.models.deepseek_v32)\r\n\r\n## Test Plan\r\n\r\nRun the repro from above and verify the models start normally\r\n\r\n## Test results\r\nGLM-5.2 tp-8 over 2 nodes, MTP-5, `FULL_AND_PIECEWISE`: fails in capture before,\r\nstarts after. Workspace attempts drop from 22 down to 1",
      "labels": [
        "bug",
        "needs-rebase",
        "nvidia"
      ],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-56107",
      "repository": "vllm-project/vllm",
      "number": 56107,
      "type": "pr",
      "category": "NEW_SUPPORT",
      "categoryValid": true,
      "ruleCategory": "NEW_SUPPORT",
      "classificationConsistent": true,
      "title": "[PCP][Spec Decode] Adds PCP support for single-module MTP and replicated DSpark.",
      "bodyExcerpt": "## Summary\n\nAdds V2 model-runner PCP support for single-module MTP and replicated DSpark.\n\n- MTP reuses the target PCP-local batch for its initial draft forward, then refreshes global-order block tables for subsequent replicated drafts.\n- DSpark stays replicated while the target is PCP-sharded.\n- PIECEWISE CUDA graphs are supported; full graphs remain rejected.\n\nThis consolidates #53653 and #53660 on current `main` with a smaller implementation.\n\n## Validation\n\nThe latest fix refreshes `input_block_tables`, which PCP's separate local gather leaves uninitialized for later MTP drafts. A GPU check with two requests and two KV-cache groups confirmed zeroed tables before the gather and correct request mappings afterward. Focused tests: 52 passed.\n\nEarlier four-GPU GLM-5.2-NVFP4 smoke tests, TP2 + PCP2 + DCP1 + EP4, with `cudagraph_mode=PIECEWISE`; logs confirmed graph capture:\n\n- MTP2, FP8 KV: 8 output tokens, 4/6 draft tokens accepted (66.7%).\n- `RedHatAI/GLM-5.2-speculator.dspark`, DSpark8, BF16 KV: 8 output tokens, 7/8 accepted (87.5%).\n\nThese acceptance figures predate the block-table fix; full-model acceptance has not been rerun after it.\n\nMTP:\n\n```bash\nvllm serve nvidia/GLM-5.2-NVFP4 \\\n  --trust-remote-code \\\n  --tensor-parallel-size 2 \\\n  --prefill-context-parallel-size 2 \\\n  --enable-expert-parallel \\\n  --moe-backend flashinfer_trtllm \\\n  --kv-cache-dtype fp8 \\\n  --speculative-config '{\"method\":\"mtp\",\"num_speculative_tokens\":2}' \\\n  --compilation-config '{\"cudagraph_mode\":\"PIECEWISE\"}'\n```\n\nDSpark:\n\n```bash\nvllm serve nvidia/GLM-5.2-NVFP4 \\\n  --trust-remote-code \\\n  --tensor-parallel-size 2 \\\n  --prefill-context-parallel-size 2 \\\n  --enable-expert-parallel \\\n  --moe-backend flashinfer_trtllm \\\n  --kv-cache-dtype bfloat16 \\\n  --speculative-config '{\"method\":\"dspark\",\"model\":\"RedHatAI/GLM-5.2-speculator.dspark\",\"num_speculative_tokens\":8,\"draft_sample_method\":\"probabilistic\",\"attention_backend\":\"FLASH_ATTN\"}' \\\n  --compilation-config '{\"cudagraph_mode\":\"PIECEWISE\"}",
      "labels": [
        "speculative-decoding",
        "ready",
        "mrv2",
        "dflash"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-56157",
      "repository": "vllm-project/vllm",
      "number": 56157,
      "type": "pr",
      "category": "NEW_SUPPORT",
      "categoryValid": true,
      "ruleCategory": "NEW_SUPPORT",
      "classificationConsistent": true,
      "title": "[PCP][DCP] Enable PCP+DCP on sparse-MLA models",
      "bodyExcerpt": "## Purpose\r\n\r\nEnable DCP to run alongside PCP on sparse-MLA models, so a deployment can shard the KV cache for decode and still split prefill query work across ranks.\r\n\r\nThis change:\r\n\r\n- gives the sparse indexer a DCP-gathered prefill path: this rank's KV shard is all-gathered across the DCP group\r\n- gives the FlashMLA-sparse backend the matching prefill path, with a rank-major gathered KV workspace and a PCP-invariant chunk plan every rank derives identically without communicating;\r\n- replicates prefills too short to fill all `2*PCP` chunks when DCP is on, which keeps the per-region row count identical on every rank — the invariant the gathered layout rests on;\r\n- adds a MoE all-reduce fast path for steps whose rows are all PCP-replicated, replacing an unconditional all-gather + reduce-scatter that duplicated every decode row `PCP` times;\r\n- relaxes the PCP CUDA graph restriction for PCP+DCP to everything up to `FULL_AND_PIECEWISE`, keeping only `FULL` over mixed batches rejected;\r\n\r\nfixes https://github.com/vllm-project/vllm/issues/53573\r\nAdditionally\r\n- fixes a pre-existing kernel/reference divergence: the top-k index kernel applied the block-table bound to prefill-workspace rows, while `_triton_convert_reference_impl` has always specified that only `-1` is invalid there. The existing test missed it because `token_indices[6, 150:160]` is an empty slice on a 128-column tensor;\r\n\r\n## Test Plan\r\n\r\n- NVIDIA GB200 x4\r\n- GLM-5.3, NVFP4 weights\r\n- TP=1, PCP=4, DCP=4\r\n\r\n## Test Result\r\n\r\nGSM8K, 1319 questions, TP1+PCP4+DCP4, `FULL_AND_PIECEWISE`. Seven runs across the\r\n\r\n| Run | Accuracy | Invalid |\r\n|---|---|---|\r\n| TP4 | 0.830 | 0.000 |\r\n| PCP4DCP4| 0.832 | 0.000 |\r\n\r\n\r\n## Benchmarks\r\n\r\nPrefill, 32768 in / 1 out:\r\n\r\n| Layout | TTFT ms | P99 TTFT ms | vs tp4 |\r\n|---|---|---|---|\r\n| tp4 | 5512.1 | 8141.1 | 1.00x |\r\n| pcp4 | 3278.1 | 4814.0 | 1.68x |\r\n| pcp4dcp4 | 3326.8 | 4888.7 | 1.66x |\r\n\r\nDecode, 1 in / 1024 out:\r\n\r\n| Layout | TPOT ms | out tok/s | vs tp4 |\r\n|---|---",
      "labels": [
        "ready",
        "deepseek",
        "nvidia",
        "mrv2"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-56176",
      "repository": "vllm-project/vllm",
      "number": 56176,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[ROCm] [Bugfix] Enable Load and Inference of GLM-5.3-Flash Quark MXFP4 Checkpoint",
      "bodyExcerpt": "\r\n\r\n## Purpose\r\n\r\nThe Quark checkpoint `amd/GLM-5.3-Flash-Quark-MXFP4` (`Glm5NextForConditionalGeneration`, global MXFP4 quant with attention projections kept as source block-FP8/BF16). Two independent bugs break weight load. This PR fixes both and the Quark MXFP4 checkpoint now loads and serves with accuracy on par with the source checkpoint (`zai/GLM-5.3-Flash`).\r\n\r\n## Bugs\r\n\r\n1. Dense-MLP `gate_up_proj` resolves to the wrong quant scheme:\r\nThe class inherits `packed_modules_mapping = {\"gate_up_proj\": [\"gate_up_proj\"]}` from `Glm4vForConditionalGeneration`. This identity mapping is correct for GLM-4.1V (whose checkpoint stores a pre-fused `gate_up_proj` tensor), but GLM-5.3-Flash stores gate/up as **separate** tensors (both the [Quark ](https://huggingface.co/amd/GLM-5.3-Flash-Quark-MXFP4)and the [base](https://huggingface.co/zai-org/GLM-5.3-Flash) checkpoints). With the identity entry, `QuarkConfig` falls back to `global_quant_config` (MXFP4) instead of the per-layer block-FP8 in `layer_quant_config` and builds the layer as MXFP4 and mismatches the FP8 checkpoint tensor at load.\r\n\r\n\r\n2. MLA projection scale name mismatch:\r\nThe MLA `q_a` / `kv_a` / `q_b` / `o` projections are dequantized on load by `_try_load_fp8_attn_proj`, which only recognized scales named `weight_scale_inv`. Quark checkpoints export `weight_scale`, so the scale fell through and the fused-qkv-a stacked mapping could not find its target.\r\n\r\n## Changes (`vllm/model_executor/models/glm5next/nvidia/model.py`)\r\n\r\n1. Override `packed_modules_mapping` on `Glm5NextForConditionalGeneration` so the  fused `gate_up_proj` expands to its real shard names `[\"gate_proj\", \"up_proj\"]` (matching the `Glm4vMoeForConditionalGeneration` sibling / `glm4_moe` / `deepseek_v2`).\r\n2. In `_try_load_fp8_attn_proj`, accept the Quark scale name:\r\n   `is_scale = \"weight_scale_inv\" in name or name.endswith(\".weight_scale\")`.\r\n\r\n## Related PRs and Issues:\r\n\r\nBug 1 was initially reported in https://github.com/vllm-project/vllm/",
      "labels": [
        "bug",
        "rocm",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 8,
      "matchedTerms": [
        "GLM",
        "GLM-4",
        "GLM4"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-56285",
      "repository": "vllm-project/vllm",
      "number": 56285,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][Frontend] Improve Responses API compatibility for Codex clients",
      "bodyExcerpt": "## Purpose\r\n\r\nRunning the Codex Responses-API conformance suite (26 cases; the Codex CLI exercises the same shapes) against `vllm serve GLM-5.3-Flash --tool-call-parser glm47 --reasoning-parser glm45` failed 11 of 26 cases on `main`. This PR fixes the four server-side defects behind them on the non-Harmony (`SimpleContext`) path:\r\n\r\n1. **Streamed items and the final response disagreed.** `responses_stream_generator` re-parsed the complete text to build `response.completed`, so `response.output_item.done` items had bare-uuid ids, `status=\"completed\"` reasoning items and a stray `summary` field, while the final `output` carried fresh `rs_`/`msg_`/`fc_` ids and `chatcmpl-tool-*` call ids. Clients that pair `output_item.done` with the final output (Codex does, by `call_id`) saw two identities per item. The final response now reuses the streamed items (resolving the TODO in `responses_full_generator`), streamed ids use the same prefixes as non-streaming, `response.function_call_arguments.done` is always emitted, and the streamed message item carries the `output_text` logprobs that were sent on the deltas so `include=[\"message.output_text.logprobs\"]` still shows them in `response.completed`.\r\n\r\n2. **`custom` tools were unusable.** They were filtered out of the rendered tool list; with `tool_choice=\"required\"` xgrammar's `get_glm_4_7_structural_tag` received zero function tools and crashed with `AssertionError` (HTTP 500); replaying `custom_tool_call` / `custom_tool_call_output` raised \"Unsupported input item type\". Custom tools are now offered to the model as a single-string function shim (`{\"input\": \"<payload>\"}`, grammar formats described in the tool description), model calls to them are emitted as `custom_tool_call` items with `response.custom_tool_call_input.delta/done` events (payload un-escaped incrementally so deltas stay a prefix of the final `input`), history is replayed through the same shim, and `{\"type\": \"custom\", \"name\": ...}` is accepted as a named `tool_cho",
      "labels": [
        "bug",
        "frontend",
        "needs-rebase",
        "tool-calling",
        "gpt-oss"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM",
        "GLM4"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-56327",
      "repository": "vllm-project/vllm",
      "number": 56327,
      "type": "pr",
      "category": "NEW_SUPPORT",
      "categoryValid": true,
      "ruleCategory": "NEW_SUPPORT",
      "classificationConsistent": true,
      "title": "[LoRA][Model] Add LoRA support for GLM-5.3-Flash",
      "bodyExcerpt": "## Purpose\r\n\r\nEnable LoRA adapters on GLM-5.3-Flash (`Glm5NextForConditionalGeneration`, added in #53906).\r\nThe class already inherits `SupportsLoRA` from `Glm4vForConditionalGeneration`, but it\r\ninherits Glm4v's `packed_modules_mapping` too, so adapters that target the HF submodule\r\nnames cannot be mapped onto the fused runtime projections. Three changes:\r\n\r\n1. **`packed_modules_mapping` for `Glm5NextForConditionalGeneration`**\r\n   (`vllm/models/glm5next/nvidia/model.py`). Maps each fused runtime linear to its\r\n   adapter-visible submodules: `gate_up_proj` -> gate/up, the 6-way KDA projection\r\n   `in_proj_qkvbfg_a` -> q/k/v/b/f_a/g_a, `fused_qkv_a_proj` -> q_a/kv_a_proj_with_mqa,\r\n   `wk_weights_proj` -> wk/weights_proj, and the vision `qkv`/`qkv_proj` -> q/k/v.\r\n\r\n2. **Accept per-rank `lora_b` slices in `MergedColumnParallelLinearWithLoRA.slice_lora_b`**\r\n   (`vllm/lora/layers/column_parallel_linear.py`). When an incoming `lora_b` slice already\r\n   has exactly this rank's shard rows, use it as-is instead of re-slicing by\r\n   `shard_id`. Re-slicing indexes past the end for `tp_rank > 0`, yields an empty tensor,\r\n   and the stacking loop silently copies nothing, so those ranks run with zero LoRA-B.\r\n   Two callers hit this:\r\n   - the KDA projection's replicated shards (`f_a`, `g_a`): `_Glm5NextMergedColumnParallelLinear`\r\n     pre-multiplies their `output_sizes` by `tp_size`, so the per-rank shard is the full\r\n     `head_dim` and every rank needs the whole tensor;\r\n   - RL frameworks that reshard adapters from `train_tp` to `gen_tp` (verl) and hand vLLM\r\n     already-sharded weights.\r\n\r\n3. **Drop the C-contiguity asserts in the Triton LoRA op wrappers**\r\n   (`lora_shrink_op.py`, `lora_shrink_fp8_op.py`, `lora_expand_op.py`,\r\n   `lora_expand_fp8_op.py`). The shrink/expand kernels take and apply explicit strides for\r\n   every dim of `inputs` and `output_tensor` (`do_shrink_kernel` / `do_expand_kernel` in\r\n   `kernel_utils.py`), so the wrappers only need the rank check",
      "labels": [
        "nvidia",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 8,
      "matchedTerms": [
        "GLM",
        "GLM4"
      ],
      "scenarios": [
        "TRAINING"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-56349",
      "repository": "vllm-project/vllm",
      "number": 56349,
      "type": "pr",
      "category": "NEW_SUPPORT",
      "categoryValid": true,
      "ruleCategory": "NEW_SUPPORT",
      "classificationConsistent": true,
      "title": "[ROCm] Auto-enable breakable CUDA graphs for DeepseekV41ForCausalLM",
      "bodyExcerpt": "## Summary\n- On CUDA, architectures in `DEFAULT_BREAKABLE_CUDAGRAPH_ARCHITECTURES` auto-enable `VLLM_USE_BREAKABLE_CUDAGRAPH=1`. On ROCm that helper currently returns an empty set, so DeepSeek-V4.1-Flash dies at capture: `piecewise CUDA graphs (cudagraph_mode=FULL_AND_PIECEWISE) unavailable, model is not torch-compiled and breakable CUDA graph is off`.\n- `DeepseekV41ForCausalLM` does not support `torch.compile`. The ROCm sparse SWA backend only reports `AttentionCGSupport.UNIFORM_BATCH`, which downgrades `FULL` to `FULL_AND_PIECEWISE`. Breakable CUDA graphs are the path that can start.\n- Keep V3.2, V4, and GLM off the ROCm default. Opt V4.1 in. Also add it to the CUDA default list.\n- On MI355X TP4 with `public.ecr.aws/q9t5s3a7/vllm-release-repo:79a7108d9aea27ddab99ce1779290d300b17fc23-rocm` and `VLLM_USE_BREAKABLE_CUDAGRAPH=1`, chat `17*19` returned `323`, and an 80-token count request completed with no worker death.\n- Recipe-side env is in vllm-project/recipes#953.\n\n## Test plan\n- [x] `test_rocm_keeps_compiled_deepseek_defaults` now asserts `DeepseekV41ForCausalLM` is in the ROCm default set\n- [ ] Run `tests/test_config.py::test_rocm_keeps_compiled_deepseek_defaults`\n- [ ] `vllm serve deepseek-ai/DeepSeek-V4.1-Flash` on gfx950 without forcing the env, confirm capture starts\n\n\nMade with [Cursor](https://cursor.com)",
      "labels": [
        "rocm",
        "deepseek",
        "nvidia",
        "DSv4"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-56403",
      "repository": "vllm-project/vllm",
      "number": 56403,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][Frontend] Constrain non-strict GLM tool calls with EBNF grammar",
      "bodyExcerpt": "## Purpose\r\n\r\nFix GLM-4.7-family tool calling when strict tool calling is disabled.\r\n\r\nWhen `VLLM_ENFORCE_STRICT_TOOL_CALLING=0` and no tool is marked `strict: true`, the glm47 parser currently falls back to unconstrained generation. This means `auto`, `required`, and named tool choices can emit malformed GLM tool-call XML or arguments that do not follow the provided tool schema.\r\n\r\nThis PR adds a parser-provided EBNF grammar for the non-strict GLM tool-calling path.\r\n\r\nThe grammar constrains the complete assistant turn rather than only the tool arguments. It covers:\r\n\r\n- optional reasoning / thinking output;\r\n- normal assistant text;\r\n- one or more GLM `<tool_call>` blocks;\r\n- tool names from the request;\r\n- shallow argument typing derived from each tool's JSON schema.\r\n\r\nFor example, instead of allowing the model to freely generate the native GLM tool syntax, the parser installs a grammar that constrains the complete output shape while still allowing normal reasoning and text before tool calls.\r\n\r\n### Request handling\r\n\r\n`Glm47MoeModelToolParser.adjust_request()` now installs this grammar when:\r\n\r\n- tools are provided;\r\n- `tool_choice != \"none\"`;\r\n- `VLLM_ENFORCE_STRICT_TOOL_CALLING` is disabled;\r\n- no tool is explicitly `strict: true`;\r\n- no structured output / response-format constraint is already present.\r\n\r\nExisting strict-tool behavior is unchanged and continues to use structural tags. \r\n\r\nNamed tool choice restricts the generated grammar to the selected tool, while `auto` and `required` use the available request tools. Both Chat Completions and Responses API tools are supported, including Responses API namespace tools.\r\n\r\n### Full-assistant grammar\r\n\r\nThe new `vllm/tool_parsers/glm_grammar.py` generates an EBNF grammar covering the full assistant output.\r\n\r\nThis is important because GLM reasoning, free-form content, and native tool calls share the same generated sequence. Applying a grammar only to the arguments would incorrectly constrain reasoning or leave",
      "labels": [
        "bug",
        "frontend",
        "tool-calling",
        "kimi",
        "k3",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 7,
      "matchedTerms": [
        "GLM",
        "GLM-4",
        "GLM4"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-56418",
      "repository": "vllm-project/vllm",
      "number": 56418,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix][Frontend] Preserve custom_tool_call on non-Harmony Responses routes",
      "bodyExcerpt": "## Summary\n- Non-Harmony Responses was coercing `type=custom` tools into `function_call`, which breaks Codex-style `apply_patch` clients (Fixes #49724; reproduced on DeepSeek V4 Flash).\n- Present custom tools to shared function parsers as a single JSON string field `input`, then unwrap back to native `ResponseCustomToolCall` / `response.custom_tool_call_input.*` (item ids use `ctc_`).\n- Grammar `format` is appended to the tool description; schema lookup, xgrammar structural tags, and named `tool_choice` with `type=custom` all see the same shim. `tool_choice=none` keeps main's default of still including tools.\n\n## Related\n- Opposite approach: #49882 (reject custom on non-Harmony).\n- History-only crash: #48885.\n- #56285 also preserves custom tools on non-Harmony Responses, with additional GLM-oriented coverage. If that PR lands first, this one can be closed or reduced to whatever remains.\n\n## Test plan\n- [x] `test_responses_utils.py` (shim, namespace flatten, grammar, history, decode, `tool_choice=none`)\n- [x] `test_streaming_events.py` (prefix decode; concatenated deltas == `done.input`)\n- [x] `test_tool_choice_required.py` (`TestCustomToolShim`)\n- [x] `test_structural_tag_registry.py` (DeepSeek V4 required custom)\n- [x] End-to-end `vllm serve` against DeepSeek V4 Flash + Codex `apply_patch`\n\n## Test result\n316 passed on the four files above, via Docker `vllm/vllm-openai:v0.25.1` overlaying this tree (GPU, `--noconftest`).",
      "labels": [
        "bug",
        "frontend",
        "tool-calling",
        "gpt-oss"
      ],
      "filterDisposition": "review",
      "relevanceScore": 4,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [
        "INFERENCE"
      ],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    },
    {
      "id": "vllm-pr-56447",
      "repository": "vllm-project/vllm",
      "number": 56447,
      "type": "pr",
      "category": "FIX",
      "categoryValid": true,
      "ruleCategory": "FIX",
      "classificationConsistent": true,
      "title": "[Bugfix] Fix GLM-OCR MTP position masking during CUDA graph capture",
      "bodyExcerpt": "Handle both one-dimensional runner positions and two-dimensional MRoPE positions with a broadcast masked fill. Add CUDA graph replay coverage for per-token masking.\r\n\r\nValidated: isolated GPU regression cases passed for both position layouts; five deterministic OCR outputs matched the non-MTP baseline, including four concurrent requests.",
      "labels": [
        "bug",
        "ready",
        "nvidia",
        "glm"
      ],
      "filterDisposition": "eligible",
      "relevanceScore": 6,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": []
    },
    {
      "id": "vllm-pr-56474",
      "repository": "vllm-project/vllm",
      "number": 56474,
      "type": "pr",
      "category": "NEW_SUPPORT",
      "categoryValid": true,
      "ruleCategory": "NEW_SUPPORT",
      "classificationConsistent": true,
      "title": "[9/N][Attention] Support adaptive verification in FlashMLA Sparse",
      "bodyExcerpt": "## Summary\n\n- allow HiSparse-backed FlashMLA Sparse to capture nonuniform adaptive-verification batches\n- preserve packed variable-length decode rows as one MQA batch instead of inventing uniform request boundaries\n- derive request rows from device metadata without a device-to-host synchronization\n\nThis is split from the core HiSparse PR because FlashMLA Sparse does not support adaptive verification on main; HiSparse can otherwise use the existing FlashInfer Sparse MLA support.\n\n## Duplicate work\n\nI searched open PRs for FlashMLA Sparse adaptive-verification support and found no duplicate. #52988 concerns Kimi-K3 rather than FlashMLA Sparse or HiSparse.\n\n## Validation\n\n- `pre-commit run --files tests/v1/attention/test_sparse_mla_backends.py vllm/model_executor/layers/attention/sparse_mla_attention.py vllm/v1/attention/backends/mla/flashmla_sparse.py`\n- `python -m pytest -q tests/v1/attention/test_sparse_mla_backends.py::test_hisparse_fp8_decode_preserves_packed_varlen_rows tests/v1/attention/test_sparse_mla_backends.py::test_flashmla_hisparse_reports_varlen_cudagraph_support tests/v1/attention/test_sparse_mla_backends.py::test_hisparse_flashmla_reorders_full_speculative_window_as_decode`\n  - 3 passed on one B300 GPU\n- GLM TP4 end-to-end smoke with `FULL_AND_PIECEWISE` and forced adaptive rejection:\n  - 25 requests completed\n  - 7,105 drafted tokens, 0 accepted\n  - 0 errors\n\nAI assistance was used. Every changed line and the validation results were reviewed by the human submitter.",
      "labels": [],
      "filterDisposition": "review",
      "relevanceScore": 3,
      "matchedTerms": [
        "GLM"
      ],
      "scenarios": [],
      "sourceMatch": true,
      "sourceMismatchFields": [],
      "warnings": [
        "relevance-review"
      ]
    }
  ]
}
